playwright>=1.49
httpx>=0.27

# 翻译与图片都走 OpenAI SDK（图片侧的第三方网关是 OpenAI 兼容形态）
# 固定大版本：3.6 是做过线级契约验证的最低版本
openai>=3.6,<4

# Pillow：产出图必须验证能解码、尺寸对、不是占位图、与原图结构相近，
# 发布侧也要在碰浏览器之前确认每张图能打开且非 0 字节 —— 没有图像库做不了
# 只加 Pillow，不加 numpy：结构相似度用纯 Python 的 dHash 汉明距离即可
Pillow>=11,<13

# tzdata：Windows 上是硬需求。本机实测 zoneinfo.TZPATH 为空、
# ZoneInfo("Europe/Berlin") 直接抛 ZoneInfoNotFoundError —— Windows 不自带
# IANA 数据库。而排期要求显式时区转换并在夏令时切换日各测一次
# 写死 +01:00/+02:00 会在切换日把帖子发到错的时刻，且没人会立刻发现
# 它是纯数据包，无原生代码、无传递依赖
tzdata>=2024.1

fastapi>=0.115
uvicorn>=0.30
