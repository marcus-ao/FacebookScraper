"""Business Suite 选择器占位。

TODO(G1): 用户跑完 ``tools/probe_publish.py`` 后，只能根据真实 probe dump
回填带来源注释的 role / accessible-name / data-testid 定位。

这里故意没有任何定位常量。React 构建期 class 或凭猜写出的 CSS 选择器，
看起来像实现，实际上会给下一位维护者一个危险的假绿灯。
"""
