# Copy this file to .env, then replace the value with your DeepSeek API key.
# Never commit .env and never paste the real key into config.toml or chat.
DEEPSEEK_API_KEY=replace_with_your_deepseek_api_key
