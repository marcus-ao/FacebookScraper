# 五阶段功能补齐与开发机真实联调

日期：2026-09-12。基线：`c67c56a`，另有 config、mirror、store、translated、hygiene、query_index 六处既有未提交修订。本轮用户已明确要求实施，并选择接续现有真实数据、开发机真实闭环、Google Trends 公开页面 CSV 采样。

## 执行记录的使用方式

本计划保留开工时的八批范围；当前验收状态统一在 [五阶段实施清单](../../OPTIMIAZATION.md)，本机实测记录在 [本轮集成记录](../../INTEGRATION_2026-09-12.md)。历史审查补充项及处理结果在 [审查交接](2026-09-12-review-handoff.md)。不要把下面的开工基线数量或“待实施”语态当作最终结果。

本轮主要代码已提交 `205c660` / `20d917a`（监测）、`d0649d1` / `5eca730` / `f02b2ed`（风险和采样）、`42c8bc9`（浏览器回归）、`7f131a3`（Trends 公开导出）、`829cbd2`（共用恢复、历史、通知、单渠道与月历集成）、`afb6784`（补扫过期处理）、`923091f`（飞书终态归档）和 `565f17c`（交接补齐、链接编辑与复审修复）。具体差异以此分支 Git 历史为准；没有把真实凭据、归档媒体或运行账本提交进版本库。

截至 2026-09-13 UTC，本机原 1,067 篇归档已备份并接续，历史分页、SQLite 一致性、当前真实月份读取及技术草稿两图核验有明确证据。原 10 份历史账本和激活文件经备份摘要复核未变。没有新 Schedule/Publish 提交、真实模型调用或飞书消息发送；技术草稿可能自动保存，接手须保留这一现场说明。

离线验收执行了全部 65 个脚本；完整运行中唯一的新增测试编码问题已修复，并与审查修复一同完成相关复验，65 个脚本均有当前通过记录。新构建下 7 个浏览器场景通过。完整运行、定向补跑、构建摘要和真实边界分别记录在集成文档，没有将补跑合并冒称单次全绿。

后续顺序仍为：人工恢复被 429 停止的访问与 `.global` 覆盖；飞书管理员和实际接收端准备；完成德语业务素材及整包确认；取得受控单渠道排期详情，补远端图片数量/顺序读取适配器并核验；分别关闭 FB/IG 排期和运营从飞书到回执的实际链路。详细子项、所需证据和离线结果不在本计划重复维护。

## 目标与架构

接续原工作区的归档与发布账本，完成监测、留档、本地化、飞书审校、FB/IG 独立排期。Python 文件账本仍为真相，SQLite 仅供查询；FastAPI + Vue 复用生产业务代码。以 FUNCTIONALITY 的最新决定为准，旧文档相反描述需要同步纠正。

## 全局约束

- 在 codex/business-workflow 现有 worktree 开发；保留六处未提交改动，不覆盖原讨论分支。真实运行接续同一 archive/state，先备份与核验，旧发布记录和激活边界保留。
- FB / IG 各自独立；.global 合作帖全部抓取；.tech 冻结，只读历史。
- 不自动登录，不自动滚历史回填，不绕过 C7 抓取预算，不伪造浏览器证据，不自动提高轮询频率。
- 上海 08:00–19:00 在岗，柏林选期，同渠道 90 分钟间隔；人工选时冲突拒绝并给建议。
- 人工稿/图不被机器覆盖；付费需统一预算及来源许可，重放不确定请求之前必须核账。每图最多受理三次优化，挂起默认三个工作日，actor 为 null。
- 云盘单向镜像；只用冻结内容补送旧版本；scheduled 表示排期确认，不等于已实际公开发布。
- 凭据只在本机配置，不进日志和版本库。真实发布前先准备具体文案、图片、账号、时间供用户确认；先做所有不依赖确认的工作。
- 本轮不做生产迁移、登录权限、视频、跨平台复用、无人审核发布。只修改文件与运行离线测试不再询问。

## Task 1: 文档同步与实施清单

同步 README、REQUIREMENTS、FUNCTIONALITY、CONTEXT、HANDOFF、MANUAL_STEPS、Web 契约与说明，纠正双渠道合并/每日一次/两个浏览器/假写入/actor/备份的旧口径。OPTIMIAZATION 按五阶段记录功能编号、代码缺口、外部依赖、验收证据；状态区分代码未完成、离线通过、待真实联调、真实通过、明确延期。保留历史事实和日期，不把测试等同真实验收。

## Task 2: 运行数据与中断恢复

备份原目录 archive/state/config，核验账本和历史防重后用本地路径配置接续，所有入口共享数据和锁。审校账本坏行必须失败闭合。模型任务关联付费请求并辨识正在执行/进程中断/未发请求/结果不确定；补恢复入口，恢复不隐含重试。统一 preflight/激活/发布能力判据。补批准快照及状态回填的恢复。已有六处修改须纳入验证。

## Task 3: 监测与调度

.global 初次回填和覆盖核对。上个完整月计算发帖窗、样本数、覆盖率；数据不足保持既有计划。截止预算覆盖两个平台及整批处理，按预算前移兜底。模型处理移出调度主循环。记录扫描和处理事实；新常驻 Windows 任务可安装/查询/停用/恢复，检测旧任务重复运行。补抓和分类跳过计数接入日报，无事不推。

## Task 4: 历史归档与镜像

历史列表支持服务端分页、平台、月份、tag，详情不受 90 天限制，查询不触发翻译。SQLite 与文件做真实一致性检查。Web/CLI/人工结转的批准和排期快照统一留档，失败后从冻结内容补送。源图片内容、数量、顺序变化可识别，已排期仅提醒。云盘权限及上传/移动/版本/恢复真实验证；账本周期备份，大型证据按版本保存。

## Task 5: 本地化增强

真实英文语义风险预扫：pun/ambiguous/us_only，源文+提示词绑定，未扫描/失败/成功零风险明确区别。复用付费许可预算，在翻译前扫描，失败不自动声称安全。Trends 德国公开 CSV 采样，在同一标签候选组内比较；IG 累计量级和同类德语账号周更采集，空名单跳过、失败/过期降级。运营设置白名单开放默认时刻/挂起天数，受控参数只读且保留说明。任务恢复前端、旧候选失效、费用统计。

## Task 6: 飞书与运行可见性

待审卡片取当前德语首图、有效人工/机器摘要和检查结果；原图回退明示。按帖汇总，不重复计数；终结或挂起项退出待发。晨间摘要含补抓/分类跳过/延迟，无事不发。发送结果不确定、凭据、图片失败状态可见，重试 UUID/内容固定。真实应用权限、两个接收组、可访问审校链接和外部心跳缺席告警联调。

## Task 7: 单渠道发布与月历

先记录 FB/IG 渠道、账号、控件和回读证据再实现渠道切换，不简单移除阻塞函数。月历完整覆盖可见月份/手工任务/延迟加载，不能用未读完的视图判空档。提交前再次读远端，柏林任意时间及 90 分钟间隔。批准中断/回执不确定核验后幂等补回状态、快照、镜像、通知。长正文、多图、FB 链接、IG CTA 真机核验。实际公开状态只读观测，未知明示。

## Task 8: 接口与整体验收

兼容现有任务接口，增加历史范围/分页/总数，模型任务恢复、快照与源指纹、风险/采样元信息、设置白名单、五阶段只读状态及 preflight --json。版本化离线浏览器回归入口。新增真实行为测试覆盖中断、损坏、源图变化、历史账本、时区、冲突、补送、消息未知、采样降级与分页。每批相关测试，最终完整 Python + build + 浏览器回归。准备两篇实际联调内容供确认后，各完成一个单渠道排期及回读；运营从飞书到审核排期的流程走完并回填证据。

## 核查基线

计划阶段：47 个 Python 测试脚本通过、Vue 构建通过；六处既有改动的十组相关回归通过。缺口另行复现：执行器启动失败留下 pending；坏审校记录回退 pending_review。原目录 FB 47 篇、旧 IG .tech 1020 篇；开发 worktree 未关联真实数据。上述数字仅为 2026-09-12 快照，不作为长期健康判据。
