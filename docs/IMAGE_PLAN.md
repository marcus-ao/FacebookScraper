# K 组实施计划 · 图内英文德语化（GPT-Image-2）

> 对应 `IMPLEMENTATION_PLAN.md` 的 K 组。本文件是 K 组的任务书，
> 写法与职责对齐 `TRANSLATION_PLAN.md`（F 组任务书）。
>
> **建立日期：2026-08-31。状态：计划已定，代码未写。**
>
> **开发分支：`feat/image-de`**（详见第 10 节的分支与文件所有权约定）。
> 本组与 G 组（`feat/business-suite-publish`）**并行开发**，
> 第 10 节列了两条分支各自能碰哪些文件——**动别人的文件之前先看那张表**。

---

## 0. 这是一次范围变更，不是新增任务

`IMPLEMENTATION_PLAN.md` 第 0 节「本期不做」原文写着：

> - 图内英文文字的德语替换（**一期走人工处理**）

归档布局里那个 `posts/<帖子>/media_de/` 目录就是按这条决定预留的——
语义是「**设计同事**把换好德文的图放这里」。

**2026-08-31 用户决定改为程序自动完成**，用第三方网关上的 `gpt-image-2`。
本文件取代上面那条「本期不做」。

`media_de/` 的语义随之扩展为：**程序产出，人工可覆盖**。
⚠️ **人工放进去的文件优先级高于程序产出，程序不得覆盖它**（见 K6）。
这条不是礼貌，是必要的：设计同事手工修的那张，一定比模型那张对。

---

## 1. 用户拍板的目标与验收标准（原话转述，不要重新讨论）

**要做的**：把图片里的英文文字换成对应的德语，
**构图、配色、产品、排版位置尽量不动**。

**验收标准**：德语文字正确 **且** 产品外观未被改动。

**额外约束（用户原话）**：品牌相关文字、Logo、费用符号、单位类数值等
**不能随意翻译为德语，必须与原图中的样式保持一致**；
具体分类由实施方按需求与常识细化——**第 3 节就是那份细化清单**。

**范围（由发布侧的决定倒推）**：用户已明确**不补发历史存量**，
只用最新几篇做端到端验证，上线后跟着每日增量走。
所以 K 组的默认工作集是**增量新帖**，不是 818 张全量。
全量是一个显式开关，不是默认行为（见第 7 节的钱）。

---

## 2. 已核实的接口契约（**不要重新推导，也不要照 SDK 签名猜**）

核对日期 2026-08-31。来源：用户指定的
<https://docs.aihubmix.com/en/api/GPT-Image-1>、
<https://docs.apiyi.com/en/api-capabilities/gpt-image-2/overview>，
以及对 `https://api.inferera.com/v1/models` 的实际探测。

| 项 | 值 |
|---|---|
| base_url | `https://api.inferera.com/v1`（**实测 `GET /v1/models` 返回 200 且为 OpenAI 形态**） |
| 模型 id | `gpt-image-2`（实测在该网关的模型列表里；另有 `gpt-image-2-free`） |
| 端点 | `POST {base_url}/images/edits` |
| Content-Type | **`multipart/form-data`**（`/images/generations` 才是 JSON） |
| 鉴权 | `Authorization: Bearer <key>`（openai SDK 自动设置） |
| 必填 | `model`、`prompt`、`image` |
| 可选 | `mask`、`n`、`size`、`quality`、`output_format`、`output_compression`、`background`、`moderation` |
| 响应 | `data[0].b64_json`（**裸 base64，无 `data:image/...;base64,` 前缀**）+ `usage` |
| usage 形态 | `input_tokens` / `input_tokens_details{image_tokens,text_tokens}` / `output_tokens` / `output_tokens_details{image_tokens}` |
| 费率 | 文本输入 $5/1M · 图像输入 $8/1M · 图像输出 $30/1M |

### ⛔ 三条会直接让请求失败的事实

**1. `input_fidelity` 必须不发送。**
gpt-image-2 **强制**对参考图做高保真处理，**不再接受这个参数，传了返回 400**。

> ⚠️ **本机装的 `openai` 3.6.0 的 `images.edit()` 签名里有 `input_fidelity`，
> 且 docstring 写着「supported for `gpt-image-1` and `gpt-image-1.5` and later models」。
> 那句话对 gpt-image-2 是错的。**
> 记下来是因为：下一个会话去看 SDK 签名，一定会觉得"保真度这么关键怎么能不传"，
> 然后把它加回去。**它加回去的那一刻，整组请求全部 400。**
> 我们要的高保真是**默认就有的**，不是靠这个参数换来的。

**2. `quality` 必须显式给 `low` / `medium` / `high`，不要用 `auto`。**
省略会退到 `auto`，计费不可预测。这个项目已经因为"参数没生效又不报错"
栽过一次（F 组那次 thinking 没关掉，4096 输出额度全烧在思考上，一个字译文没产出）。

**用户 2026-08-31 拍板 `high`。** 我原本建议 `medium`（便宜 4 倍）；
用户选了 high。**这是业务取舍，代码不替用户改**——
与 `[translate].reasoning_effort` 那条同源。费用见第 7 节。

**3. `size` 必须满足四个约束，而我们的归档大面积不满足。**

约束：边长 ≤ 3840、**宽高都必须是 16 的整数倍**、宽高比 ≤ 3:1、
总像素在 **655,360 – 8,294,400** 之间。

对 818 个已归档图片文件实测的尺寸分布：

| 张数 | 尺寸 | 16 的倍数？ | 处理 |
|---:|---|---|---|
| 318 | 1080×1080 | ❌ 1080/16 = 67.5 | → **1088×1088** |
| 100 | 1080×1350 | ❌ | → **1088×1360**（正好 4:5，无形变） |
| 65 | 1440×1440 | ✅ | 原样 |
| 33 | 1440×1920 | ✅ | 原样 |
| 27 | 1350×1687 | ❌ | → 1344×1680 |
| 21 | 1440×1080 | ❌ | → 1440×1088 |
| 15 | 960×1200 | ✅ | 原样 |
| 12 | 720×720 | ✅ 但 **518,400 < 655,360** | → **816×816**（放大到过下限） |

**最常见的两档（418 张，超过一半）都不合法。**
直接 `size=f"{w}x{h}"` 会在一半以上的图上 400 —— 这正是"看起来最自然的写法"。

因此必须有一个 `legal_size(w, h)` 函数（K2），并且**它要有自己的单元测试**：
四条约束里任意一条写错，症状都是"跑起来一半图片 400"，而不是编译期错误。

---

## 3. 「哪些字要译、哪些字碰都不许碰」的细化清单

用户要求由我细化。以下分类**基于真实语料统计**，不是凭空想的：
对 1057 条有正文的帖子做了品牌/型号/大写词频统计，高频项直接进清单。

### 3.1 ⛔ 必须逐字符原样保留（译错 = 商业事故，不是文案问题）

| 类别 | 语料实测项 | 为什么 |
|---|---|---|
| **优惠码 / 折扣码** | `P1MMCG`(62) `NEAKACG`(49) `SOCIAL05`(11) `Q11`(11) | **最危险的一类，且最不显眼。** 它们是任意字符串却长得像单词，模型最容易顺手"翻译"。码错 = 用户结账失败 |
| 品牌名与商标 | `Neakasa`(899) `NEAKASA`(16) `Neabot`（旧品牌名，语料里仍在） | 商标不翻译 |
| **合作方水印 / 署名** | 263 篇合作帖中 **229 篇原作者是第三方创作者** | **改掉别人的水印比翻错严重得多，那是署名权。** 见 `HANDOFF.md` 的合作帖一节 |
| 产品型号 | `M1`(222) `P2`(55) `P1`(53) `S1`(30) `F1`(27) `N3`(16) `Magic1`(17)，及连写形态 `NeakasaM1`(123) `NeakasaF1`(12) `NeakasaS1Pro`(8) `NeakasaMagic1`(49) | 型号是标识符 |
| 货币金额 | `$49.99` 逐字符 | **与 `prompts/translate_de.md` 第 3 节同源同理由**：德国站定价是商务决策，模型无从知道。译文里出现 `€` 一律是错的 |
| 数值 + 单位 | 尺寸 / 容量 / 功率 / 分贝 / 时长 / 百分比 | 见下方 ⚠️ |
| 认证与合规标识 | CE / FCC / RoHS / UL | 法规标记 |
| 展会与活动名 | `CES`(32) `IFA`(19) `CES2025` `IFA2025` `CES2026` | 专名 |
| 法律标记 | ® ™ © `MSRP`(19) | 专名 |
| @提及 / #标签 / URL / 二维码 / 条码 | — | 与 translate 第 7 节一致 |
| 人名 / 账号名 / 地名 | — | 专名 |

⚠️ **图内的单位比正文更严：一律不换算。**
`prompts/translate_de.md` 允许正文换算物理量（inch→cm、lb→kg），
**图片这边不允许**。理由是可验证性不同：

> 正文换算错了，`review.md` 里两栏一对就看出来；
> 图片是像素，错了只能靠人眼逐张看，而**没有人会逐张看 818 张图**。
> 在无法验证的介质上做不可逆的数值变换，是拿不确定性换好看。

**不确定时的默认动作：不动。** 与 translate 的"原文没有的数字绝对不要造"同源。

### 3.2 ✅ 应当译成德语

描述性文案、卖点短句、CTA 按钮文字、功能标注、章节标题、通用名词
（`Self-Cleaning` → `selbstreinigend`、`Free Shipping` → `kostenloser Versand`）。

**必须复用 `config.toml` 的 `[translate.glossary]`，不得另写一份。**
否则图里写 `Katzentoilette`、正文写别的，同一篇帖子里出现两种说法——
术语表存在的全部意义就是防这个。

### 3.3 德语文字从哪来

两个来源，缺一不可：

1. **该帖已有的德语译文 `text_de`**（用户明确要求用它做参照）。
   它已经过金额硬校验、标签硬校验，是当前项目里质量最高的德语来源。
2. **术语表**（同 3.2）。

⚠️ 因此 **K 组依赖 F 组**：没有 `translated.jsonl` 里当前版本的译文，
这篇的图就不该处理。硬依赖，不做降级。

---

## 4. 架构：照抄 `translate.py` 已被验证的那一套

**不发明新结构。** F 组那套（真相源 jsonl + 指纹 + 版本 + 单实例锁 + 断点续跑
+ 写盘前硬校验）已经被真实付费批次验证过，K 组逐条复用。

```
localize_images.py            ← 新增，与 translate.py 平级
prompts/image_de.md           ← 新增，提示词本体（改它不用动 Python）
config.toml  [image]          ← 新增段
config.toml  [image.keep_verbatim]  ← 第 3.1 节那份清单的配置化
.env         IMAGE_API_KEY    ← 新增密钥（沿用「密钥永不进 config.toml」）

archive/<账号>/
  images_de.jsonl             ← **德语图的真相源**（与 translated.jsonl 同级同语义）
  posts/<帖子目录>/
    01.jpg                    ← 原图，只读，永不改动
    media_de/
      01.jpg                  ← 产出。**人工放的同名文件不得被覆盖**
```

**每行 `images_de.jsonl` 的字段**（与 `translated.jsonl` 同构）：

```json
{"post_id": "...", "media_index": 0,
 "source_sha256": "原图字节的 SHA-256",
 "text_de_sha256": "参照译文的指纹",
 "prompt_version": 1,
 "model": "gpt-image-2",
 "size_requested": "1088x1088", "size_returned": "1088x1088",
 "quality": "medium",
 "out_path": "posts/.../media_de/01.jpg",
 "dhash_distance": 7,
 "created_at": "2026-...Z",
 "usage": {...}}
```

**"这张图已经做过了"的判据**（三者全等才算完成，任一变化即过期）：
`post_id + media_index` + **原图字节指纹** + **参照译文指纹** + `IMAGE_PROMPT_VERSION`。

把原图指纹算进去，是因为归档支持"残缺→补全"升级（`Archive.should_append`），
同一个 `post_id` 的 `01.jpg` 是可能被换掉的。只认 post_id 会让旧德语图
配到新原图上——和 F 组"旧德文配新英文"是同一个 bug，那边已经踩过。

---

## 5. 任务分解

### K0 · 依赖与配置骨架　✅ **已在主干预置，本组不必再动这三个文件**

以下三项已于 2026-08-31 提前落进 `docs/plan-image-publish`：

- ✅ **Pillow 已写进 `requirements.txt` 并装进 `.venv`**（实测 12.3.0）。
  > ⚠️ 它违反了全局约定「不引入新依赖」，理由记在 `requirements.txt` 的注释里：
  > 产出图必须验证「能解码 / 尺寸对 / 不是纯色 / 与原图结构相近」，
  > 没有图像库做不了。**只有 Pillow，没有 numpy**——
  > 结构相似度用纯 Python 的 dHash 汉明距离即可（见 K5）。
  >
  > **它被提前落地，是为了让 `feat/image-de` 与 `feat/business-suite-publish`
  > 不必同时改 `requirements.txt`**（G 组的 compose.py 也要读图）。见第 10 节。
- ✅ `config.toml` 的 `[image]` 与 `[image.keep_verbatim]` 已写好。
- ✅ `.env.example` 已加 `IMAGE_API_KEY`；**用户已把真实密钥填进 `.env`**（已核对可读）。

本项剩下的只有一条：

- 【验收】**配置项审计**：`[image]` 段里每一个键都有代码读它，
  且代码读的每个键配置里都有。这是 CR-40 那轮立的规矩：
  既不许有"配置里有、代码不读"的死旋钮，也不许有"代码读、配置没写"的静默默认值。

### K1 · `Settings` 与 `--check` 自检

- 照 `translate.py::Settings` 写：provider / base_url / api_key_env / model /
  quality / output_format / timeout / max_retries / request_gap_seconds。
- `validate()` 在任何网络请求前给出**可操作**的错误，而不是等网关返回模糊 400。
- `--check` 发**一个最小的真实 edits 请求**（一张本地生成的小图 + 一句无害提示词），
  并逐条断言：
  1. 网关可达、鉴权通过；
  2. **响应 `model` 确实是 `gpt-image-2`**，没有被静默降级成 `gpt-image-2-free` 或别的；
  3. 响应里有 `data[0].b64_json` 且能 base64 解码成合法图片；
  4. `usage` 四个字段都在（后面的预算全靠它）。
- ⚠️ **`--check` 会真的花钱**（一次 low 质量的小图）。必须在输出里说清楚，
  与 F 组 `--check` 的口径一致。
- 【验收】`--check` 在真实 Key 下通过并打印实际 model 与 usage；
  缺 Key 时给出复制 `.env.example` 的完整命令且**不联网**。

### K2 · `legal_size()` —— 尺寸合法化

- 输入原图宽高，输出**满足第 2 节四条约束**、且尽量贴近原图宽高比的尺寸。
- 规则：各边四舍五入到 16 的倍数 → 边长夹到 ≤3840 → 若总像素 < 655,360
  则按比例整体放大直到过线（放大后仍须是 16 的倍数）→ 若总像素 > 8,294,400 则缩小
  → 最后校验宽高比 ≤ 3:1，越界则显式失败（**不裁剪、不填充**，那是改画面）。
- 记录 `size_requested` 与原图尺寸的**形变百分比**；超过阈值的进人工清单。
- 【验收】用第 2 节那张实测分布表逐行断言（1080×1080→1088×1088、
  1080×1350→1088×1360、**720×720→816×816**、1440×1440 原样）；
  再加随机模糊测试：任意 (w,h) 的输出必然满足四条约束，或显式抛错。

### ~~K3 · 预扫描：这张图里有没有英文，有哪些~~　⛔ **已取消（用户 2026-08-31 决定）**

**用户的决定**：不加预扫描这一步，直接用 gpt-image-2 自带的视觉能力。

**设计保留在下面（不删除），是因为它的两个后果需要有人记得**，
而不是为了劝你改回去。这是用户的决定，**不要重新讨论**。

> 原设计：用同一网关上的便宜视觉模型先读一遍图，产出 `images_scan.jsonl`，
> 记 `has_text` / `text_items[]`（原文串 + 大致位置 + 属于第 3.1 节哪一类）。

**取消带来的两个后果，写下来免得以后当成 bug 去查**：

1. **没有"这张图有没有文字"的闸。** 所有配图一律送进 edits，
   无文字的产品图也会被付费处理一次。
   当前范围（不补发历史、只跟增量）下总额很小，可接受——见第 7 节。
2. **"德语文字正确"这条验收完全落到人工。** 机器不再知道原图上有哪些英文，
   也就**无法自动回答"有没有漏改"**。
   **K8 的审校清单因此是唯一的验收关口**，不是"之一"。

**因此 K8 从"锦上添花"升级为"K 组能不能验收的唯一依据"**，
它的优先级要跟着提上来。

### K4 · 提示词 `prompts/image_de.md`

- 与 `prompts/translate_de.md` 同构：中文写指令（营销同事能改）、
  `{{占位符}}` 由 `config.toml` 注入、`IMAGE_PROMPT_VERSION` 改一次加一。
- 占位符：`{{TEXT_DE}}`（该帖德语译文）、`{{GLOSSARY}}`（复用翻译术语表）、
  `{{KEEP_VERBATIM}}`（第 3.1 节清单，由 `[image.keep_verbatim]` 渲染）。
  ⚠️ **没有 `{{SCAN_ITEMS}}`**——K3 已取消，模型自己找图上有哪些英文。
  这意味着**提示词要自己承担"找哪些字要改"的职责**，
  第 3.1 节那份清单在提示词里的分量因此更重，要写得能被模型当规则用。
- ⚠️ **外部正文按不可信数据封装**——与 translate 一致。
  `text_de` 来自社媒语料，不能让它闭合标签或改写指令。
- 【验收】`--show-prompt` 能对任意一篇真实帖渲染出完整提示词，零 API 调用。

### K5 · 产出校验（写盘前的硬闸）

任一条不过就**不写盘**、计为失败、保留断点——与 F 组金额/标签硬校验同一套语义。

1. base64 能解码、字节数 > 0；
2. Pillow 能打开，**尺寸等于 `size_requested`**；
3. 不是纯色 / 不是接近全黑全白（抓"模型返回了个占位图"）；
4. **结构相似度闸**：原图与产出图的 dHash 汉明距离 ≤ 阈值。
   这一条抓的是本组**最严重也最可能发生**的失败模式——
   模型不是"改了几个字"，而是**整张重画了**。用户的验收标准
   「产品外观未被改动」在机器侧就落在这一条上。
   > ⚠️ **阈值现在不定。** 这个项目已经立过规矩：阈值要在真实数据上标定，
   > 不许拍脑袋（`[integrity]` 那几个值改过两次，见 `config.toml` 注释）。
   > 做法：第一批 20 张跑完后，把 20 对 (原图, 产出图) 的距离分布打出来，
   > 人眼判定哪些是"只改了字"、哪些是"重画了"，再定这个数并写进完成行。
   > 在标定完成前，阈值配 `-1`（关闭），只记录不拦截。

### K6 · 落盘与幂等

- 产出写 `posts/<帖子>/media_de/<两位序号><扩展名>`，与原图同序号。
- ⛔ **目标文件已存在且不是本程序产出的（`images_de.jsonl` 里没有对应记录），
  一律跳过并告警。** 那是设计同事手工修的版本，比模型那张对。
- 追加 `images_de.jsonl` 用 `translate.py::append_jsonl` 同款写法
  （补齐坏尾换行、`flush + fsync`、读取时逐行独立解码）。
- 单实例锁 `state/images.lock`，防重复双击重复付费。
- 【验收】连跑两次，第二次全部跳过、零 API 调用、零写盘。

### K7 · `--estimate` 与费用汇总

- ⚠️ **必须按真实 usage 外推，不许按图片数×猜的单价。**
  这是 CR-40 血的教训：F 组的离线估算按字符换算给出 US$1.08–5.41，
  真实账单是 **US$23.73**，**低估一个数量级**，因为漏掉了主导项。
- 已跑过的图的 usage 就在 `images_de.jsonl` 里，取**中位数**外推，
  且只采当前 `IMAGE_PROMPT_VERSION` 的样本。
- 一张图都没跑过时，明确说"还没有真实样本，这个数只是量级参考"，
  不要冒充上界。
- 【验收】跑完第一批后 `--estimate` 的外推值与实际账单同量级。

### K8 · 接入审校清单　⬆️ **K3 取消后，它的优先级提高了**

- `translate.py --review` 生成的 `review.md` 增加一节：**原图与德语图并排**，
  外加逐条人工勾选框（原文英文项由审校人自己看图列出，
  **不再有 K3 扫描结果可用**）。
- 勾选项要覆盖第 3.1 节每一类：优惠码 / 品牌 / 型号 / 合作方水印 /
  金额 / 数值单位 / 认证标识 —— **一类一个框**，
  比一个笼统的"检查过了"有用得多。
- ⚠️ **K3 取消之后，这里是唯一能执行「德语文字正确」这条验收的地方。**
  机器只能拦灾难（K5），读不出德语对不对。
- 【验收】对至少 3 篇真实帖生成并排预览，Markdown 里图片能显示。

### K9 · 端到端真实验收

- 对**最新 3 篇**帖子的全部配图跑完整流程。
- 【验收】懂德语的人确认：文字正确、第 3.1 节那些项一个都没被动过、
  产品外观没变。**这条不通过不许勾选 K 组**（与 F 组同一条规矩）。
- 【记录】实际单张耗时、单张费用、dhash 距离分布、失败率。

---

## 6. 依赖与顺序

```
F（德语译文）──┐
               ├─→ K4 提示词 ─→ K5 校验 ─→ K6 落盘 ─→ K9 真实验收
K0 骨架 → K1 ─┤          （K3 已取消）              ↑
K2 尺寸 ──────┘                          K7 预算 · **K8 审校（K9 的唯一依据）**
                                                    ↓
                                              G 组（发布）
```

**K 组硬依赖 F 组**：没有当前版本的德语译文就不处理这篇的图（第 3.3 节）。
**G 组依赖 K 组**：发布优先用 `media_de/`，没有则回退原图并**显式告警**。

---

## 7. 钱

单张 1024² 参考图：输入约 $0.008；输出 `medium` 约 $0.053、`high` 约 $0.211。

**用户已拍板 `quality = "high"`**（我建议过 medium，用户选了 high；
业务取舍，代码不替用户改）。**加粗那一列是实际会发生的支出**：

| 场景 | 张数 | medium（未采用） | **high（已采用）** |
|---|---:|---:|---:|
| **最新 3 篇做验收**（本期路径） | ~10 | ≈ $0.6 | **≈ $2.2** |
| 上线后每日增量（每帖 1–5 图） | ~5/天 | ≈ $0.3/天 | **≈ $1.1/天 ≈ $33/月** |
| 全量历史（**用户已决定不做**） | 818 | ≈ $49 | ≈ $179 |

⚠️ **K3 取消后没有"无文字图"的闸**，所以上表的"张数"是**全部配图**，
不是"有英文的配图"。在本期范围下这个差别只值几美元，可接受；
**但如果哪天决定补发历史，它会把 $179 变成实打实的全额支出**——
那时应当先把 K3 加回来。

⚠️ 上面的费率是 OpenAI 上游价，**中转网关通常有加价**。
真实账单以 inferera 控制台为准；`--estimate` 只用来算量级和"要不要跑"。

---

## 8. 待拍板项　✅ **两项都已在 2026-08-31 结清**

| 问 | 用户的决定 | 落到哪 |
|---|---|---|
| K3 预扫描要不要加 | **不加**，直接用 gpt-image-2 自带的视觉能力 | K3 标为已取消（保留设计与后果）；K8 优先级提高 |
| `quality` 用哪档 | **`high`**（我建议过 medium） | `config.toml` 已改；费用见第 7 节 |

**外部前置也已就绪**：`IMAGE_API_KEY` 用户已填进 `.env`（已核对可读，长度 51）。

**本组现在没有任何外部卡点，可以直接开工。**

---

## 9. 五条不许违反的

1. ❌ **不得发送 `input_fidelity`**（第 2 节，会 400）。
2. ❌ **不得用原图宽高直接当 `size`**（第 2 节，一半以上会 400）。
3. ❌ **不得覆盖 `media_de/` 里人工放置的文件**（K6）。
4. ❌ **不得修改原图**。`01.jpg` 是抓取产物，只读。
5. ❌ **不得把密钥写进 `config.toml`**（全局红线 8）。

---

## 10. 分支与文件所有权（K 组与 G 组**并行开发**的约定）

> 这一节在 `IMAGE_PLAN.md` 与 `PUBLISH_PLAN.md` 里**内容相同**，
> 两边都留一份是有意的：并行开发时没人会去读另一组的任务书。
> **改这一节要两边一起改。**

### 10.1 两条分支

| 组 | 分支名 | 从哪里开 | 何时合回 `main` |
|---|---|---|---|
| **K 组**（图片德语化） | **`feat/image-de`** | `docs/plan-image-publish` | **K9 真实验收通过**（懂德语的人确认过图） |
| **G 组**（Business Suite 发布） | **`feat/business-suite-publish`** | `docs/plan-image-publish` | **G8 验收通过** |

```bash
git checkout -b feat/image-de docs/plan-image-publish
```

> `docs/plan-image-publish` 是两份计划、`config.toml` 的 `[image]`/`[publish]` 段、
> `requirements.txt` 的 Pillow 所在的分支。**必须从它开**，
> 从 `main` 开会拿不到计划和配置。
> （若用户已把它快进合进 `main`，从 `main` 开等价。）

**合回 `main` 用 `--ff-only`，保持历史线性**——这是项目既定约定，
合不上说明 `main` 在你之外动过，**先查清楚再动手，不要 `-f`**。

### 10.2 文件所有权：**动别人的文件之前先看这张表**

| 文件 / 目录 | `feat/image-de` | `feat/business-suite-publish` |
|---|---|---|
| `localize_images.py` | ✅ 独占 | ⛔ |
| `prompts/image_de.md` | ✅ 独占 | ⛔ |
| `scripts/run_images.bat` | ✅ 独占 | ⛔ |
| `tests/tests_localize_images.py` | ✅ 独占 | ⛔ |
| `translate.py`（仅 `run_review` / `sync_text_de`，为 K8） | ✅ 独占 | ⛔ |
| `tests/tests_translate.py`（仅 K8 相关断言） | ✅ 独占 | ⛔ |
| `publish/**` | ⛔ | ✅ 独占 |
| `tools/probe_publish.py`、`tools/start_chrome_publish.py` | ⛔ | ✅ 独占 |
| `scripts/start_chrome_publish.bat`、`scripts/run_publish.bat` | ⛔ | ✅ 独占 |
| `tests/tests_publish.py` | ⛔ | ✅ 独占 |
| **`core/chrome.py`**（G0 的 port/profile 参数化） | ⛔ | ✅ 独占 |
| `core/config.py`（如需 `[publish]` 派生路径） | ⛔ | ✅ 独占 |
| `tests/tests_chrome.py`（G0 扩断言） | ⛔ | ✅ 独占 |
| `config.toml` 的 `[image]` / `[image.keep_verbatim]` 段 | ✅ | ⛔ |
| `config.toml` 的 `[publish]` 段 | ⛔ | ✅ |
| `requirements.txt` | ⛔ **Pillow 已预置，不用动** | ⛔ **同左** |
| `docs/IMAGE_PLAN.md` | ✅ | ⛔ |
| `docs/PUBLISH_PLAN.md` | ⛔ | ✅ |
| `docs/IMPLEMENTATION_PLAN.md` | ⚠️ **只改 K 组那一节**，附录 D 追加自己的一段 | ⚠️ **只改 G 组那一节**，附录 D 追加自己的一段 |
| `docs/MANUAL_STEPS.md` | ⚠️ 只改与自己相关的步骤 | ⚠️ 只改与自己相关的步骤 |
| `docs/HANDOFF.md` | ⚠️ **只改「现在卡在哪」里自己那一行** | ⚠️ 同左 |
| `core/store.py`、`core/parse.py`、`core/capture.py`、`routes/**` | ⛔ **都不许动** | ⛔ **都不许动** |

### 10.3 三条降低冲突的规矩

1. **只在自己那一节里编辑共享文档。** 不要重排其它节、不要"顺手"改格式、
   不要动行尾空白。`config.toml` 的 `[image]` 与 `[publish]` 相邻，
   `IMPLEMENTATION_PLAN.md` 的 K 组与 G 组也相邻——
   **各自只碰自己那一段，git 就能自动合**。
2. **附录 D 各自追加一节，标题里带上分支名。** 两段都要保留，
   合并冲突时**不许二选一**——那是项目的历史记录。
3. **先完成的先合。** 第二个合之前先
   `git fetch && git rebase main`（或 `docs/plan-image-publish`），
   **在自己分支上解冲突**，别把冲突带进 `main`。

### 10.4 收尾时两条分支都必须做的

- 跑**全部** `tests/tests_*.py`，不能只跑自己新增的那套（项目既定要求）。
- `git diff --check` 与 `python -m compileall -q .` 干净。
- `.bat` 保持**纯 ASCII + CRLF + 无 BOM**（`tests_schedule.py` 有字节级断言）。
- 新入口调 `core.console.force_utf8()`——本机代码页 936，
  输出一旦被重定向就会崩在 `ß`/`⚠` 上。
- 按项目工作协议更新 `IMPLEMENTATION_PLAN.md`：勾选完成项、
  在下面追加 `> 完成：<日期> · <实际做法与偏差>`。
  **未通过【验收】的不得勾选。**
