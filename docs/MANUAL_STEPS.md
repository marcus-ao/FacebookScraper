# 人工操作指南

本文件**只讲需要你亲自动手的步骤**，自动化部分不在这里。
进度真相源仍是 [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md)，本文件是它的操作手册。

按顺序做即可。每一步都写了：**怎么做 → 成功长什么样 → 不对时怎么办**。

下文的“项目根目录”指包含 `README.md`、`config.toml`、`core/` 的目录；当前目录名
是 `FacebookScraper`。除非另有说明，命令都从这个目录运行，避免依赖某台机器上的
固定绝对路径。

---

## 当前进度

| 步骤 | 内容 | 状态 |
|---|---|---|
| A1 / A2 | 环境搭建、离线测试基线 | ✅ 已完成（无需你操作） |
| C / D / E | 登录态增量、完整性检查、通知、计划任务 | ✅ 代码完成；计划任务尚未在本机安装 |
| C1 | 登出增量（旧方案） | 端点已对登出关闭，代码保留备用，不用管它 |
| F1 / F2 / F3 | DeepSeek 翻译流水线 | ✅ **新版官方接口已真实跑通**（`--check` + FB 3 + IG 3 全成功）。⏳ 等**懂德语的人**审校，并拍板全量要不要跑（实测约 US$24） |
| 第 1–3 步 | 起 Chrome、登录小号、填账号 | ✅ 已完成 |
| 第 4 步 | 人工滚动回填 | ✅ 已跑完 |
| — | 修解析器 + 重建归档 + 归档改版 | ✅ 已完成（2026-08-30，**没让你重滚**） |
| 第 5 步 | 抽查媒体 | ✅ 已代你验收（见下）；增量下载 2026-08-31 也验过了 |
| 第 6 步 | 放 DeepSeek Key | ✅ 已完成（`.env` 里已有，程序能读到） |
| **第 7 步** | **德语质量审校 + 拍板全量预算** | ⬅️ **轮到你**：6 篇新版译文已在 `review.md` 里；全量按真实 usage 外推约 **US$24** |
| 每日增量方案 | 登录态 + CDP（方案 B） | ✅ **你已定案**，代码已写完 |
| 合作帖 | 你发现的问题，已修 | ✅ 归档 756 → **1020 篇**；**2026-08-31 真实抓取验证通过**（当天新帖本身就是合作帖），见下 |
| ~~第 8 步~~ | 验证合作帖修复 + 增量复测 | ✅ **2026-08-31 你已全部跑完并通过**（详见第 8 步开头的结果） |
| **第 9 步** | **装每日计划任务** | ⬅️ **轮到你，而且这是最该先做的一件事**：装之前，整个项目没有任何东西是自动跑的 |
| ~~第 10 步~~ | 放图片 API 密钥 + 填 DE 发布账号 | ✅ **你已完成**（密钥已在 `.env`，两个账号已填进 `config.toml`） |
| **第 10b 步** | **跑图片德语化（K9 验收）** | ⬅️ **轮到你**：代码与译文都就绪，只读预演排出 **11 张**；只差你按 **≈US$2.4** 点一次头 |
| **第 10c 步** | **看一眼「要发出去的到底长什么样」** | 🟢 **随时可跑，零风险**（零浏览器/网络/写盘/费用）：`scripts\run_publish.bat --latest 3` |
| K 组 | 图内英文换德语（GPT-Image-2） | ✅ 代码完成、17 套测试全绿、真实预演通过。**只差第 10b 步的费用确认与德语人工审校** |
| G 组 | Business Suite 发布（FB DE + IG DE 同发） | ✅ G0/G0b 已完成并验收（最新 3 篇真实组装通过，可用 `scripts\run_publish.bat` 复跑）。**仍卡在你做一次 DOM 探查（第 11 步）** |
| L 组 | 把上面这些串成一条不用人管的流水线 | 📝 规划已定（`docs/PIPELINE_PLAN.md`）。**第一步就是第 9 步** |

---

## ✅ 你的数据现在是干净的（2026-08-30 两轮修复后）

抓下来的数字前后修过两次，**你一次都没有重滚过**（靠的是保存的原始响应）：

| 平台 | 最初 | 第一轮修完 | 现在 | 最后一轮差在哪 |
|---|---:|---:|---:|---|
| Instagram | 1500 | 756 篇 | **1020 篇** | 加回 263 篇合作帖；2026-08-31 增量又新抓 1 篇，**也是合作帖**（合作帖合计 264） |
| Facebook | 47 | 46 篇 | **47 篇** | FB 没有合作帖机制；2026-08-31 增量新抓 1 篇 |

第一轮减掉的是 478 条轮播里的单张图和真正的他人帖；第二轮加回来的是
**本来就在你们主页上、只是原作者不是本账号**的合作帖。

**图片一张都没少**，多出来的重复文件在 `_orphan_media/`，
**是移动不是删除**，你确认没问题之后可以自己删。

代我做完的抽查（对应第 5 步）：**没有 0 字节文件**；图片分辨率
**IG 主要是 1080×1080，FB 主要是 1080×1350**，都在 1080px 上下，
下游图像处理的质量没有受限；视频按约定只记录、没下载。

---

## 📁 归档改版了：一篇帖子一个文件夹

你提的方案采纳了。现在打开 `archive\in_neakasa.tech\posts\` 是这样：

```
posts\
  2026-07-15_1434_3941857863710867472\
    post.json          元信息
    text.txt           英文原文
    text_de.txt        德语译文（翻译之后才有）
    01.jpg 02.jpg 03.jpg 04.jpg 05.jpg
    media_de\          ← 设计同事把换好德文的图放这里
  2026-07-10_0912_...\
```

**文件夹名前面就是发布日期和时间**，按名称排序就是按时间排序，
一眼看得出哪篇是哪天发的、哪些图属于哪篇。

### 最省事的看法：双击 index.html

```
archive\fa_neakasaofficial\index.html      47 篇
archive\in_neakasa.tech\index.html         1020 篇（含 264 篇合作帖，带绿色标签）
```

一页看完整个账号的历史，按时间倒序，每篇有日期、正文、配图、原帖链接。
**这个文件是可以随时重新生成的**，不用担心弄坏：

```bash
.venv\Scripts\python.exe -m tools.layout index instagram
```

> ⚠️ 一条规则请记住：**`post.json` 是真相，`manifest.jsonl` 只是索引。**
> 如果你手工删了某个帖子文件夹，跑一下
> `.venv\Scripts\python.exe -m tools.layout reindex instagram`
> 索引就会跟上。反过来改索引是没用的。

---

## 第 0 步 · 确认一条通知（30 秒）

我在验收 D2 时往你桌面发过一条测试通知，标题「测试」，正文「这是一条测试通知」。

**请回想或确认你是否看到了。** 程序侧显示发送成功，但如果你开着
「专注助手 / 请勿打扰」，通知会被系统吞掉而程序侧无感——
那样的话每天的增量告警你也收不到，得改用别的方式提醒。

看不到就告诉我，我把告警改成更醒目的形式。
不确定的话，可以在项目根目录打开命令提示符，自己发一条试试：

```bash
.venv\Scripts\python.exe -m core.notify
```

---

## 第 1 步 · 起专用 Chrome（对应 A3）

### 怎么做

在项目根目录里**双击 `scripts\start_chrome.bat`**。

### 成功长什么样

黑窗口里依次出现：

```
使用 Chrome: C:\Program Files\Google\Chrome\Application\chrome.exe
专用 profile: C:\Users\marcus_ao\.fbscraper-chrome
调试端口:     9222

等待调试端口就绪....
[ok] 调试端口 9222 已就绪。
```

同时弹出一个**全新的、什么都没登录的 Chrome 窗口**。

### 几件要知道的事

- **你日常用的 Chrome 可以照常开着，不冲突。** 这个脚本用的是独立的
  profile 目录（`%USERPROFILE%\.fbscraper-chrome`），Chrome 的「同一实例」
  限制是按 profile 目录算的，两者互不干扰。
- **这个新窗口在整个抓取过程中要一直开着。** 关了就得重来。
- 端口号想改的话只改 `config.toml` 的 `[chrome].debug_port` 一处即可，
  脚本会跟着走（旧版本还要同步改 .bat 顶部，现在不用了）。

### 不对时怎么办

| 现象 | 原因 | 处理 |
|---|---|---|
| `[i] 端口 9222 已在监听` | 专用 Chrome 已经在跑了 | 正常，不用重复启动，直接下一步 |
| 等了 15 秒仍未监听 | 该 profile 目录被残留进程占着 | 任务管理器 → 详细信息 → 找 `chrome.exe`，结束掉**用这个 profile 的**那些，再重跑 |
| `[!] .venv not found` | 环境没建 | 先双击 `scripts\setup.bat` |
| 提示找不到 Chrome | 装在非标准路径 | 把 `chrome.exe` 完整路径填进 `config.toml` 的 `[chrome].exe` |

---

## 第 2 步 · 登录抓取小号（对应 A4）

### 怎么做

1. 在**第 1 步打开的那个 Chrome 窗口**里，访问 `https://www.facebook.com/`
2. 用**抓取专用小号**登录，走完二次验证
3. 同一窗口再访问 `https://www.instagram.com/`，同样登录小号
4. **验证会话持久化**：完全关掉这个 Chrome 窗口 → 重新双击 `scripts\start_chrome.bat`
   → 打开 facebook.com，确认**仍是登录状态**

### 成功长什么样

重开之后不需要再输密码，直接就是登录态。说明 profile 目录生效了。

### ⚠️ 三条必须遵守的

1. **绝对不要用主账号或持有 DE 资产的账号。** 抓取路径存在被判定异常的可能，
   被封的必须是损失可控的小号。
2. **不要在这个窗口里做日常浏览。** 它的浏览器指纹应该只和抓取行为绑定。
   ⚠️ **定案方案 B 之后这条更重要了**——这个窗口现在每天都要代表你
   去访问一次 Facebook 和 Instagram，混进日常浏览行为会让指纹变脏。
   另外**它得一直开着**，关了每日增量就跑不了。
3. **登录只做这一次，只在这里做。** 全项目只允许这一条登录路径——
   代码里没有任何自动登录，也不要加。多一条路径就多一份被 checkpoint 拦的机会。

### 不对时怎么办

- **重开后又要求登录** → profile 目录没写成功。确认
  `C:\Users\marcus_ao\.fbscraper-chrome` 存在且里面有文件；
  若目录是空的，检查该路径是否被安全软件拦了写入。
- **登录时被要求验证身份 / 出现 checkpoint** → 换个时间再试，不要连续重试。
  连续失败的登录尝试本身就是风险信号。

---

## 第 3 步 · 填写抓取目标（对应 A5）✅ 已完成

> **2026-08-29 已填好，你不用再动它**：
> `facebook = "neakasaofficial"`、`instagram = "neakasa.tech"`。
> 两个都是自定义用户名，没踩下面那个 `profile.php?id=` 的坑。
> 下面的内容留作参考，将来换账号时照着改。

### 怎么做

用 VS Code 打开 `config.toml`，找到 `[targets]`：

```toml
[targets]
facebook = "your-us-page"          # facebook.com/<这里>
instagram = "your_us_handle"       # instagram.com/<这里>
```

把引号里的值换成真实的。**只填 URL 里账号名那一段**，不要带
`https://`、不要带斜杠、不要带 `@`。

举例：

| 页面地址 | 该填什么 |
|---|---|
| `https://www.facebook.com/AcmeStoreUS` | `AcmeStoreUS` |
| `https://www.facebook.com/AcmeStoreUS/` | `AcmeStoreUS` |
| `https://www.instagram.com/acme.us/` | `acme.us` |
| `@acme_us`（IG 显示名） | `acme_us` |

### 验证

浏览器里打开这两个地址，确认都能打到正确页面：

```
https://www.facebook.com/<你填的 facebook 值>
https://www.instagram.com/<你填的 instagram 值>
```

### ⚠️ 一个可能的坑

如果这个 FB 主页**没有自定义用户名**，地址会长这样：

```
https://www.facebook.com/profile.php?id=100012345678901
```

这种情况**现在的代码拼不出正确 URL**（它只会拼 `facebook.com/<值>`）。
遇到了就告诉我，我改一下 URL 构造逻辑——别自己往 `[targets]` 里塞
`profile.php?id=...`，那样拼出来是坏地址。

---

## 第 4 步 · 人工滚动回填（对应 B1 / B3）

这是整个项目里**最耗你时间的一步**，两个平台各约 20 分钟。

### 为什么要你手滚

因为滚动的确实是人，没有任何可识别的自动化行为特征。
脚本全程不驱动页面，只在旁边把浏览器自己发出的接口响应捞下来。
这是拿 20 分钟换掉一整类封号风险，值得。

### 怎么做

**前置**：第 1 步的 Chrome 窗口开着，小号已登录。

1. 双击 **`scripts\run_backfill.bat`**，参数给 `facebook`
   （或在终端里 `scripts\run_backfill.bat facebook`）
2. 脚本会在那个 Chrome 里打开目标主页，终端出现：

   ```
   已打开 https://www.facebook.com/xxx
   ==============================================================
     现在请在那个 Chrome 窗口里手工向下滚动，直到看见最早的帖子。
     慢慢滚，让每屏内容都加载出来（看到图片显示出来再继续）。
     滚完之后回到这里按 Enter。
   ==============================================================
     已捕获 12 个响应 / 34 段 JSON
   ```

3. **切到 Chrome 窗口，开始往下滚。** 要点：
   - **慢滚。** 每屏停一下，等图片真的显示出来再继续。图片没加载 = 那批数据没请求 = 没捞到。
   - **用鼠标滚轮或方向键，不要按 End 键。** 一次跳到底会跳过中间的加载。
   - 时不时瞄一眼终端里的计数器，数字在涨说明在捞到东西。
   - 一直滚到**看见最早的一条帖子**（页面不再加载新内容）为止。
4. 滚完切回终端，**按 Enter**。
5. 终端会打印转储路径和解析结果。
6. Instagram 重复一遍：`scripts\run_backfill.bat instagram`。

### 成功长什么样

```
原始响应已转储 → D:\...\archive\fa_xxx\_capture_1756...json  (2048 KB)
解析出 37 篇帖子
  + 1234567890  3图/0视频  Summer sale starts now
  ...
新增 37 篇
```

### 请记录下来告诉我

- 捕获了多少个响应 / 多少段 JSON
- 解析出多少篇
- **你目测这个账号大概有多少篇帖子**（用来判断有没有漏抓）

### ⚠️ 解析出 0 篇怎么办

**不要重滚。** 这是预料之内的情况——解析器是按已知响应结构写的，
从没和真实响应比对过，B 组任务就是干这个的。

脚本在解析之前**已经无条件把原始响应转储**到
`archive/<账号>/_capture_<时间戳>.json`，你 20 分钟的成果一点没丢。
把这个文件告诉我，我按真实结构改解析器，然后离线重放，不用你再滚一次。

### 其它情况

| 现象 | 处理 |
|---|---|
| `调试端口 9222 没开` | 回第 1 步，Chrome 没起或被关了 |
| 滚到一半计数器不涨了 | 可能触发了限流。停下按 Enter 保住已有数据，隔几小时再补 |
| 超过 30 分钟自动收尾 | 正常保护机制。已捞到的会正常落盘；需要更久就调 `config.toml` 的 `[backfill].max_session_seconds` |
| 媒体下载报 403 | URL 签名过期。告诉我，需要调整下载时机 |

---

## 第 5 步 · 抽查媒体（对应 B5）✅ 已代你完成

重建归档时顺手把这一步的四项验收都跑了，结果在上面「你的数据现在是干净的」里。
**你不需要再做**。摘要：

| 检查 | Facebook | Instagram |
|---|---|---|
| 图片文件数与记录一致 | 70 == 70 ✅ | 648 == 648 ✅ |
| 0 字节文件 | 无 ✅ | 无 ✅ |
| 抽查 3 张：能打开、分辨率与记录一致 | ✅ | ✅ |
| 视频只记录、未下载 | ✅ | ✅ |

**典型分辨率**：IG 多为 1080×1080（部分 1440×1440），FB 多为 1080×1350。
都在 1080px 上下，**下游做图不会因为素材太小而受限**。

> 想自己看的话，图片现在在每篇帖子自己的文件夹里：
> `archive\in_neakasa.tech\posts\<日期>_<时分>_<id>\01.jpg`

---


## 第 6 步 · 配置 DeepSeek（F 组前置）

翻译代码、官方端点、最新模型、thinking 参数和术语表都已配置好。你只需要放密钥；
待译量是 **1055 篇**（Facebook 45 + Instagram 1010）。

### 6.1 放 API 密钥（不要发给我）

在项目根目录执行：

```powershell
Copy-Item .env.example .env
```

打开新建的 `.env`，只替换占位值：

```text
DEEPSEEK_API_KEY=你的真实密钥
```

`.env` 已在 `.gitignore` 中。不要把密钥写进 `config.toml`、提交到 Git，
也不要粘贴到聊天。默认业务配置无需再改：

- 端点：`https://api.deepseek.com`（官方 OpenAI 兼容 `/chat/completions`）
- 模型：`deepseek-v4-pro`
- 鉴权：`Authorization: Bearer`（OpenAI SDK 自动设置）
- thinking：显式开启，`reasoning_effort = "high"`
- 输出限制：不发送 `max_tokens` / `max_completion_tokens`

### 6.2 验证连接与模型

```powershell
scripts\run_translate.bat --check
```

这会发一个极小请求并产生少量 token。成功时会显示 API 连通、usage 和实际响应模型。
程序会区分 401/403/404/400/422/429/402/5xx；如果请求 Pro 却回了 Flash，会立即停止，
避免把不同模型的结果混在一批里。

### 6.3 看提示词与离线预算

```powershell
scripts\run_translate.bat --show-prompt
scripts\run_translate.bat --estimate
```

两个命令都不会调用 API。`--show-prompt` 会分别显示 Facebook 与 Instagram 的完整
system prompt，风格参照只取目标账号自己发布的正文，不取第三方合作方正文。
`--estimate` 会给出当前 1055 条真实正文的输入、可见译文输出和**未含 reasoning**的
基础费用参考。High thinking 的 reasoning token 无法离线可靠估计，真实 3 条 usage
才是全量依据。

品牌语域当前是 `du` + `neutral` + `moderate`。13 条术语已按真实高频语料与
Neakasa 德国站用语预填。懂德语的业务人员仍应在试译时确认术语和口吻。

---

## 第 7 步 · 分账号试译、全量与审校（F1 / F2 / F3）

前置：第 6.2 步的 `--check` 已通过。

### 7.1 旧版真实试跑记录（2026-08-30 晚；仅作历史证据）

| | 结果 | 输出 token | 费用上界 |
|---|---|---:|---:|
| `--check` | 旧 Anthropic 路径通过，且确认 thinking 已关闭 | 1 | ~0 |
| Facebook 3 篇 | **3 成功 0 失败** | 1128 | US$0.019 |
| Instagram 3 篇 | **3 成功 0 失败** | 168 | US$0.0065 |

译文已经在 `review.md` 里，双击就能看：

```
archive\fa_neakasaofficial\review.md      3 篇
 archive\in_neakasa.tech\review.md         3 篇
```

> 这 6 条不是新版验收结果：它们由旧 Anthropic 协议、关闭 thinking、标签裁到 3 个的
> 提示词生成。当前 `PROMPT_VERSION=5` 会自动把它们判为过期；本轮不会删除历史付费
> 记录，但普通试跑会写入新版后写记录。

#### 历史故障：第一次跑曾全军覆没

第一次试译**3 篇全部失败**，报的是"译文被 max_tokens=4096 截断"。
真相是：**DeepSeek 的 thinking（长推理）一直没关掉。** 原来的代码用了一个
这个端点根本不认的参数去关它——不报错、也不生效。
结果是一篇 785 字的帖子换来 **15000 字的思考**，4096 个输出额度全烧在思考上，
**一个字的译文都没产出**。

当时在旧 Anthropic 路径上的两件修复是：

1. 改用 DeepSeek 真正认的写法关闭 thinking（同一篇文案，输出从 4096 降到 30 个 token）；
2. **`--check` 以后会自己检查 thinking 是不是真的关了**。
   以前它不会——那句"回复 OK"太短，思考几句也答得出来，
   所以出现过**自检绿灯、真实文案全灭**。

这段只解释旧事故。**当前实现已经切到官方 OpenAI 接口并彻底不发送 `max_tokens`**；
若现在出现 `finish_reason=length`，程序会明确说明这是 DeepSeek 服务端上限，不会让你
去增加一个客户端输出限制。

### 7.1b 想自己再跑一遍试译

```powershell
scripts\run_translate.bat --account fa_neakasaofficial --limit 3
scripts\run_translate.bat --account in_neakasa.tech --limit 3
```

旧版 6 条会因提示词版本过期而自动重翻，不需要 `--force`。
不要只跑全局 `--limit 3`，它会先命中排序在前的 Facebook 目录，不能验证 Instagram。
### ✅ 7.1c 话题标签规则已经拍板：全部原样照搬

试译出来的 IG 译文比原文**短 40–55%**。我查过了，**德语正文本身反而略长**
（正常），缩短全部来自**话题标签从 22 个砍成 3 个**：

```
原文：#smartvacum #smartvacuumcleaner #vacuum #robotvacuum #robovac #vacuuming
     #SmartRobotVacuum #smarthomes … （共 22 个）
译文：#smartvacuum #robotvacuum #smarthome                        （3 个）
```

用户已明确决定：**原帖有几个，译帖就照搬几个**。当前规则要求标签数量、内容、
大小写和顺序完全一致，不翻译、不删减、不新增、不调序。程序会在写盘前逐个比对；
任何偏差都把该条判失败并留在待重试队列。旧版 22→3 的结果因此不能继续使用。

### 7.1d 请让懂德语的人检查

1. `du` 与 CTA 是否一致，德语是否自然；
2. 语气、换行、emoji、品牌名、型号、@提及与 URL；
3. 话题标签数量、内容、大小写与顺序是否和原帖完全一致；
4. 已经是德语的原文片段是否被合理保留；
5. 金额、数字尺码、英制单位是否正确进入人工处理流程。

金额规则是硬边界：原文 `$49.99` 必须逐字符仍是 `$49.99`。任何换算、改格式、
遗漏或新增金额都会被程序拒绝写盘并计为失败；修正提示词或模型后直接重跑该项，
不需要先 `--force` 全账号。

需要调节时，可改 `config.toml` 的 `tone` / 术语表，或修改
`prompts/translate_de.md`。改提示词后把 `translate.py` 的 `PROMPT_VERSION`
加 1；普通运行就会自动把旧版译文列为待译。只有想在**同一个版本**强制重试时才执行：

```powershell
scripts\run_translate.bat --account in_neakasa.tech --limit 3 --force
```

### 7.2 业务确认后跑全量

```powershell
scripts\run_translate.bat
```

成功结果追加到各账号的 `translated.jsonl`；只有 `post_id`、当前英文正文指纹和
当前提示词版本都一致才自动跳过，中断后可直接重跑。若正文或提示词版本改变，普通重跑只会重译
受影响的帖子，不会把旧德文继续配给新英文。付费批次有单实例锁，重复双击会被拒绝。

### 7.3 生成审校清单

```powershell
scripts\run_translate.bat --review
```

打开两个账号目录下的 `review.md`，在 VS Code 按 `Ctrl+Shift+V` 预览。
每篇包含原文、译文、配图、数字警示、合作帖原作者和人工勾选框。
译文同时派生到帖子目录的 `text_de.txt`。

过期译文不会进入 `review.md`；旧 `text_de.txt` 派生副本也会被移除，避免设计人员
误拿。历史付费记录仍保留在 `translated.jsonl`，不会被删除。

再次运行 `--review` 会把上一版保存为 `review.previous.md`，人工批注不会静默消失。
但 `translated.jsonl` 仍是译文真相源；直接修改 `review.md` 不会回写数据。
审校回写与自动发布尚未实现，不在本轮边界内。

> 当前没有真实 Key 或真实译文，所以真实 3 条质量、1055 条全量和带图审校仍是待执行
> 的业务验收；代码离线通过不等于这些步骤已经完成。

---

## ✅ 已定案：每日增量走「登录态」（方案 B）

你选了 B。文档已经全部按 B 更新完毕，下一个会话可以直接开工。

### 这对你意味着什么

原方案是每天用**完全登出**的方式抓新帖——没有账号可以被封。
但你确认了 FB/IG 没登录态就直接报错，所以增量改成**复用你登录过的那个
专用 Chrome**，每天自动看一眼有没有新帖。

**代价，你应该知道**：以前是"滚一次回填，冒一次险"；现在是"每天都要露一次面"。
风险不再是一次性的，而是天天累积。抓取小号一旦被封，回填和增量**会同时断掉**。

我在计划里加了一组必须实现的缓解措施（随机化每天的运行时刻、每次只滚两三屏
不滚到底、一遇到登录墙立刻停不重试、连续失败就停下来找你、长期没新帖自动降频）。
**这些不是可选的**，是这个方案能站住的前提。

### 你以后要做的三件事

| 什么时候 | 做什么 |
|---|---|
| **平时** | 让那个专用 Chrome 窗口保持开着。关了增量就跑不了 |
| **收到"会话失效"通知时** | 去那个窗口重新登录小号一次。**程序不会替你登**——那是全项目的红线 |
| **偶尔** | 瞄一眼 `state\alerts.log`，确认任务真的在跑，不是安静地失败了一个月 |

### ⚠️ 一条要提前想清楚的

那个 Chrome **不要拿来做日常浏览**（第 2 步里已经写过，方案 B 之后更重要了）。
它现在每天都要代表你去访问一次 Facebook 和 Instagram，
浏览器指纹应该只和抓取行为绑定。

> 如果哪天你觉得"其实不用天天查"，跟我说一声——
> `config.toml` 里 `stale_after_hours` 和降频开关都可以调，
> **调低频率就是直接降低风险**。

### 🎯 你发现的那个问题：合作帖。已修，归档多了 263 篇

你问"很多帖子是两个账号共同发的，会不会我们抓的这个只是转发角色"——
**问对了，而且这一条把之前几个结论全推翻了。**

Instagram 有「合作帖」：一方发布、**双方主页同时显示**，但数据里
只记原始发布者。我们的代码只认"谁发的"，于是把 **263 篇就在你们主页上的帖子**
当成别人的丢掉了。

| | 之前 | 现在 |
|---|---:|---:|
| Instagram 归档 | 756 篇 | **1019 篇**（756 原创 + 263 合作） |
| 可以翻译的 | 747 篇 | **1010 篇** |
| 最新一帖 | 2026-07-15 | **2026-08-27** |

**图片一张没少**（742 张全部对上，其中 94 张从 `_orphan_media/` 里捞了回来），
**你不需要重滚**。

被推翻的三件事：

1. **"IG 一个多月没发新帖"是假象。** 原创确实停在 7-15，但合作帖最新是
   **8-27**，2026 年 8 月一个月就有 21 篇。**这个账号一直在更**，
   只是主要以合作帖的形式。
2. 我之前说"增量没看到 IG 的时间线"——**说错了**，它看到了，是我们自己丢掉的。
3. 我给你的告警阈值 **FB 7 天 / IG 21 天**，其中 IG 那个是按"已停更"算的。
   现在两个账号都是日更（间隔中位都是 1 天左右），**我把 IG 收到了 10 天**：
   21 天意味着真出问题要三周才通知你。**这个数你批准过第一版，
   基础数据变了所以我改了，觉得不合适随时说。**

**关于那 229 篇第三方创作者的合作帖**：你已决定全部进翻译和发布流水线。
我在 `review.md` 里给每篇加了一行提示（原作者是谁、发布前确认二次使用授权），
`index.html` 里也有绿色的「合作 · @原作者」标签，方便你随时复查。

#### 补做的一轮（2026-08-30 晚，无需你操作）

上面那条修的是"这一次"。这轮补的是**"下一次"**——因为让这个问题拖了那么久
才被发现的，不是修不好，而是**丢弃是完全静默的**：程序丢掉 263 篇，
输出里一个字都没有。Instagram 的合作机制以后还会变。

用你已有的四份原始响应离线复核 + 加固，三件事：

1. **复核**：合作帖判定在**回填和增量两份真实数据上都是对的**
   （回填 1019 篇 = 756 原创 + 263 合作，丢弃 3 篇确实是推荐位；
   增量 36 篇 = 1 原创 + 35 合作）。Facebook 上不存在这种形态，也核对过了。
2. **加固**：合作信息在两份响应里各给一半时按**并集**合并（以前是"空了才补"，
   会把人挡在外面）；合作方条目换个形态也认得出来。
3. **哨兵**：以后**只要丢掉的帖子里有你们合作方发的，程序就会说出来**并弹通知。
   离线验证过：正常情况下一次都不响；把判定退回修复前的样子，
   回填那份立刻报 263 篇、增量那份报 35 篇。

**全程零真实访问**，用的是已经存下来的响应。

---

## 第 8 步 · 验证合作帖修复 + 增量复测（约 8 分钟）

> ## ✅ 2026-08-31：你已经全部跑完，**通过了**
>
> 下面的步骤保留下来，用途从"验证这次修复"变成**"以后想再确认时照着跑"**。
> 8.1 ~ 8.4 随时可以重跑（全离线、零风险）；8.5 是真实访问，别没事就跑。
>
> **你那次跑出来的结果**：
>
> | | 结果 |
> |---|---|
> | 归档层 | 原创 756 · **合作 263** ✅ |
> | 增量代码路径（离线） | 本账号 36 篇（原创 1 · **合作 35**）✅ |
> | 哨兵自检 | 退回旧判定后**当场中止并点名合作方** ✅ |
> | **真实抓取** | FB 新增 1 篇（**下了 5 张图**）、IG 新增 1 篇（**下了 1 张图**）✅ |
>
> **最关键的一条**：IG 那篇新帖 `3975547640610092585` 的原作者是
> `@neakasa.global`，你们只是 coauthor —— **它本身就是一篇合作帖，
> 修复前会被直接丢掉**。它被正常留下、配图也下载成功。
>
> 这同时把**图片下载**验掉了（此前几次都是"新增 0 篇"，那段代码从没跑过）。
>
> ### 那条 Facebook 告警是怎么回事？
>
> 你看到的这句是**正常的、而且是对的**：
>
> ```
> [!] facebook 最近 60 天内新出现 1 处时间缺口（阈值 5 天），最大一处 5.9 天
> ```
>
> 我查过归档：2026-08-06 → 08-12 之间 **Facebook 确实 6 天没发帖**，
> 两篇都在归档里，**不是漏抓**。这是近一年 FB 唯一一处超过 5 天的间隔。
> 同一个缺口**只会报这一次**，不会天天弹。**不需要你做任何事。**


> **前置**：只有 8.5 之后才需要专用 Chrome 开着、小号登录。
> **8.1 ~ 8.4 全部离线，零网络、零写盘，随便跑几次都不要紧。**

**顺序是有讲究的：先把能离线证明的都证明完，最后才去真实访问一次。**
每一次真实访问都是一次露面，而这个项目里能离线验的东西比想象的多。

### 8.1 归档里那 263 篇合作帖到底在不在（30 秒，离线）

```bash
.venv\Scripts\python.exe -m tools.layout index instagram
```

**应该看到**：

```
总计 1020 篇：原创 756 · **合作 264**（别人发布、本账号是 coauthor，同样在本账号主页上）
已生成 ...\archive\in_neakasa.tech\index.html  （1020 篇，814 KB）
```

- **合作 264** 就是修复找回来的那批（263 篇）**外加 2026-08-31 增量新抓的 1 篇**
  —— 那篇也是合作帖。修复前这个数是 0，总数是 756。
- 这两个数**只会往上走**：账号继续发帖，增量每天往里加。
  要看的是「合作」那一栏**不为 0 且占大头**，不是某个精确数字。
- 顺带对照一下 Facebook：`.venv\Scripts\python.exe -m tools.layout index facebook`
  应该是 **原创 47 · 合作 0**（FB 没有这个机制，合作栏有值反而是错的）。

然后**双击 `archive\in_neakasa.tech\index.html`**，往下翻：
合作帖每张卡片左上角有绿色的 **「合作 · @原作者」** 标签，抬头那行也写着篇数。

### 8.2 点开 3 篇真实的合作帖，用眼睛确认（2 分钟，离线 + 浏览器）

**这一步才是真正回答你那个问题的**——"我们抓的这个账号不是原帖发帖者，
这种帖子有没有被漏掉"。下面这几篇就是**原作者不是你们、但确实在你们主页上**的帖子：

| 发布日期 | 原作者 | 打开看 |
|---|---|---|
| 2026-08-27 | `@neakasa.global` | <https://www.instagram.com/p/Dci-ZLLjbp-/> |
| 2026-08-24 | `@neakasa.global` | <https://www.instagram.com/p/DcbUPUfEnpO/> |
| 2026-08-18 | `@snowy_daisy_catfluencers` | <https://www.instagram.com/p/DcMC7PWKMcd/> |
| 2026-08-27 | `@pommymommy` | <https://www.instagram.com/p/DcjVStopR49/> |

**每篇确认两件事**：

1. 帖子顶部显示的是 **原作者 and neakasa.tech**（合作发布的双署名）；
2. 去 <https://www.instagram.com/neakasa.tech/> 的主页九宫格里，**能找到这篇**。

两条都成立 = 这确实是"你们主页上的内容、但发布者是对方"，
而它现在在归档里。**修复前这 263 篇全部被当成别人的帖子丢掉了。**

对照组（你们自己发的，最新两篇）：
<https://www.instagram.com/p/Da0SteZkQAQ/>（2026-07-15）、
<https://www.instagram.com/p/Dan8YF5mWwE/>（2026-07-10）——
**原创停在 7 月 15 日，8 月那些全是合作帖**。这就是当初"IG 一个多月没发帖"
那个假象的由来。

> ⚠️ **打开归档文件夹时别被"没有图片"吓到。** 263 篇合作帖里 **225 篇是纯视频
> （Reels）**，而项目约定**视频只记录不下载**（体积太大、也用不到）。
> 归档里 **0 篇缺媒体、0 篇标记为媒体不全**，这个已经核对过。

### 8.3 让增量的真实代码跑一遍（30 秒，离线）

上面看的是归档结果。这一步看的是**每天真正干活的那段代码**——
它会把保存下来的真实响应喂给一个假页面，把增量流程原样跑完，
**不联网、不写盘**：

```bash
.venv\Scripts\python.exe -m tools.dryrun_delta instagram
```

**应该看到**：

```
新增 0 篇 · 本账号 36 篇（原创 1 · 合作 35，2026-06-06 ~ 2026-08-27）· 丢弃 3 · 归档最新 2026-08-27
```

**要看的是「合作 35」**：增量看到的那一屏 36 篇里，**35 篇靠合作帖判定才留得住**，
你们自己发的只有 1 篇。这个比例说明——**合作帖判定一旦坏掉，
Instagram 增量基本等于失明**。

Facebook 同样可以跑：`... -m tools.dryrun_delta facebook`
→ `本账号 6 篇（原创 6 · 合作 0）`。

### 8.4 证明"万一以后又坏了，会被发现"（30 秒，离线）

修好不等于以后不会再坏——Instagram 改字段是常事。这条命令**故意把合作帖判定
退回修复前的样子**，看程序会不会喊：

```bash
.venv\Scripts\python.exe -m tools.dryrun_delta instagram --break-coauthors
```

**应该看到它当场中止，并且直接点名**：

```
[!] 当次中止：只看到 1 篇属于 neakasa.tech 的帖子（丢弃 38 篇他人内容，期望至少 3 篇）
    —— 大概率没有拿到时间线，而不是没有新帖；其中 35 篇来自**已知合作方**
    （irina.catmom、neakasa.global、pommymommy、purrfectinfluencer）
    —— 优先怀疑合作帖判定失效，而不是被拦
```

**这就是修复的第二半**：以前丢掉 263 篇是完全无声的，现在会停下来并说清是哪一类问题。
（这条命令只改内存里的判定，**不碰任何文件**，跑完什么都没变。）

### 8.5 最后一步：真实跑一次增量（约 3 分钟，唯一一次真实访问）

前面四步都是离线的。**只有这一步能验证两件事**：
Instagram **今天的**响应里还带不带合作信息，以及**新帖的图片能不能下下来**
（历次实测都是 0 新增，下载那段代码从来没被触发过）。

> **前置**：第 1 步的专用 Chrome 开着，小号还是登录态。
> 不确定的话，在那个窗口里打开一下 facebook.com，看是不是不用重新输密码。

#### 8.5a 先空跑，不写盘、不下载

在项目根目录开命令提示符：

```bash
.venv\Scripts\python.exe -m routes.delta --dry-run --no-jitter
```

`--dry-run` = 只看不写，`--no-jitter` = 跳过那个随机延迟（正式跑时它会先随机
等 0–45 分钟再动手，那是防止每天固定整点访问；手工跑不需要等）。

**成功长这样**（这一行现在信息量大得多，是上次那句光秃秃的"新增 0 篇"改的）：

```
=== facebook / neakasaofficial ===
新增 0 篇 · 本账号 6 篇（原创 6 · 合作 0，2026-08-16 ~ 2026-08-25）· 丢弃 0 · 归档最新 2026-08-25

=== instagram / neakasa.tech ===
新增 0 篇 · 本账号 36 篇（原创 1 · 合作 35，2026-06-06 ~ 2026-08-27）· 丢弃 3 · 归档最新 2026-08-27
```

**要看的是「本账号 N 篇」以及后面那个「原创 / 合作」拆分**：

- IG 的**合计**应该是 **30 篇以上**。如果又变成 1 篇，说明合作帖那条修复
  没生效，告诉我。
- IG 的**「合作」那个数应该是大头**（离线核对过：那一屏 36 篇里 35 篇是
  合作帖、你们自己发的只有 1 篇）。**这个拆分是这次新加的**——
  合计和拆分要一起看：合计 36 而合作掉到 0，和合计掉到 1，是同一个故障，
  但只看合计的话前者看不出来。
- FB 那边「合作」应该是 **0**：Facebook 上没有这种形态（47 篇全是你们自己发的）。
- 「丢弃 3」是正常的，那是页面上混进来的推荐位（上次实测是
  Chicago Fire FC、Shaq、卖车广告这类）。
- 「新增」是 0 很正常——归档刚重建过，页面上这些帖子都已经在里面了。
  除非这两天又发了新的。

⚠️ 如果看到 **`只看到 N 篇属于 ... 的帖子 —— 大概率没有拿到时间线`**，
那是一道闸：它宁可停下来报错，也不把"没看到"伪装成"没新帖"。
把整段输出发我。

⚠️ 如果看到 **`丢弃的 N 篇里有 M 篇来自已知合作方`**，那是新加的哨兵：
被丢掉的帖子里有你们的合作方发的，**很可能是又漏判了合作帖**。
把整段输出发我即可——原始响应已经存在 `_capture_delta_*.json` 里，
我离线查，**不用你重跑**。

#### 8.5b 正式跑一次

```bash
.venv\Scripts\python.exe -m routes.delta --no-jitter
```

这次会真的下载图片、写进 `archive\...\posts\` 下的新文件夹。

#### 8.5c 再跑一次，确认不会重复

```bash
.venv\Scripts\python.exe -m routes.delta --no-jitter
```

**两个平台都应该是「新增 0 篇」。** 这是最关键的一条验收——
它证明每天跑一年也不会把同一篇写 365 遍。

#### 8.5d 看一眼状态

```bash
.venv\Scripts\python.exe -m routes.delta --status
```

```
facebook   上次成功 2026-08-30T21:14:00Z · 上次新增 0 篇 · 零新增 0 天 · 连续失败 0 次
instagram  上次成功 2026-08-30T21:15:00Z · 上次新增 0 篇 · 零新增 46 天 · 连续失败 0 次
```

这条命令**不抓取、不联网**，以后任何时候想确认"它这几天到底在不在跑"，
跑它就行。

### 跑完请把这几样发给我

1. 8.1 空跑那两行「本账号 N 篇」各是多少
2. 8.2 实际新增了几篇、有没有报错
3. 8.3 是不是两个平台都 0 篇
4. 8.4 的输出原样贴一下

### 不对时怎么办

| 现象 | 含义 | 处理 |
|---|---|---|
| `页面被重定向到 .../login` | **小号会话失效了** | 去那个 Chrome 窗口重新登录小号。**程序不会替你登**，这是全项目红线 |
| `接口返回 429（限流）` | 被限流了 | **不要立刻重试**。隔几小时再跑。程序自己已经停手了 |
| `一个接口响应都没捞到` | 页面没加载出来 | 确认那个 Chrome 窗口是活的、网络通；再跑一次 |
| `解析器可能已经与真实结构不符` | 响应结构变了 | 告诉我。`_capture_delta_*.json` 已经存下来了，我离线改解析器，不用你做什么 |
| `已连续失败 3 次，停止自动运行` | 保护机制生效 | 先按上面几条把原因解决掉，再跑 `.venv\Scripts\python.exe -m routes.delta --reset-failures` |
| `专用 Chrome 没在跑，正在拉起...` | 正常 | 它会自己把那个窗口开起来（你定的） |

> ⚠️ **看到「限流」「登录墙」时，最不该做的事是马上再跑一次。**
> 程序被设计成一遇到就停手、不重试、不换 UA、不绕——继续试探是把
> "可能被注意到"变成"确定被注意到"。你手工重试会绕过这个保护。

---

## 第 9 步 · 装每日计划任务　⬅️ **现在轮到你了**

> ✅ **前置条件已满足**：第 8 步 2026-08-31 真实验收通过（两个平台都新增了帖子、
> 图片也下载成功），所以"装一个每天准时失败的东西"这个顾虑不存在了。
>
> ⚠️ **但这一步我仍然故意不替你做。** 任务一装上，**从那一刻起每天就会真的
> 去访问一次 Facebook 和 Instagram**——方案 B 那个"累积风险"就是从这里
> 开始计的。这是一个需要你知情同意的开关，不该由程序替你按下。

### 9.1 先看它要装什么（不改任何东西）

```bash
.venv\Scripts\python.exe -m tools.schedule install --dry-run
```

只打印两条 `schtasks` 命令和生成的 XML，什么都不注册。

### 9.2 真的装上

```bash
.venv\Scripts\python.exe -m tools.schedule install
```

会注册**两个**任务：

| 任务名 | 什么时候跑 | 说明 |
|---|---|---|
| `FBScraperDelta` | 每天 09:30 | 主力。程序内部还会**再随机延迟 0–45 分钟**，所以实际时间不是整点 |
| `FBScraperDeltaCatchup` | 登录时 / 解锁时 | 补跑。合盖睡过了头靠它兜底；距上次成功不够久会自己跳过 |

几件已经替你设好的事：

- **取消了「仅在使用交流电时运行」**——笔记本常在电池上，勾着等于大半时间不工作
- **不会把电脑叫醒**去抓取（睡着就睡着，醒来后补）
- **只在你登录时运行**（不是"不管用户是否登录"）——那样拉起的 Chrome
  没有桌面会话，抓取会不稳定
- 单次最多跑 2 小时，卡住不会一直挂着

### 9.3 验证

```bash
.venv\Scripts\python.exe -m tools.schedule status
```

想立刻试一次（不用等到明天 09:30）：

```bash
schtasks /Run /TN FBScraperDelta
```

跑完看日志（**注意它会先随机等最多 45 分钟**，所以别急着看）：

```bash
.venv\Scripts\python.exe -m routes.delta --status
```

### 9.4 想停掉

```bash
.venv\Scripts\python.exe -m tools.schedule remove
```

### 装上之后你要做的三件事

| 什么时候 | 做什么 |
|---|---|
| **平时** | 让那个专用 Chrome 保持开着。关了也没关系——程序会自己把它拉起来，但那个窗口会弹出来 |
| **收到「会话失效」通知时** | 去那个窗口重新登录小号。**程序不会替你登**，这是全项目红线 |
| **每隔一两周** | 跑一次 `-m routes.delta --status`，或瞄一眼 `state\alerts.log`。这条命令不联网、零风险 |

> ⚠️ 如果连续失败 3 次，程序会**自己停下来**并通知你，不会天天硬撞。
> 处理完之后跑 `.venv\Scripts\python.exe -m routes.delta --reset-failures` 恢复。

---


## 第 10 步 · 放图片 API 密钥 + 填 DE 发布账号　✅ **你已完成**

> 2026-08-31。你拍板了：图片用 GPT-Image-2 把图内英文换成德语（K 组），
> 发布同时投 Facebook DE Page 和 Instagram DE（G 组）。

### 10.1 图片 API 密钥　✅ 已放好

`IMAGE_API_KEY` 已在 `.env` 里，程序能读到（我核对过，**没有打印内容**）。
`.env` 在 `.gitignore` 里，不会进版本库。

配置已经替你填好，不用动：端点 `https://api.inferera.com/v1`、
模型 `gpt-image-2`、输出 `jpeg`、**质量 `high`（你选的）**。

> 💰 **钱的量级**（按 OpenAI 上游价估，中转网关通常有加价）。
> 你选了 `high`，比 `medium` 贵约 4 倍：
>
> | 场景 | 张数 | 费用 |
> |---|---:|---:|
> | 最新 3 篇做验收 | ~10 | **≈ $2.2** |
> | 上线后每天 | ~5 | **≈ $1.1/天，≈ $33/月** |
> | 全量历史 818 张 | 818 | ≈ $179 —— **你已定不补发，不用跑** |
>
> 想省钱的话把 `config.toml` 的 `[image].quality` 改成 `medium` 即可，
> 月度约 $8。**但这是画质取舍，我不替你定。**

> ⚠️ **你选了"不做预扫描"，有一个后果要知道**：程序不会先判断"这张图里有没有英文"，
> **所有配图一律送去处理**，没有文字的产品图也会付一次费。
> 当前范围下这只值几美元。另一个后果更重要：**"德语文字对不对"完全靠人看**——
> 程序不知道原图上本来有哪些英文，也就没法告诉你"有没有漏改"。
> 所以审校清单（`review.md`）是唯一的验收关口，别跳过。

### 10.2 DE 站的发布目标　✅ 已填

```toml
facebook_page_name = "Neakasa Deutschland"   # 主页显示名
facebook_page_slug = ""                      # 留空，第 11 步顺手补
instagram_account  = "neakasa.de"            # IG handle
```

> ⚠️ **你给的这两个值形态不一样，我拆成了三个键。**
> `Neakasa Deutschland` 是**主页显示名**（带空格），
> 不是网址里那一段——`facebook.com/Neakasa Deutschland` 打不开。
>
> **这不影响发布**：程序在 Business Suite 的主页切换器里**按显示名**找到那个 Page，
> 全程不需要拼网址。`facebook_page_slug` 留空就行，
> 第 11 步探查时从浏览器地址栏抄一下补上即可。
>
> （为什么专门拆开：这个项目已经因为"显示名 ≠ 网址账号名"栽过一次——
> 抓取时误把显示名当账号名比对，混进了 266 条别人的帖子。）

### 10.3 准备发布用的账号

| 要求 | 为什么 |
|---|---|
| **必须和抓取小号是不同账号** | 抓取路径存在被判定异常的可能 |
| **必须有 DE Page 的发布权限** | 否则 Business Suite 里根本没有那个 Page |
| **会登录在一个全新的 profile 里**（`%USERPROFILE%\.fbscraper-publish`） | ⚠️ 见下 |

⚠️ **为什么非要再开一个 Chrome**：抓取小号被封是这个项目**唯一不可恢复**的失败模式
（回填和增量会同时断掉）。把有 DE 发布权的账号和抓取小号放进同一个浏览器指纹里，
等于把两件事绑成一件——一个出事，两个一起没。

所以以后你桌面上会有**两个**这样的 Chrome 窗口：

| 窗口 | profile | 端口 | 登录谁 |
|---|---|---|---|
| 抓取 | `.fbscraper-chrome` | 9222 | 抓取小号（US 站只读） |
| 发布 | `.fbscraper-publish` | 9223 | DE 发布账号 |

**两个都不要拿来做日常浏览。**

---

## 第 10b 步 · 跑图片德语化（K9 验收）　⬅️ **只差你点一次头**

> **前置条件已全部满足**（2026-08-31）：
> `IMAGE_API_KEY` 已在 `.env`；计划点名的**三篇都已有当前版本德语译文**
> （已真实翻译，费用上界 US$0.1799）；只读预演排出 **11 张**待处理图片。

### 先看不花钱的两条

```bash
.venv\Scripts\python.exe localize_images.py --dry-run --post-id 3975547640610092585 --post-id 3973012230169803390 --post-id 122123185335379375
```

```bash
.venv\Scripts\python.exe localize_images.py --estimate --post-id 3975547640610092585 --post-id 3973012230169803390 --post-id 122123185335379375
```

两条都是**零 API、零费用、零写盘**。应当看到 FB 5 张 + IG 5 张 + IG 1 张 = 11 张。

⚠️ **必须用 `--post-id`，不要用 `--latest-posts 3`。** 实测原因：
真实归档里第 3 新的帖子是纯视频（0 图），它占掉一个名额，
把 FB 那篇挤到第 4 位（两篇发布时间只差 4 秒），
于是 `--latest-posts 3` 只排到 6 张、**完全漏掉 Facebook**。

### 再决定要不要花这笔钱

真正跑起来会调用 11 次 GPT-Image-2（`quality = "high"`）：

| 项 | 数 |
|---|---|
| 图片数 | 11 |
| 上游参考单价 | ≈ US$0.219/张 |
| **上游参考合计** | **≈ US$2.4** |
| 中转网关加价 | 未知，真实账单以 inferera 控制台为准 |

**你点头之后**：

```bash
scripts\run_images.bat --post-id 3975547640610092585 --post-id 3973012230169803390 --post-id 122123185335379375
```

跑完之后：

```bash
scripts\run_translate.bat --review
```

`review.md` 里每张图会有**原图 / 德语图并排**，外加十个逐类勾选框
（优惠码 / 品牌 / 型号 / 合作方水印 / 金额 / 数值单位 / 认证标识 / @标签 /
德语正确性 / 产品外观）。**K3 预扫描已取消，所以这份清单是唯一的验收关口——
机器只能拦灾难，读不出德语对不对。**

### 三件跑完要记的事

1. **dHash 距离分布**：再跑一次 `--estimate` 会把 11 个真实距离全部打出来。
   人眼判定哪些是"只改了字"、哪些是"重画了"，**然后**才把
   `config.toml` 的 `[image].dhash_max_distance` 从 `-1` 改成真值。
   ⛔ 不要凭感觉先填一个数。
2. **真实账单**：与 `--estimate` 的外推值对照，量级不符要记下来。
   （翻译那边已经出现过一次：离线外推 US$0.038、实际上界 US$0.18，
   差在 thinking，与 CR-40 同型。）
3. **失败率与单张耗时**：写进 `IMPLEMENTATION_PLAN.md` 的 K9 完成行。

---

## 第 10c 步 · 看一眼「要发出去的到底长什么样」（随时可跑，零风险）

> 新增于 2026-08-31。**零浏览器、零网络、零写盘、零费用。**
> 它把发布前的全部离线硬闸跑一遍，把结果按人能读的样子打出来。

```bash
scripts\run_publish.bat --latest 3
```

或者精确指定：

```bash
scripts\run_publish.bat --post-id 122123185335379375 --post-id 3973012230169803390 --post-id 3975547640610092585 --at 2026-09-05T10:00
```

会看到每一篇的：德语正文、**逐张图是德语图还是回退的原图**、
合作帖原作者、以及所有告警（缺德语图 / 缺 G1 实测值 / 授权提示）。

**这就是 `[publish].require_confirmation = true` 那道闸要你看的那张清单。**

2026-08-31 实跑结果：计划点名的三篇**全部组装成功、零拦下**；
真实正文里的 `$219.99` 和 `$100` 逐字符保留（金额硬闸通过）；
因为 K 组还没跑，11 张图目前全部是"回退原图"并逐张点名告警。

加 `--strict` 会按真实发布的严格模式组装 —— G1 没完成时它会**正确地失败**，
提示"严格发布缺少 G1 实测定时窗口"。这是设计，不是故障。

---

## 第 11 步 · Business Suite 探查（G1，发布环节的前置）

> **这一步之前我一行选择器都不会写。** Business Suite 是 React SPA，
> class name 是构建期混淆的，凭猜写的选择器**一定**是错的——
> 这是项目的全局红线之一。

> ⚠️ **做这一步之前先跑一次 `scripts\setup.bat`。**
> 2026-08-31 发现本机缺 `tzdata`（Windows 不自带 IANA 时区数据库，
> `ZoneInfo("Europe/Berlin")` 会直接抛异常）。它已经写进 `requirements.txt`，
> setup 会装上。**没有它，G5 的排期时区根本没法做对**，
> 而排期错了帖子会在错误的时刻发出去、没人会立刻发现。

### 你要做什么

G0 已在 2026-08-31 完成并实机验收：9222 / 9223 能同时运行，新的发布
profile 没有抓取小号的 FB/IG cookie。按下面顺序做：

1. 双击 `scripts\start_chrome_publish.bat`。它只会使用
   `.fbscraper-publish` / 9223，不会碰抓取侧 `.fbscraper-chrome` / 9222；
2. 在弹出的**发布专用 Chrome** 里人工登录有 DE Page 发布权的账号。
   不要把账号密码输入任何脚本；会话过期以后也仍然是你在这个窗口里重登；
3. 另开一个终端，`cd` 到项目根目录后运行：

   ```bat
   .venv\Scripts\python.exe tools\probe_publish.py
   ```

4. 终端出现“G1 探查已开始”以后，在发布 Chrome 里按你平常的方式**手工**
   进入 Business Suite 并完整走一次：建帖 → 传图 → 写文案 → 勾选 FB 和 IG →
   开定时 → 选日期时间 → 提交；
5. 每次 click / input / change / submit 都会记录命中元素及语义祖先的
   `tag / role / aria-label / data-testid / name / placeholder / 可见文本 /
   contenteditable`，并存一张当前视口截图。**工具不会打开网址、点击、填写、
   上传或提交，驱动页面的始终是你**；
6. 完成后回终端按 Enter。程序会请你原样抄下入口 URL、FB slug、UI 时区、
   定时上下限（同时填整数秒）、IG 四类限制的接受边界与实际拒绝行为、成功信号；
   不知道的项直接留空，绝不猜。FB slug 是可选留痕、留空不阻塞；真正的
   时区/窗口/IG 边界/拒绝行为/成功信号若留空，该记录不能配置成“已验证”；
7. 把终端打印的 JSON 路径和截图目录交回来。输出形态是：

   ```text
   state/publish_probe_<时间戳>.json
   state/publish_probe_<时间戳>_screenshots/001_click.png
   ```

> ⚠️ 如果你为了取得“提交成功信号”排了一条测试帖，验证后要在 Business Suite
> 里**人工取消**。探查工具不会删除帖子或草稿；异常退出时也先检查是否留下草稿。

### 走的时候请覆盖七个流程点，并另记定时窗口上下限

| 要留意 | 为什么 |
|---|---|
| **创建帖入口的最终 URL 与按钮** | SPA 可能不换 URL，两个信号都要留 |
| **图片控件是 file input 还是拖拽区** | 决定后续能否用稳定的 `set_input_files()` |
| **文案框是 textarea 还是 contenteditable** | contenteditable 的多段换行不能想当然用 `fill()` |
| **FB 和 IG 两个渠道的勾选在哪** | 你要的是同时发，这一路是新增的 |
| **定时开关在哪里** | 后续每一步都要回读确认 |
| **日期时间选择器上显示的是哪个时区** | ⚠️ **这是整个发布环节最容易出错的一步。** Business Suite 跟 **Page 的时区设置**走，不一定是你电脑的时区，也不一定是德国时区 |
| **最早能排多久之后、最晚能排多远** | 这两个数后面会被代码依赖 |
| **IG 画幅/图片数/正文长度/标签数的边界与实际拒绝提示** | 代码只接受本次 probe 里逐项留下的实测值，不采用网上数字 |
| **提交成功的明确信号** | 必须是 toast / 跳转 / 排期列表项之一，不能“点完就算成功” |

### 用来做端到端测试的两篇（我已经挑好）

同一波内容的两个平台版本，都是 5 图：

```
Facebook   122123185335379375   2026-08-27
Instagram  3973012230169803390  2026-08-27
```

排一个**几天后**的时间，验证完再取消。

### ⚠️ 一件我要先说清楚的

**发出去的东西是不可回滚的。** 抓错了可以重抓、译错了可以重译，
**发出去了只能删帖，而删帖在粉丝侧是能看见的。**

所以我把这条链路的最后一棒默认设成**需要你点头**：
`config.toml` 的 `[publish].require_confirmation = true`，
程序把待发清单（原作者、译文、配图、排期时刻）打出来，你确认才提交。

**想全自动就把它改成 `false`**，一行的事。建议至少前几周留着。

---

## 附录 A · 命令速查

双击类（在项目根目录）：

| 命令 | 作用 |
|---|---|
| `scripts\setup.bat` | 建环境、装依赖、跑全部离线测试（只需跑一次） |
| `scripts\start_chrome.bat` | 起专用 Chrome + 调试端口 |
| `scripts\run_backfill.bat facebook` | 回填 Facebook（需人工滚动） |
| `scripts\run_backfill.bat instagram` | 回填 Instagram（需人工滚动） |
| `scripts\run_translate.bat --check` | 极小请求验证 DeepSeek 配置与实际模型 |
| `scripts\run_translate.bat --show-prompt` | 打印实际发给模型的提示词（不调 API） |
| `scripts\run_translate.bat --estimate` | 按真实待译正文估算 token/费用（不调 API） |
| `scripts\run_translate.bat --dry-run` | 列出待译帖子，不调 API |
| `scripts\run_translate.bat --limit 3` | 试跑 3 篇 |
| `scripts\run_translate.bat` | 翻译全部未翻译的 |
| `scripts\run_translate.bat --review` | 生成人工审校清单 |
| `scripts\start_chrome_publish.bat` | 起**发布用**的专用 Chrome（端口 9223，与抓取那个并存） |
| `.venv\Scripts\python.exe tools\probe_publish.py` | **G1 探查**：你手工操作，程序只记录并截图 |

**待实现**（K 组 / G 组，命令名以最终实现为准）：

| 命令 | 作用 |
|---|---|
| `scripts\run_images.bat --check` | 极小请求验证图片 API 与实际模型（**会花一点钱**） |
| `scripts\run_images.bat --show-prompt` | 打印实际发给模型的图片提示词（不调 API） |
| `scripts\run_images.bat --estimate` | 按已跑过的真实 usage 外推费用（不调 API） |
| `scripts\run_images.bat --limit 3` | 给最新 3 篇的配图做德语化 |
| `scripts\run_publish.bat --dry-run` | 组装待发清单并跑完全部离线硬闸，不碰浏览器 |
| `scripts\run_publish.bat --post <id>` | 给指定的一篇排定时发布 |

命令行类（前面加 `.venv\Scripts\python.exe -m`）：

| 命令 | 作用 |
|---|---|
| `routes.delta --status` | 看增量这几天在不在跑（**不联网、零风险**） |
| `tools.schedule status` | 看计划任务装没装、上次跑得怎么样 |
| `tools.schedule install` | 装每日计划任务（先跑 `--dry-run` 看看） |
| `tools.schedule remove` | 卸掉计划任务 |
| `routes.delta --dry-run --no-jitter` | 空跑一次增量，只看不写 |
| `routes.delta --no-jitter` | 真跑一次增量 |
| `routes.delta --reset-failures` | 失败预算用尽、你已处理完，用它清零 |
| `tools.layout index instagram` | 重新生成 index.html 总览 |
| `tools.layout reindex instagram` | 手工改过 posts/ 之后，让索引跟上 |
| `tools.layout migrate instagram` | 旧扁平布局 → 每帖一个文件夹（**已经跑过，不用再跑**） |
| `tools.replay instagram --dry-run` | 用保存的原始响应重建归档，先看结果 |
| `core.notify` | 发一条测试通知 |

> 上面这些都支持 `--dry-run`（除了 `reindex` 和 `core.notify`），
> 先看结果再决定要不要真跑。

---

## 附录 B · 产物都在哪

```
FacebookScraper\
  config.toml             全部可调参数（含品牌语域三项决策与术语表）
  scripts\                ← 你要双击的入口都在这里
  docs\                   ← 本文件、进度真相源、会话交接
  prompts\
    translate_de.md       ← 翻译提示词本体。可直接编辑，不用改 Python
  archive\<平台前缀>_<账号>\
    index.html            ← **双击这个**，一页看完整个账号的历史
    manifest.jsonl        索引（派生，可从 posts\ 重建）
    _rejected.jsonl       被丢掉的别人的帖子，留个凭证
    _orphan_media\        重建后多余的重复图片（**是移动不是删除**，可自行清理）
    posts\
      2026-07-15_1434_<post_id>\
        post.json         这篇的全部元信息（**真相源**）
        text.txt          英文原文
        text_de.txt       德语译文（翻译后才有）
        01.jpg 02.jpg     配图，编号是它在这篇帖子里的位置。**只读，程序从不改它**
        media_de\         ← 德语版图。**程序产出 + 你可以覆盖**（K 组，2026-08-31 起）
                             ⚠️ 你手工放进去的文件**程序不会覆盖**——
                             设计同事修的那张一定比模型那张对
      undated_<post_id>\  时间读不出来的帖子放这（目前没有）
    translated.jsonl      德语译文的**真相源**，抓取重跑不会冲掉它
    images_de.jsonl       德语图的**真相源**（K 组，待实现）
    review.md             人工审校清单（VS Code 里 Ctrl+Shift+V 预览）
                          ⚠️ K 组落地后它是**德语图唯一的验收关口**
    _capture_*.json       回填的原始响应。**别删** —— 解析器再出问题时靠它
                          离线重建，不用你重滚
    _capture_delta_*.json 增量每次的原始响应，只留最近 7 份、自动清理旧的
    _orphan_media\        没有归属的重复图片（**只是移动，从不删除**）
  state\
    alerts.log            告警记录（含实际走的通知通道）
    delta_state.json      增量的运行状态（上次成功、零新增天数、连续失败次数）
    delta.log             每天增量的完整输出（追加，不覆盖）
    task_FBScraper*.xml   计划任务的定义文件（装任务时生成）
    published.jsonl       **发过什么的唯一凭证**（G 组，待实现）。UI 自动化没有
                          事务性，出了事全靠它回答"到底发出去了什么、用的哪版文案"
    publish_probe_*.json  G1 探查记录（工具已实现，尚待你真实跑一遍）
    publish_failures\     发布失败时的截图（待实现）
  .env                    你的 API 密钥（已 gitignore，不会进版本库）
                          DEEPSEEK_API_KEY（翻译）+ IMAGE_API_KEY（图片）
```

> ⚠️ 旧版的 `media\` 和 `raw\` 目录已经不用了。`media\` 已空并删除；
> `raw\` 里还剩一些旧文件（FB 47 个 / IG 1500 个），**没有自动删**，
> 它们只是 `post.json` 的旧副本，你确认后可以直接删掉整个 `raw\` 目录。

---


## 附录 C · 卡住时给我什么

出问题时把下面这些贴给我，能省一轮来回：

- **完整的终端输出**（不要只截最后一行，前面的上下文往往才是原因）
- 解析出 0 篇 → `_capture_*.json` 的**路径和文件大小**
- DeepSeek 报错 → `--check` 的**完整输出**（密钥已自动打码，可以直接贴）
- 通知没弹出来 → `state\alerts.log` 的内容

❌ **任何情况下都不要把 API 密钥、账号密码贴给我。**
`--check` 的输出已经做了打码处理，可以放心贴。
