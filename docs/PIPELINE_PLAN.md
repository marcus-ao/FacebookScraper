# L 组 · 流水线自动化总体规划

> **建立日期：2026-08-31。作者视角：全局。**
> 对应 `IMPLEMENTATION_PLAN.md` 的 L 组。这是唯一一份**跨组**的文档——
> A~K 每组只回答"这一段怎么做对"，本文件回答"**这些段怎么连成一条不用人管的线**"。
>
> **开发分支：`docs/pipeline-plan` → 实施时 `feat/pipeline`。**
> 与 `feat/image-de`、`feat/business-suite-publish` 的边界见第 13 节。

---

## 0. 一句话结论

这个项目现在有 **四个各自完好、互不相连的阶段**，而且**没有任何东西在自动跑**——
计划任务至今没有安装（E3 故意留给用户按）。

把它们连起来**不需要新建队列**，需要一个**对账器**（第 2 节）。

而真正决定"人要干多少活"的**不是编排，是分流规则**（第 6 节）。
编排最多把四条命令变成一条；**分流规则才能把"每篇都要人"变成"每类只要人一次"**。

按第 11 节的估算：稳态下人工投入可以从**约 90 分钟/周降到约 10 分钟/周**，
而其中剩下的 10 分钟里，**没有一分钟是可以再省掉的**——它们全都落在
第 4.1 节那三条不可消除的事情上。

---

## 1. 现状盘点：四个孤岛，底子出奇地好

| 阶段 | 现在怎么触发 | 幂等 | 断点判据（**内容寻址**） | 失败怎么被发现 |
|---|---|---|---|---|
| 回填 | 人工滚一次，**已完成** | — | `_capture_*.json` | — |
| **增量** `routes/delta.py` | ⛔ **计划任务未装，纯手工** | ✅ `should_append()` | `manifest.jsonl` / `post.json` | ✅ toast + `alerts.log` + `delta.log` + `delta_state.json` |
| **翻译** `translate.py` | 手工命令 | ✅ | `post_id + 正文指纹 + PROMPT_VERSION` | ⚠️ **只在终端**，跑完就没了 |
| **调图** K 组 | 未实现 | 设计有 | `post_id + media_index + 原图指纹 + 译文指纹 + 版本` | 未定 |
| **发布** G 组 | 未实现 | 设计有 | `post_id + 平台` | 未定 |
| 完整性 `core/integrity.py` | 内嵌在增量里 | ✅ 只报新问题 | `delta_state.alerts` | ✅ 同增量 |

**这里有一件运气很好的事，值得点破**：

> **四个阶段的"做完了没有"全都是内容寻址的**——不是"跑过了"，
> 而是"**这份内容 + 这个版本**的产物存在"。
> 正文改了、提示词版本升了、原图被补全了，下游自动过期。

这正是对账器所需要的全部前提，而它是**在没人打算做对账器的时候自然长出来的**——
因为每一组都独立遵守了同一条纪律：**真相在文件里，索引是派生的**。

**所以缺的不是能力，是连接。** 现在的形态是：

```
人：跑 delta   →  人：跑 translate  →  人：跑 images  →  人：跑 publish
    ↑ 每一步都要人记得、要人看输出、要人判断该不该进下一步
```

---

## 2. 为什么是「对账器」而不是「队列」

**队列的做法**：delta 抓到新帖 → 写一条待办 → 翻译消费 → 写下一条待办 → ……

**不要这样做。** 三个理由，第一个是这个项目自己的教训：

1. **队列会和现实脱节。** 这个项目已经为此立过一条铁律：
   「`post.json` 是真相，`manifest.jsonl` 是派生索引，冲突时以文件夹为准，
   **方向永远单一**」。队列是第二个真相源，一旦它和归档不一致，
   就退化成"两个都不可信"——`manifest` 那次已经演示过代价了。
2. **失败恢复要额外机制。** 队列要重试计数、死信、超时回收。
   对账器不需要：**没做完的下次自然又被算出来**。
3. **新增阶段要改协议。** 对账器加一个阶段只是多一个谓词。

**对账器的做法**：每次运行时，对每一篇帖子算一次

```
期望状态：已归档 → 有当前版译文 → 有当前版德语图 → 已排期发布
实际状态：从 manifest.jsonl / translated.jsonl / images_de.jsonl / published.jsonl 读
差值    ：这篇卡在哪一阶段
动作    ：推进它能推进的那一步；推不动就归类到"为什么推不动"
```

**没有队列文件，没有状态机，没有重试计数。** 全部信息都已经在盘上。

> ⚠️ 一个必须守住的边界：**对账器只读四份产物、只调各阶段已有的入口。**
> 它不许自己解析响应、不许自己拼提示词、不许自己点浏览器。
> 一旦它开始"顺手做一点"，它就成了第五个真相源。

---

## 3. 目标架构

```
                       ┌──────────────────────────────────┐
   计划任务（每天一次）  │  pipeline run                     │
   ── 触发 ───────────▶ │  ①对账 ②按顺序推进 ③分流 ④汇报    │
                       └──────────────────────────────────┘
                                     │
        ┌────────────┬───────────────┼───────────────┬──────────────┐
        ▼            ▼               ▼               ▼              ▼
   routes.delta  translate.py  localize_images.py  publish/     needs_human
   （带抖动）     （带预算闸）    （带预算闸）      （带确认闸）   （人工队列）
        │            │               │               │              │
        ▼            ▼               ▼               ▼              ▼
   manifest.jsonl translated.jsonl images_de.jsonl published.jsonl  review.html
        └────────────┴───────────────┴───────────────┴──────────────┘
                                     │
                          state/pipeline_state.json
                        （心跳 · 各阶段积压 · 成本 · 死人开关）
```

**新增的东西只有三样**：

| 新增 | 是什么 | 为什么非有不可 |
|---|---|---|
| `pipeline.py` | 对账器 + 唯一编排入口 | 现在四条命令要人记得 |
| `state/needs_human.jsonl` + `needs_human.html` | **把"人"变成一个阶段，而不是一次中断** | 见第 5 节 |
| ~~`state/pipeline_state.json`~~ | 心跳 + 积压 + 成本 + **死人开关** | 见第 7 节。⚠️ **2026-09-01 实现时没有建这个文件**：`delta_state.json` 已经记了运行标记，再记一份就是第二个真相源。理由见 10.2，L1a 时再议 |

**沿用已被验证的做法，不发明新的**：
`needs_human.html` 走 `tools/layout.py::index` 那条路子（**双击就能看**，
用户已经在用 `index.html` 了）；状态文件走 `delta_state.json` 的形态；
告警走 `core/notify.py` 的三级降级。

---

## 4. 人工干预的诚实清单

**"人为干涉尽可能少"的正解不是砍掉闸门，是让闸门按"类"触发而不是按"篇"触发。**
下面把每一处人工都摊开，标明它属于哪一种。

### 4.1 ⛔ 不可消除的三条（不要试图自动化，它们是承重墙）

| # | 人工 | 频率 | 为什么不能自动化 |
|---|---|---|---|
| 1 | **登录**（抓取小号 / 发布账号） | 会话失效时，约数月一次 | **全项目红线 1。** 方案 B 让增量也带登录态，但那是**复用**人工登录留下的会话。会话过期的正解是通知人去登，不是替他登。多一条登录路径就多一份被 checkpoint 拦的机会 |
| 2 | **Business Suite 选择器维护**（G1 重跑） | SPA 改版时，不可预测 | **全项目红线 5。** React SPA 的 class 是构建期混淆的。计划里早就把这条写成"预期内的定期维护"，不是故障 |
| 3 | **德语质量的最终责任** | 见第 5 节的分级 | 机器读不出"这句德语自不自然"。K3 预扫描被取消之后，连"图上有没有漏改的英文"机器都不知道了 |

⚠️ 还有一条隐性的：**这台 Windows 笔记本必须开着。**
红线 6 禁止部署到 VPS/云函数（必须住宅 IP），所以"跑在哪"这件事没有自动化空间。
`FBScraperDeltaCatchup`（登录/解锁触发）已经在兜合盖睡过头的情况。

### 4.2 ✅ 可以消除的四条（这才是自动化的着力点）

#### ① 金额 → 德国站定价：**最大的一处杠杆**

**现状**：`prompts/translate_de.md` 第 3 节要求金额**逐字符原样保留**，
理由是"德国站卖多少钱是商务决策，模型无从知道"，
并明确写着「**这些金额会由人工在审校环节替换成德国站定价**」。

**这意味着每一篇带价格的帖子都要人动一次手。** 实测：

| | 图文可发帖 | 含金额 | 占比 |
|---|---:|---:|---:|
| Facebook | 27 | 10 | **37 %** |
| Instagram | 443 | 71 | 16 % |
| **近 90 天合计** | **67** | **26** | **39 %** |

**每 5 篇就有 2 篇要人工改价** —— 这是流水线上最密的一道人工。

**解法：把"每篇一次决策"变成"每个价格一次表项"。**

```toml
[publish.price_map]
# 左边是原文里逐字符出现的美元串，右边是德国站定价。
# 程序在**发布前**替换；translate 那一侧继续保持原样不动
# （译文真相源不该混进商务决策，这条边界不动）。
"$219.99" = "219,99 €"
"$299"    = "299 €"
```

**实测覆盖面**：全部图文可发帖里只出现 **46 种**不同金额串。
**一张 46 行的表覆盖整个归档。** 而且其中相当一部分是抽奖金额
（`$100`、`$200,000`）而非产品定价。

**配套的硬闸**：正文里出现了 `price_map` 里没有的金额串 → **这一篇停下来进人工队列**，
提示"请补一行价格映射"。人**填一次表**，之后这个价格永远不再需要人。

> ⚠️ 这道闸不能省。没有它，缺失的映射会以两种方式失败：
> 要么把美元价直接发到德国站（商业事故），要么静默跳过（更糟，没人知道）。
> **它是把人工从"每篇"降到"每个新价格"的那个装置，本身就是自动化的一部分。**

#### ② 合作帖的授权确认：**实测触发频率接近 0，可以白名单化**

`HANDOFF.md` 里那条顾虑是真的：263 篇合作帖里 229 篇原作者是第三方创作者。
**但那是翻译口径。发布口径下它基本蒸发了**——

| | 图文**可发**的合作帖 | 近 90 天 | 近 90 天的原作者 |
|---|---:|---:|---|
| Instagram | **39 篇**（全档只来自 16 个原作者） | 22 篇 | **只有 2 个：`neakasa.global`、`neakasa.de`** |
| Facebook | 0（FB 无此机制） | 0 | — |

**近 90 天所有可发的合作帖，原作者全是自家兄弟账号。**
那 229 篇第三方创作者的合作帖**几乎全是纯视频 Reels——本来就发不了**
（项目约定视频只记元数据不下载）。

**解法**：

```toml
[publish.trusted_owners]
# 自家账号，合作帖直接放行
own = ["neakasa.global", "neakasa.de", "neakasaofficial", "neakasa.tech"]
```

不在名单里的原作者 → **首次遇到时停一次**，人确认授权后加进名单，之后放行。
**按作者停一次，不是按帖停一次**——16 个作者对 39 篇，差 2.4 倍；
按近 90 天算是 2 个对 22 篇，差 11 倍。

#### ③ 排期时刻：规则化，不要人挑

```toml
[publish.schedule_rule]
# 程序按规则自己找下一个空位，不需要人选时间
weekdays = [1,2,3,4,5]        # 周一到周五
times    = ["10:00", "17:00"] # 每天两个位置
order    = "chronological"    # 按原发布时间正序
```

冲突处理：位置被占就顺延到下一个。窗口上下限以 G1 实测的 UI 限制为准。

#### ④ 全量翻译的 US$24 预算拍板：**在新范围下已经不必要了**

这是本次全局复盘**最值钱的一个发现**：

> 用户已拍板**不补发历史**。而**只有要发的帖才需要译文**。
> 稳态是 **5.2 篇/周**，不是 1051 篇。

`MANUAL_STEPS.md` 第 7 步那个"等你拍板全量预算（约 US$24）"的待办，
**已经不在关键路径上了**。它现在是一个可选项：
跑，就得到一份完整的德语语料（当前没有任何自动化环节消费它）；
不跑，流水线照常运转，只译它要发的那几篇。

**建议：先不跑全量。** 让对账器**按需翻译**，用真实节奏验证质量，
攒够信心再决定要不要补齐语料。
（当前已译 12 篇：旧版 3+3 已因 `PROMPT_VERSION=5` 过期，新版 3+3 有效。）

---

## 5. 自治分级：一个旋钮，四档

**不要在"全手动"和"全自动"之间二选一。** 这个项目的风险是不对称的——
抓取被封不可恢复、发布不可回滚，而翻译和调图错了只是重跑。
所以应当**按阶段、按风险分别放开**。

```toml
[pipeline]
autonomy = "assisted"   # manual | assisted | supervised | autonomous
```

| 档 | 抓取 | 翻译 / 调图 | 发布 | 人在哪 |
|---|---|---|---|---|
| `manual` | 手工 | 手工 | 手工 | **今天就在这一档** |
| `assisted` | 自动 | 自动（受预算闸） | **停在待确认** | 每次批量点头 |
| `supervised` | 自动 | 自动 | **自动发"零风险帖"**，其余进人工队列 | 只处理被分流出来的 |
| `autonomous` | 自动 | 自动 | 全自动，只有硬闸拦 | 只处理硬闸告警 |

**建议的推进路径**：`manual` →（装计划任务）`assisted` →（跑够 2 周、G8 通过）
`supervised` →（稳定一个月、无事故）视情况 `autonomous`。

⚠️ **不建议直接跳到 `autonomous`。** 理由和 C7 那七条一样：
这类风险的反馈是**延迟的，而且第一次通常就是最贵的那次**。
分级的价值不在于"慢慢来"，在于**每一档都能真实运行一段时间，
把没想到的失败模式暴露在还便宜的时候**。

---

## 6. 分流规则：把"要不要人"变成"哪些帖要人"

`supervised` 档的核心。**一篇帖子命中任一条就进 `needs_human`，
否则自动走完。**

| # | 触发条件 | 为什么它值得停 | 预估触发率（近 90 天） |
|---|---|---|---|
| 1 | 正文含 `price_map` 里没有的金额串 | 发错价格是商业事故 | 首次建表后 **≈ 0**；新价格出现时才响 |
| 2 | 原作者不在 `trusted_owners` 里 | 二次发布的授权 | **≈ 0**（近 90 天全是自家账号） |
| 3 | 德语图的 dhash 距离超阈值 | 模型整张重画了，产品外观被改 | 阈值标定后才启用 |
| 4 | 译文里出现术语表外的新产品名/型号 | 品牌一致性 | 新品发布时 |
| 5 | 这一篇的图有人工放进 `media_de/` 的版本 | 设计同事修过，说明这篇本来就特殊 | 少 |
| 6 | 该帖媒体不全（`media_complete=False`） | 素材有缺 | 归档现状为 0 篇 |
| 7 | 任一阶段连续失败达预算 | 已经不是单篇问题了 | 少 |

**规则 1 和 2 是"一次性成本"**：填一次表就长期沉默。
**规则 3~7 是"真事件"**：它们响的时候，人确实应该看。

> ⚠️ **不许为了"看起来更自动"去放宽这几条。**
> 它们和 C7 的七条缓解措施是同一类东西：
> **不是防回归，是防"跑得挺好就顺手优化掉"。**

---

## 7. 可观测性：当前最大的缺口是「死人开关」

**现在有的**：`delta_state.json`（增量的运行状态）、`alerts.log`、
`delta.log`、`core/integrity.py` 的三+一项检查、`core/notify.py` 的三级降级通知。

**现在没有、而且是最危险的那个缺口**：

> `core/integrity.py` 检查的是「**账号有没有在发新帖**」。
> **没有任何东西检查「我们的流水线还在不在跑**」。
>
> 如果计划任务因为任何原因不再触发——任务被系统禁用、`.venv` 被删、
> 笔记本连续两周没开机、`.bat` 改坏了——
> **今天没有任何机制会告诉用户。安静地停一个月，和正常运行,在用户那边长得一模一样。**

这个项目已经吃过一次同型的亏：合作帖丢了 263 篇，**丢弃是完全静默的**，
"程序丢掉 263 篇，输出里一个字都没有"，因此拖了很久才被发现。
**"没跑"和"跑了但没事做"在输出上一样**，是同一个故障模式。

### 要做的三件

1. **死人开关**：`pipeline_state.json` 记 `last_successful_run`。
   超过 `[pipeline].dead_man_days`（建议 3 天）没有成功运行 → 告警。
   ⚠️ 这个检查**不能只写在 `pipeline run` 里**——它自己不跑的时候正是要它响的时候。
   放进 `FBScraperDeltaCatchup`（登录/解锁触发）那条链路，或单设一个只读检查任务。
2. **一条命令回答"它这几天在干嘛"**：`pipeline status`，
   **零网络、零花钱、零写盘**，打印各阶段积压、最近一次成功、本月花了多少、
   人工队列里有几篇。沿用 `routes.delta --status` 已经被用户接受的形态。
3. **每周一条摘要通知**："本周发了 N 篇，M 篇等你确认，花了 US$X"。
   ⚠️ 频率要克制。`core/integrity.py` 已经立过规矩：
   **误报的代价不是打扰，是让整条告警通道失效**——用户关掉通知之后，
   真正的故障就再也没人知道了。

---

## 8. 成本闸

翻译和调图都花钱，而**自动化意味着没人盯着**。

```toml
[pipeline]
monthly_budget_usd = 60      # 超了就停下来问，不是继续跑
daily_budget_usd   = 5
```

按第 11 节的稳态节奏估算：翻译 ≈ US$1/月（5.2 篇/周）、
图片 high 档 ≈ US$33/月（11.9 图/周）。**合计约 US$34/月。**
设 60 是留出发布新品、活动周这类波动的余量。

**账单口径必须按真实 usage 外推，不许按字符或张数猜。**
这是 CR-40 的教训：F 组按字符估给出 US$1.08–5.41，真实是 **US$23.73**，
**低估一个数量级**，因为漏掉了主导项（reasoning）。
两个阶段的真实 usage 都已经逐条写进 `translated.jsonl` / `images_de.jsonl`，
对账器直接读它们。

---

## 9. 故障处理矩阵

| 类别 | 例子 | 处置 |
|---|---|---|
| **自愈** | 网络抖动、429、单条内容错误 | 退避 / 跳过留断点 / 下次自然重来。**已实现** |
| **降频** | 连续 N 天零新增 | `quiet_days_before_slowdown`。**已实现** |
| **停手叫人** | 会话失效、连续失败达预算、选择器失效、金额没映射、预算超限 | 停止自动运行 + 通知 + 进人工队列。**部分已实现** |
| ⛔ **永不自愈** | 登录、绕限流、换 UA、重试登录墙 | **红线。** 遇到就停手——继续试探是把"可能被注意到"变成"确定被注意到" |

⚠️ **一条要写进代码注释的**：用户手工重试会绕过保护。
`MANUAL_STEPS.md` 已经写了「看到限流/登录墙时最不该做的事是马上再跑一次」，
流水线的输出里也要重复这句——**自动化之后，看到告警的人更可能是"顺手再跑一下"**。

---

## 10. 实施阶段

分四段，**每一段都独立可用**，不必等下一段。

### L0 · 现在就能做，且不依赖 K/G 任何一组

> ⚠️ **2026-08-31 第七轮：L0 内部顺序改过两处，理由在下面 10.1。**
> 原顺序是 a → b → c → d，现在是 **b → c → a → （等 G1）→ d**。

- [x] **L0b `pipeline status`** —— ✅ **2026-09-01 完成**。`pipeline.py status`，
      零网络零花钱零写盘。实测口径见 10.2。
- [x] **L0c 死人开关** —— ✅ **2026-09-01 完成**。`pipeline.py check-alive`
      + 独立计划任务 `FBScraperAlive`。**两处偏离任务书，理由见 10.2。**
- [ ] **L0a 装计划任务（E3）** —— 一条命令，`MANUAL_STEPS.md` 第 9 步。
      ⚠️ **这是整个自动化的第 0 步。** 在它装上之前，
      后面所有编排都只是"把四条手工命令变成一条手工命令"。
      **现在解锁了**（死人开关已就位），轮到用户。
- [ ] **L0d 价格表 `[publish.price_map]`** —— 第 4.2 ① 。
      用现有归档的 46 种金额串生成初稿，交给业务填德国站定价。
      ⚠️ **改成排在 G1 之后**（原文写的是"可以完全先于 G 组完成"），理由见 10.1。

### 10.1 L0 顺序为什么改（2026-08-31 第七轮，K/G 合并落地之后实测）

**① L0c 提到 L0a 之前。** 原顺序是"先装计划任务，再补死人开关"。
但第 7 节自己写着：**"没跑"和"跑了但没事做"在输出上长得一模一样**，
而这个项目已经为同型失效吃过一次亏（合作帖静默丢 263 篇）。
装计划任务是**用户 5 分钟**的事，写死人开关是**我半天**的事——
把便宜的那件放后面，就不会出现"自动化已经在跑、但没人能确认它还活着"的窗口。
**代价是零**：延后 5 分钟的操作，换掉一整段无观测运行期。

**② L0d 挪到 G1 之后。** 原文的理由是"它只是一张表"——
对**数据**成立（46 种金额串已经抓好，见 14 节 L0d），
但对**位置**不成立：`[publish.price_map]` 是 `[publish]` 的 TOML **子表**，
必须放在该段最后，而 **G1 还要往 `[publish]` 写值**
（`ui_probe_dump` / `ui_constraints_verified` / 两个排期窗口数）。
先插子表就留下一个窗口：G1 之后任何人往 `[publish]` 加一个标量键，
**它会静默变成 `price_map` 的一项**。段末那条警告注释就是为这个留的，
但顺序上避开它比依赖人读注释更可靠。
**延后同样零代价**：价格替换发生在**发布前**，而发布本身被 G1 卡着。

**③ 顺带还掉一笔合并带来的债。** 实测 `grep -rn '"pipeline"' --include=*.py` 为空：
`main` 上的 `[pipeline]` 四个键**目前没有任何代码在读**。
CR-40 立过规矩「不许有改了不生效的死旋钮」，
**L0b/L0c 正是把这四个键接上的那一步**——`dead_man_days` 归 L0c，
两个 budget 键归 L1b。在那之前它们是配置骨架，别再往里加键。

### L1 · 对账器主干（K 或 G 任一落地后即可接）

- [ ] **L1a `pipeline run`** —— 按顺序推进；每阶段调各自已有的入口，不重写逻辑
- [ ] **L1b 预算闸** —— 第 8 节
- [ ] **L1c `needs_human` 队列 + 单页 HTML** —— 第 3 节
- [ ] **L1d `autonomy = assisted`** —— 抓取/翻译/调图自动，发布停在待确认

### L2 · 分流（G8 通过之后）

- [ ] **L2a 七条分流规则** —— 第 6 节
- [ ] **L2b `trusted_owners` 白名单** —— 第 4.2 ②
- [ ] **L2c 排期规则** —— 第 4.2 ③
- [ ] **L2d `autonomy = supervised`**

### L3 · 收敛

- [ ] **L3a 每周摘要通知**
- [ ] **L3b 按第 11 节量一次真实的人工分钟数**，与目标对照
- [ ] **L3c 视情况决定要不要 `autonomous`** —— 这是业务决定，不是技术决定

### 10.2 L0b / L0c 落地记录（2026-09-01）· 两处偏离任务书

新增 `pipeline.py`（两个子命令）、`scripts/run_pipeline.bat`、
`tests/tests_pipeline.py`（**61 项断言**）；`tools/schedule.py` 多注册一个任务。
全量 **19 套 / 1183 项全绿**。

#### 偏离一：**没有新建 `state/pipeline_state.json`**（第 14 节 L0c 原文要求）

任务书写的是"`pipeline_state.json` 记 `last_successful_run`"。实现时改成
**从 `state/delta_state.json` 已有的 `last_success` 读**，理由有两条：

1. **第 2 节明令不要第二个真相源。** `delta_state.json` 早就逐平台记了
   `last_run` / `last_success`，再记一份就是"队列化"的开端。
2. **判据必须是「跑过」，不是「产出过」。** 第 7 节要区分的正是
   「没跑」和「跑了但没新内容」——而 `delta_state.json` 的 `last_success`
   **抓到 0 篇也会更新**，恰好是这个语义。若改用 `manifest.jsonl` 的最新一条
   当判据，一个每天准时跑、只是没有新帖的流水线会被误判成死的，
   **而误报会让整条告警通道失效**（`core/integrity.py` 立过这条规矩）。

`pipeline_state.json` 的读取位置已经预留：L1a 的 `pipeline run` 真的有了
跨阶段运行概念之后写自己那一份，`last_successful_run()` 会取两者的较大值。
**现在这个文件不存在是正常的，不报错。**

#### 偏离二：死人开关是**独立**计划任务 `FBScraperAlive`

任务书给了两个选项（挂进 `FBScraperDeltaCatchup`，或单设一个任务），选了后者：

- **挂进增量任务的检查，会跟着增量任务一起哑掉**——而"增量任务被禁用/删除"
  正是最常见的停法，也正是这个开关要抓的东西。独立之后，
  **要两个东西同时失效才会静默，而不是一个**。
- 触发器是「登录时 + 每天 20:00」：前者抓"笔记本两周没开机"，
  后者抓"机器开着但任务被禁用"。**故意不加解锁触发器**——
  一天最多只会得出一个新结论，锁屏解锁就弹一次是纯噪音。
- ⚠️ **`RunOnlyIfNetworkAvailable` 设成 `false`**（另外两个任务是 `true`）。
  这个检查一个字节都不联网，**"家里断网了"绝不能成为警报不响的理由**。

#### 三处实现细节，都写成了断言

- **「待发布」列打 `—` 而不是 0。** `state/published.jsonl` 由 G6 写、G6 被 G1 卡着，
  今天这个文件不存在。返回空集会让"待发布"等于"可发"，
  打出一个**看起来像积压、实际是"这阶段还没接上"**的数字。与 CR-19 同源。
- **`[pipeline]` 做了双向配置审计。** CR-40 立的规矩：不许有改了不生效的死旋钮。
  实现前实测 `grep -rn '"pipeline"' --include=*.py` 为空——那四个键当时**一个都没被读**。
  现在 `dead_man_days` 真的被消费了，其余三个 `status` 会**显式打印"还没接上"**，
  而不是假装一切正常。
- **首次安装后必然响的那一次，消息里写明是自检。** 装完到第一次增量成功之间，
  `check-alive` 一定报"从未成功运行"。不解释清楚，用户第一次见到就会把通知关掉——
  **那正是最坏的结果**。

---

## 11. 量化目标：人工分钟数

按近 90 天实测节奏 **5.2 篇/周、11.9 图/周**：

| 环节 | 今天（`manual`） | 目标（`supervised`） |
|---|---:|---:|
| 记得并跑四条命令 | ~10 min/周 | **0** |
| 看输出判断有没有出事 | ~10 min/周 | **0**（异常才通知） |
| 德语文案审校 | ~25 min/周 | ~5 min/周（抽样） |
| 德语图目视检查 | ~20 min/周 | ~3 min/周（抽样 + 只看被分流的） |
| **改价格** | ~15 min/周 | **≈ 0**（一次性 46 行表） |
| 挑排期时刻 | ~10 min/周 | **0**（规则） |
| 合作帖授权确认 | ~5 min/周 | **≈ 0**（白名单） |
| **合计** | **≈ 95 min/周** | **≈ 8–10 min/周** |

**外加不定期**：会话失效重登（数月一次，约 3 分钟）、
选择器失效重跑 G1（SPA 改版时，约 20 分钟）。

**剩下的 8–10 分钟没法再压**，因为它们落在第 4.1 节那三条承重墙上。
**任何号称能把它压到 0 的方案，压掉的一定是安全性，不是工作量。**

---

## 12. 把自动化推过头会踩的红线

自动化的压力会自然地指向这几处。**它们一条都不能动**：

1. ❌ **不得为了"发布不用人登录"而实现自动登录。**（红线 1）
2. ❌ **不得为了"选择器不用维护"而凭猜写选择器或加容错兜底。**（红线 5）
   猜错的选择器 + 自动发布 = 把错误内容自动发出去。
3. ❌ **不得为了"跑得更快"而提高并发、去掉随机间隔、加大抓取深度。**（红线 3 / C7）
   C7 那七条**是方案 B 的组成部分，不是锦上添花**。
4. ❌ **不得为了"少一次确认"而删掉金额闸、授权闸、dhash 闸。**
   它们是把人工从"每篇"降到"每类"的装置——**删掉它们不是自动化，是无保护**。
5. ❌ **不得让对账器自己去解析响应/拼提示词/点浏览器。**（第 2 节）
   它一旦"顺手做一点"，就成了第五个真相源。
6. ❌ **不得部署到 VPS / 云函数。**（红线 6）住宅 IP 是硬约束。
7. ❌ **不得把发布确认默认关掉。** `require_confirmation` 的默认值是 `true`；
   改成 `false` 应当是用户在看过一段真实运行之后的显式决定。

---

## 13. 分支与所有权

| 组 | 分支 | 文件 |
|---|---|---|
| **L 组（本文件）** | `docs/pipeline-plan` → 实施时 `feat/pipeline` | `pipeline.py`、`docs/PIPELINE_PLAN.md`、`config.toml` 的 `[pipeline]` 段、`IMPLEMENTATION_PLAN.md` 的 L 组一节 |
| K 组 | `feat/image-de` | 见 `IMAGE_PLAN.md` 第 10 节 |
| G 组 | `feat/business-suite-publish` | 见 `PUBLISH_PLAN.md` 第 12 节 |

**L 组与 K / G 的边界**：

- **L 组不改 K / G 的任何实现文件。** 它只**调用**它们的入口、**读**它们的产物。
  这既是架构约束（第 2 节），也顺带让三条分支互不冲突。
- `config.toml` 的 `[publish.price_map]` / `[publish.trusted_owners]` /
  `[publish.schedule_rule]` 是 `[publish]` 的**子表**，
  按 TOML 规则必须放在 `[publish]` 段的**最后**
  （`[translate.glossary]` 已经为这条规则写过一次警告）。
  ⚠️ **它们由 L 组添加**，G 组不要动；G 组只用 `[publish]` 的标量键。
> ⚠️ **2026-08-31 补记：`[pipeline]` 整段配置目前只存在于 `wip/parallel-2026-08-31`。**
> `main` 与两条 feature 分支的 `config.toml` 里**都没有它**（实测 `grep -c` 为 0）。
> 合并时必须显式把它搬过去，否则 L0b / L0c 一开工就踩空 —— 见 `CODE_REVIEW.md` CR-49
> 与 `HANDOFF.md`「下一个会话的三步」第 2 步（那里有验证过的取块命令）。

- **L 组必须最后合。** 它依赖 K / G 的入口签名，
  在那两组合回 `main` 之前，`pipeline run` 里对应的阶段只能是占位。
  但 **L0 全段（装计划任务、status、死人开关、价格表）不依赖任何一组，现在就能做。**

---

## 14. L0 实施细则（**下一个会话直接照做**）

> 2026-08-31 补写。L0 四项**不依赖 K / G / 代码审查任何一条线**，
> 现在就能开工。分支：`feat/pipeline`，从 `main` 开。
>
> ⚠️ **下面四小节按 a/b/c/d 编号排版，但实际执行顺序是 b → c → a →（等 G1）→ d**，
> 理由见 10.1。**照做之前先看那一节**，不要按本节的排版顺序做。
>
> ⚠️ 第 15 节那条「多 Agent 共享工作区」的纪律**已经不适用了**：
> 三个 worktree 已在 2026-08-31 第七轮全部归位，
> 仓库现在是单目录单分支（见 `HANDOFF.md`「当前仓库形态」）。
> 那一节保留作历史；将来再开并行分支时它仍然成立。

### L0a · 装计划任务（用户操作，5 分钟）

**这一项不是我做，是用户做**，但它是所有后续项的前提，所以放在最前面。

```bash
.venv\Scripts\python.exe -m tools.schedule install --dry-run   # 先看要装什么
.venv\Scripts\python.exe -m tools.schedule install             # 真的装
.venv\Scripts\python.exe -m tools.schedule status              # 验证
```

装完会有两个任务：`FBScraperDelta`（每天 09:30，程序内部再随机延迟 0–45 分钟）
与 `FBScraperDeltaCatchup`（登录/解锁触发，补跑）。

**【验收】** `tools.schedule status` 显示两个任务已注册；次日 `state\delta.log`
出现新记录。

> ⚠️ **装上那一刻起，每天真的会去访问一次 Facebook 和 Instagram**——
> 方案 B 的"累积敞口"从这里开始计。这是需要用户知情同意的开关，
> **不要替用户按**（`IMPLEMENTATION_PLAN.md` 的 E3 一直刻意留着它）。

### L0b · `pipeline status`（**先做这个**，它零风险且立刻有用）

新增 `pipeline.py`，第一个子命令就是只读对账：

```
pipeline status            # 零网络、零花钱、零写盘
```

**要读的四份产物**（都已存在，不要新建第五个真相源）：

| 阶段 | 产物 | "做完了"的判据 |
|---|---|---|
| 归档 | `archive/<账号>/manifest.jsonl` | 有正文 **且** 至少 1 张已下载的图 = 可发 |
| 翻译 | `archive/<账号>/translated.jsonl` | `post_id` + 正文 SHA-256 + `PROMPT_VERSION` 三者全等 |
| 调图 | `archive/<账号>/images_de.jsonl` | `post_id` + `media_index` + 原图指纹 + 译文指纹 + `IMAGE_PROMPT_VERSION` |
| 发布 | `state/published.jsonl` | `post_id` + 平台 |

**输出应当长这样**（形态照抄 `routes.delta --status`，用户已经接受那个形态）：

```
账号                  可发  待译  待调图  待发布  人工队列
fa_neakasaofficial     27     22      27       27        0
in_neakasa.tech       443    437     443      443        0
最近一次成功运行：2026-08-31T04:41Z（1 小时前）
本月花费：US$0.00 / 60.00
```

⚠️ **"可发"要按发布口径算，不是翻译口径**：有正文 **且有已下载的图**。
实测 470 篇（FB 27 / IG 443），而不是 1051 篇——585 篇纯视频帖没有可上传素材。
这个口径差是这个项目里最容易被搞错的一处。

**【验收】** 在当前归档上跑出与上表同量级的数字；用抓包或断言确认**一次网络请求都没发**。

### L0c · 死人开关

- `state/pipeline_state.json` 记 `last_successful_run`；
  超过 `[pipeline].dead_man_days`（默认 3）→ 走 `core.notify` 告警。
- ⚠️ **不能只写在 `pipeline run` 里**——它自己不跑的时候正是要它响的时候。
  挂进 `FBScraperDeltaCatchup`（登录/解锁触发）那条链路，
  或在 `tools/schedule.py` 里单加一个只读检查任务。
- 复用 `core/notify.py` 的三级降级（toast → msg → `alerts.log`），不要新写通知。
- **【验收】** 人为把 `last_successful_run` 改到 4 天前 → 下次触发时告警弹出；
  改回当天 → 不响。

### L0d · 价格表

**原始数据已经抓好了，直接用，不用重新推导。**
全部图文可发帖里出现的**全部 46 种**金额串（按出现频次降序）：

```
$100  $5  $20  $10  $1  $50  $179  $200,000  $219  $219.99  $299  $30
$369  $399  $49  $799  $120  $79  $80  $129.99  $200  $250,000  $29
$379.99  $399.99  $59.99  $75  $89.99  $1,000,000  $119  $149.99  $180
$199  $199.99  $250  $26.99  $38.36  $39  $479.99  $499.99  $500
$599.99  $69.99  $79.99  $90  5$
```

重新生成的命令（正则复用 `translate.py::_MONEY_TOKEN_RE`，不要另写一套）：

```bash
.venv\Scripts\python.exe -c "import json,sys,collections;from pathlib import Path;sys.path.insert(0,'.');from translate import _MONEY_TOKEN_RE;c=collections.Counter();[c.update(m.group(0) for r in {json.loads(l)['post_id']:json.loads(l) for l in (b/'manifest.jsonl').open(encoding='utf-8') if l.strip()}.values() if (r.get('text') or '').strip() and [x for x in (r.get('media') or []) if x.get('kind')=='image' and x.get('local_path')] for m in _MONEY_TOKEN_RE.finditer(r['text'])) for b in Path('archive').iterdir() if (b/'manifest.jsonl').exists()];print(len(c));print(' '.join(sorted(c,key=lambda t:(-c[t],t))))"
```

三件要注意的：

1. **`5$` 是脏数据**（美元符号在后面）。别在代码里"顺手规范化"它——
   映射表就按原文逐字符匹配，规范化会让"原文里到底是什么"这件事变得不可查。
2. **大额是抽奖金额不是定价**（`$200,000` / `$250,000` / `$1,000,000`）。
   它们**多半应当原样保留**，业务填表时要能区分"这是价格"和"这是奖池"。
   建议表里允许写 `"$200,000" = "$200,000"`（显式声明不改），
   **而不是留空**——留空和"还没填"分不开。
3. **替换发生在发布前，不在翻译时。** 译文真相源
   （`translated.jsonl`）不该混进商务决策，这条边界不动。

**表的位置**：`config.toml` 的 `[publish.price_map]`。
⚠️ 它是 `[publish]` 的**子表，必须放在该段最后**（TOML 规则），
`[publish]` 段末尾已就地留了警告注释。**加子表时先确认 G 组已经不再往
`[publish]` 加标量键**，否则它们会静默变成 `price_map` 的键。

**硬闸**：正文出现表里没有的金额串 → 该篇进人工队列，提示补一行映射。
**【验收】** 46 种全部有映射；构造一个新金额串 → 该篇被正确拦下并说清原因。

---

## 15. ⚠️ 多 Agent 共享工作区的协作纪律（2026-08-31 实战教训）

**2026-08-31 当天，三个 Agent（代码审查 / K 组 / G 组）与本会话
在同一个 `FacebookScraper` 目录里并行工作。** 这暴露了一件事：

> **git 一个工作目录只能有一个 checked-out 分支。**
> 三个 Agent 各自以为在自己的分支上，实际上全都在改同一个工作区、
> 提交时会落到同一个分支。**谁先 `git add -A`，谁就把另外两条线一起提交进去。**

当天的实际后果：`config.toml`、`translate.py`、`tests/tests_translate.py`
各自混了两到三条线的内容，**按线拆分提交已不可能**，只能一次性收下并在
提交说明里记归属（见 `3047c65`）。

### 三条必须遵守的

1. **一次只让一个 Agent 动手。** 这个项目的稳态量是 5.2 篇/周，
   并行省下来的时间远不值这个复杂度。
2. **如果一定要并行**：每个 Agent 开工前先 `git status --short`，
   **只 `git add` 自己所有权表里的文件**，永远不要 `git add -A` / `git add .`。
3. **不要 `git checkout` 切分支**（会把别人未提交的改动带走或卡住），
   也不要 `git stash` / `git reset --hard`（项目既定禁令）。
   需要让 `main` 前进时用 `git branch -f main <已验证的提交>`——
   它**不碰工作区**，前提是 `main` 未被 checkout 且是目标提交的祖先。

### ✅ 更正（2026-08-31 晚）：`git worktree` 实际走通了

上面第 15 节写的三条纪律仍然成立，**但下面这条结论是错的，已被实践推翻**：

> ~~`git worktree` + 给每个 Agent 一个独立目录 —— **不能靠 junction 共享 `archive/`**：~~
> ~~`core/store.py::assert_physical_direct_path` 会**主动拒绝**~~
> ~~symlink / junction / reparse point，`Archive()` 一构造就抛异常。~~

**「不能靠 junction 共享 `archive/`」这半句仍然成立**（`assert_physical_direct_path`
确实会拒绝 reparse point）。**但结论错了——不共享 `archive/` 就行。**

2026-08-31 晚 K / G 两组的实际做法：

```
FacebookScraper            wip/parallel-2026-08-31      完整 archive/（主工作区）
FacebookScraper-image-de   feat/image-de                只复制 manifest/translated 做夹具
FacebookScraper-publish    feat/business-suite-publish  完全不需要 archive/
```

**离线测试全部用 `tempfile` 夹具，本来就不读真实 `archive/`。**
两条分支各 17 套全绿，`config.toml` 也自动合干净了——
**所有权表 + 独立 worktree 的组合是有效的**，上一轮的混线是
「三个 Agent 一个工作目录」造成的，不是并行本身的问题。

⚠️ **但真实数据验收仍然必须回主工作区做**：`--estimate` / `--dry-run` /
K9 / G8 都要读完整 `archive/`，worktree 里的夹具会给出误导性的 0。
⚠️ 还有一个真踩过的坑：**`cd` 到主目录之后再跑 `tests/...`，跑的是主目录的旧快照**，
不是 feature 分支的代码。跑测试前先 `pwd`。

详细的 worktree 现状见 `HANDOFF.md` 的「🧭 三个 worktree 的现状」一节。

---

## 16. 如果只做一件事

⚠️ **这个问题有两个答案，取决于你问的是哪条终点线**——
2026-08-31 第七轮补记，K/G 落地之后这两条才真正分得开：

**要「能自动跑」→ 装计划任务（`tools.schedule install`，L0a）。**
在它装上之前，这个项目的自动化程度是 **0**——
所有的幂等、断点、告警、降级都写好了、测过了，
**但没有任何东西会在没人的时候运行它们**。
紧接着就是 **L0c 的死人开关**：一旦开始无人值守运行，
"它是不是还活着"就立刻变成比"它抓到了什么"更重要的问题。
**所以实际顺序是先 L0c 再 L0a**，见 10.1。

**要「能发出去」→ G1 的真实 DOM 探查（`MANUAL_STEPS.md` 第 11 步，约 20 分钟）。**
这是**唯一**卡着 G2–G9 全部九项的东西，也是"全都建好了"与"真的能发一篇"
之间仅剩的一步。`publish/selectors.py` 现在是空的、`business_suite.py`
五个函数在碰 `page` 前就抛 `ProbeRequired`——**这是对的（红线 5），
而解开它的钥匙只在用户手上**，没有任何代码能替代。

**两条终点线互不阻塞，可以并行推进。**
自动化那条现在就能动手（L0b/L0c），发布那条只能等用户那 20 分钟。
