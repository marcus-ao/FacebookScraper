# Stage H — 运行状态

完成 Header 唯一入口、五阶段保守 mapping、顶部可处理事项、业务结论及技术折叠区。进程活跃单独显示；未知、未观测、不可读不会变成成功。30 秒共享查询更新指示器，阅读页保持当前快照，变化后显示刷新提示。

browser-stage-h.json：登记送达必须 message ID；未送达恢复必须核对接收组/摘要并勾选确认；关闭中断付费批次显示费用和文案/图片核账提示。三个 POST body 的 action/version/message_id 或 batch_id/version/outputs_reviewed 均与旧接口一致。未确认发布只有计数，提供维护说明，不伪造帖子链接。已实际推进浏览器时钟验证自动检查不替换阅读页。

后端核对结果：core/feishu.py 的 Outbox.status() 明确不返回接收对象及消息正文，因此确认框要求运营从飞书核对后填写接收组与原消息摘要。此信息仅用于本次确认，不伪装成服务端原文、不追加请求字段。自动展示接收对象/原文需要另行批准后端契约，列为新的非阻塞后续项。DEF-1 继续延期。

428 前端测试通过（原 415 + C 的 6 + H mapping 的 7）；类型检查与构建通过。真实飞书重发和付费调用为 0。
