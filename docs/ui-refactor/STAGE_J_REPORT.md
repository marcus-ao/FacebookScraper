# Stage J — 视觉、键盘与最终交付

20 张正式截图（10 个视图 × 1366×768 / 1920×1080）逐张查看并量取。列表首屏 12/19 行、48px 行高；历史分页首屏可达。正文/图片/标签/第三方详情小屏 1.20–1.48 屏，大屏不超过 1.34；可操作详情唯一主按钮、行内 danger=0、边框 1–3。冻结详情无写入口，保留来源查阅。所有页面 h1=1、无横向或吸顶头溢出。

本阶段只修实际验收缺口：图片高度单独 token；初翻授权移到正文前 warning Alert、紧凑显示并预留正文空间；语义标签可单个删除且同步草稿；标签建议显示付费口径；关闭的浮层不再屏蔽正文快捷键；补 antd 在 Chromium 中首尾 Tab 移往浏览器工具栏的情况。

Modal/Drawer 正反向 Tab、Esc、焦点恢复、可见浮层抑制、关闭后 N/P/方向键/Esc、编辑输入、删除标签、reduced motion 均通过。现有浮层动画完成后验证键盘循环。保留原来进入/关闭生命周期，不自建弹窗框架。

最终 npm test 430/430、22/22 文件，build/typecheck 通过。D5 受影响的初翻/优化/恢复与两次终态轮询再次通过；新旧 16×2 请求对照再次通过。原有 Python 不作无关扩测。全部数据仍在隔离 fixture，真实外部操作 0。

完整 A–U 结论、逐页量值、B1–B24、文件清单、延期项及只写不执行的切换/回滚步骤见 FINAL_IMPLEMENTATION_REPORT.md。原 BASELINE 保持。
