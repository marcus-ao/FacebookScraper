# Stage D — 单篇审核

D1→D5 按序完成。详情共用列表 Query 与 URL 上下文，保留 56px 动作条、三个页签、人工编辑与草稿恢复、独立分类保存、图片逐张核对、完整排期契约、初翻/优化及记录抽屉。

隔离浏览器证据：browser-stage-d1.json 至 browser-stage-d5.json，以及 browser-stage-d.json。八状态的编辑、处理动作与排期判据全部通过；带补充平面表情的长文标记落点与重叠优先级通过，滚动位置 1353；两种 DST 歧义/不存在时刻由真实 berlin_time 解析函数拒绝，HTTP 动作仍 stub。冻结详情无写入口，未查看图片不禁用通过。详情刷新后的 n/N、相邻项、返回筛选及草稿返回守卫均通过。

初翻与优化各两次轮询到终态后停止；中断恢复仅传 expected_updated_at，未重复调用模型；候选覆盖有确认。D1 的 check、保存、409 保留草稿通过，D2 的光标插入、存储占位符、标签保护和分类版本字段通过，D4 的 200 未确认回执、409 备选和严格五字段提交通过。

421 前端测试、typecheck/build 通过。历史完整详情的 B19/B24 已复跑通过（browser-stage-e.json）。全量请求对照和剩余组合情形在 I 统一取证，视觉密度在 J 检查。

发现并修复一项实际竞态：冲突恢复期间暂停保存，防止已保存后被较晚的恢复响应重新打开草稿。未变更后端、生产配置或状态；付费、发布和外部发送均为 0。
