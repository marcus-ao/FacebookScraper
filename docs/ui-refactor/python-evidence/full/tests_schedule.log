[1] 生成的 XML 本身要是合法的、且是那三个任务
  OK   生成三个任务：FBScraperDelta / FBScraperDeltaCatchup / FBScraperAlive
  OK   任务名纯 ASCII —— 它要经过 schtasks 的命令行
  OK   三份 XML 都能被 XML 解析器接受

[2] 参数不能搞反（搞反了要几天后才看出来）
  OK   每日任务调用 pipeline run，由 autonomy 决定 manual/assisted 行为
  OK   每日任务**不带 --if-stale** —— stale 阈值 26 小时 > 24，带上会变成跑一天、跳一天
  OK   补跑任务调用 pipeline run --if-stale —— 否则每次解锁都要真抓一遍

[3] 三个触发器（每天 / 登录时 / 解锁时）
  OK   每日任务有按天的日历触发器
  OK   触发时刻取自 config.toml 的 [delta].daily_time（实得 2026-01-01T09:30:00，配置 09:30）
  OK   补跑任务有「用户登录时」触发器
  OK   补跑任务有「会话状态变化」触发器
  OK   那个会话触发器是**解锁**（唤醒后解锁即补跑；schtasks 命令行做不出这个）

[4] 笔记本相关的设置（错一个，任务就大半时间不工作）
  OK   FBScraperDelta：不勾「仅在使用交流电时运行」（笔记本常在电池上）
  OK   FBScraperDelta：切到电池时不中断
  OK   FBScraperDelta：错过的触发在醒来后补跑
  OK   FBScraperDelta：**不把机器叫醒**来抓取
  OK   FBScraperDelta：只在用户登录时运行 —— 不能是 Password/S4U，那样拉起的 Chrome 没有桌面会话，CDP 附着行为不确定
  OK   FBScraperDelta：上一次还没跑完时不并发第二个
  OK   FBScraperDelta：有执行时间上限（PT2H），卡住不会永远挂着
  OK   FBScraperDeltaCatchup：不勾「仅在使用交流电时运行」（笔记本常在电池上）
  OK   FBScraperDeltaCatchup：切到电池时不中断
  OK   FBScraperDeltaCatchup：错过的触发在醒来后补跑
  OK   FBScraperDeltaCatchup：**不把机器叫醒**来抓取
  OK   FBScraperDeltaCatchup：只在用户登录时运行 —— 不能是 Password/S4U，那样拉起的 Chrome 没有桌面会话，CDP 附着行为不确定
  OK   FBScraperDeltaCatchup：上一次还没跑完时不并发第二个
  OK   FBScraperDeltaCatchup：有执行时间上限（PT2H），卡住不会永远挂着
  OK   FBScraperAlive：不勾「仅在使用交流电时运行」（笔记本常在电池上）
  OK   FBScraperAlive：切到电池时不中断
  OK   FBScraperAlive：错过的触发在醒来后补跑
  OK   FBScraperAlive：**不把机器叫醒**来抓取
  OK   FBScraperAlive：只在用户登录时运行 —— 不能是 Password/S4U，那样拉起的 Chrome 没有桌面会话，CDP 附着行为不确定
  OK   FBScraperAlive：上一次还没跑完时不并发第二个
  OK   FBScraperAlive：有执行时间上限（PT10M），卡住不会永远挂着

[5] 指向的入口脚本得真的存在
  OK   scripts\run_delta.bat 存在（E1）
  OK   scripts\run_pipeline.bat 存在（L0b/L0c）
  OK   FBScraperDelta 指向 run_pipeline.bat
  OK   FBScraperDelta 的工作目录是项目根（.bat 里也会 cd，双保险）
  OK   FBScraperDeltaCatchup 指向 run_pipeline.bat
  OK   FBScraperDeltaCatchup 的工作目录是项目根（.bat 里也会 cd，双保险）
  OK   FBScraperAlive 指向 run_pipeline.bat
  OK   FBScraperAlive 的工作目录是项目根（.bat 里也会 cd，双保险）
  OK   死人开关传的是 check-alive —— 传错子命令会变成一个不告警的哑任务

[6] 所有 .bat 的字节级约定（靠人守迟早会破，所以在这里守）
  OK   run_backfill.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_backfill.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_backfill.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_delta.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_delta.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_delta.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_images.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_images.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_images.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_pipeline.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_pipeline.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_pipeline.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_probe_signals.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_probe_signals.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_probe_signals.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_publish.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_publish.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_publish.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_publish_post.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_publish_post.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_publish_post.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_python.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_python.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_python.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_scheduler.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_scheduler.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_scheduler.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_translate.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_translate.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_translate.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_web.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   run_web.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   run_web.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   setup.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   setup.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   setup.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   start_chrome.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   start_chrome.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   start_chrome.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   start_chrome_detect.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   start_chrome_detect.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   start_chrome_detect.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   start_chrome_publish.bat 纯 ASCII（含中文的行会被 cmd 从中间劈开、后半段当命令执行）
  OK   start_chrome_publish.bat 没有裸 LF（LF 换行会让 cmd 误解析整行）
  OK   start_chrome_publish.bat 没有 BOM（cmd 不认 BOM，会把 @echo off 一起打坏）
  OK   run_delta.bat 设了 PYTHONIOENCODING —— 输出重定向进 delta.log 时，本机 cp936 编不出 ⚠/❗，第一次打印就会把整个进程带走
  OK   输出是**追加**（>>）而不是覆盖（>）：上一次的输出往往是排查今天问题的唯一线索
  OK   参数原样透传，所以 --if-stale / --status / --reset-failures 都能用

全部通过
