[1] 端口监听与 CDP 就绪是两件不同的事
  OK   本地测试服务端口确实在监听
  OK   普通 HTTP 服务不会被误认成 Chrome
  OK   含 webSocketDebuggerUrl 的 /json/version 被认作 CDP
  OK   探测的是 Chrome 标准版本端点
  OK   cdp_ready() 不给端口时仍退回 [chrome].debug_port
  OK   显式 profile 与监听进程匹配时 CDP 才就绪
  OK   端口虽是 CDP 但属于另一 profile 时失败闭合
  OK   Windows 把整段 --user-data-dir 参数加引号时仍能识别含空格 profile
  OK   只给 --user-data-dir 的值加引号时同样能识别
  OK   端口返回脏响应时安全地判定为未就绪

[2] start_chrome 遇到非 CDP 占用时不会继续启动 Chrome
使用 Chrome: unused-chrome.exe
专用 profile: D:\Temp\tmp1a5v8_89
调试端口:     43210

[!] 端口 43210 已被其它程序占用，但它不是 Chrome 调试端口。
    请关闭占用程序，或修改 config.toml 的 [chrome].debug_port。
  OK   非 CDP 端口占用返回失败，而不是假报 Chrome 已运行
  OK   没有在已占用端口上启动第二个进程

[3] launch 的显式 port/profile 不会串回抓取小号配置
  OK   零等待且模拟端口未就绪时如实返回 False
  OK   显式目标只启动一个 Chrome 进程
  OK   发布端口 9223 进入 Chrome 参数
  OK   发布 profile 进入 Chrome 参数
  OK   没有悄悄退回抓取 profile
  OK   显式发布 profile 会在启动前创建
  OK   无参 launch 保持旧 CDP-only 行为，不调用 Windows profile 归属检查

[4] attach 参数化保留旧调用，并给发布入口正确提示
  OK   attach 同时接受显式 port 与 profile
  OK   发布端口没开时提示发布专用启动脚本，不误导去开抓取 Chrome
  OK   提示同时点名发布账号与目标 profile
  OK   无参 attach 保持旧 CDP-only 行为，PowerShell 归属不可用也不阻断抓取

[5] [publish] 的派生配置与 [chrome] 彼此独立
  OK   两个 CDP 端口分别读取各自配置
  OK   两个 profile 分别读取各自配置
  OK   发布与抓取的派生 profile 不会指向同一路径
  OK   端口/profile 都不同时隔离配置通过硬校验
  OK   发布端口误配成抓取端口时启动前失败闭合
  OK   发布 profile 误配成抓取 profile 时启动前失败闭合

[CR-59] launch 轮询期间不做昂贵的 profile 归属核对
  OK   端口起来之后 launch 返回 True
  OK   整个启动过程只核对 2 次 profile 归属（开头一次 + 就绪后一次），不是每秒一次 —— 每次都要 fork 一个 powershell.exe（CR-59）
  OK   轮询用的是不带 profile 的轻量 CDP 探测
  OK   轻量探测次数必须多于昂贵核对次数；反过来说明有人把归属核对搬回轮询里了

[归属核对的两个真实教训（CR-63，2026-09-01 用户调 G1 时踩的）]
  OK   归属核对超时 >= 15 秒：实测这条链路要 ~3.5 秒，5 秒那版**每次都超时**，于是一个完全正确的发布 Chrome 被报成'核对不了'并失败闭合
  OK   从 netstat 输出里取出监听端口的 PID
  OK   不同端口取到不同 PID
  OK   **端口按整段匹配，不是子串**：9223 不能命中 92230，反之亦然
  OK   ESTABLISHED 不算：只认 LISTENING，否则会拿到连过去的那一头
  OK   IPv6 的 [::1]:9225 也能解析
  OK   没人监听时返回 None
  OK   netstat 失败时返回 None，不抛异常
  OK   拿不到 PID → None（核对不了），**不是 False**
  OK   拿不到命令行 → None，不是 False
  OK   命令行里的 --user-data-dir 与目标一致 → True
  OK   确实是别的 profile → False（这一种才该让用户去关浏览器）
  OK   attach 按三态分诊：'是别的 profile' 与 '核对不了' 必须给不同的话
  OK   '核对不了'那一支给出可自查的只读命令，而不是让用户去关浏览器
  OK   真正执行的命令里不再有 Get-NetTCPConnection —— 它光加载 NetTCPIP 模块就 ~5.7 秒，是原来那次超时的主因
  OK   端口→PID 走 netstat（0.12s），不是 PowerShell cmdlet

全部通过
