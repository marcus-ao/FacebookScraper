
[1] [pipeline] 配置审计：不许有死旋钮，也不许有拼错了却静默失效的键
  OK   合法配置正常读出
  OK   拼错的键被拒绝——静默失效的配置项是这个项目明令不许有的（CR-40）
  OK   少了键也被拒绝，不悄悄用默认值兜底
  OK   autonomy 只接受四档里的一个，不接受看起来合理的别名
  OK   dead_man_days = 0 被拒：那等于把死人开关关掉，必须显式改配置才行
  OK   布尔值不算整数（Python 里 True == 1，这个坑踩过就知道）
  OK   负预算被拒
  OK   autonomy、死人开关与日/月预算都已经有真实消费者
  OK   真实 config.toml 的 [pipeline] 段能通过审计（合并时补回来的那一段）

[2] 最近一次成功运行：判据必须是「跑过」，不是「产出过」
  OK   取各平台 last_success 的最大值
  OK   点名证据来源——用户要能自己去核对那个文件
[ok] 流水线活着：最近一次成功运行 2026-09-01T04:41:34Z（7.3 小时前），阈值 3 天。
     证据：delta_state.json[instagram]
  OK   **抓到 0 篇也算活着**：这正是 PIPELINE_PLAN 第 7 节要区分的两件事，拿产出当判据会把一个健康的流水线误判成死的
  OK   L1a 以后写的 pipeline_state.json 会被一并考虑；现在缺了也不报错
  OK   没带时区的时间戳当作没有证据——不猜本机时区，猜错会让死人开关早响或晚响整整一天

[3] 死人开关（L0c）：任务书那条验收
  OK   4 天前 → 判定为死并告警一次
  OK   告警里同时写出实际天数与阈值，用户不用去翻配置
  OK   告警直接给出下一步查哪两样，而不是只说'出事了'
  OK   --no-popup 一路传到 notify，自动化验收不糊桌面
  OK   改回当天 → 不响，一条通知都不发
  OK   从未跑过也算死，不是'暂时没数据'
  OK   首次安装后必然响的那一次被明确写成自检——不解释清楚，用户第一次见到就会把通知关掉，那是最坏的结果
  OK   还没装的情况直接指向 MANUAL_STEPS 第 9 步
  OK   退出码 0/1/2 三态分开：'查不了'与'查出来是死的'不能混成同一个非零码
  OK   默认真的走 core.notify（三级降级 toast → msg → alerts.log）——上面几条都注入了假 notify，这条防的是'假的留在了默认值里'

[4] 「可发」是发布口径，不是翻译口径（这个项目最容易搞错的一处数字）
  OK   只有'有正文且有已下载的图'算可发
  OK   纯视频与无媒体帖不算可发——归档 1067 与可发 470 的差就是这 597 篇
  OK   **有 URL 但没落盘**不算：发布要上传真实文件，不是 URL
  OK   脏行被跳过而不是让整份对账崩掉（manifest 允许有脏行）

[5] 待发布只认 scheduled/source_refs，并受激活边界约束
  OK   published.jsonl 不存在表示尚无 scheduled 成功记录
  OK   逐行容错且只把最终 scheduled 计为已发布；prepared 不扣积压
  OK   积压按每个账号的 source_refs 求交集；其它账号记录不会从本账号扣除
  OK   激活前历史库存不计入待发布，只统计边界后的 source_refs
  OK   G8/activate 前待发布列打 ——，不会把历史存量冒充流水线积压
  OK   可发的口径写在脚注里，读表的人不用去翻文档
  OK   status 如实说明 autonomy 与预算已经接入 pipeline run

[6] 本月花费按真实 usage 算，不按字符/张数外推（CR-40）
  OK   真实费率可用时不报问题
  OK   只统计本月那一条，按 config.toml 的真实费率算（1.32 + 3.96）
  OK   没有 images_de.jsonl 时图片花费是 0，不是编一个数
  OK   上月那条归到上月
  OK   上个月跨年正确
  OK   上个月常规情况正确
  OK   时间解析只接受带时区的 ISO，其余一律当作没有
  OK   status 从独立 ledger 计入 output_rejected 真实费用，并按 paid_request_id 去重产物
  OK   status 明示 paid ledger 的 uncertain/unknown，不再假称费用完整

[7] 表格列宽按终端显示宽度算（中文占两列）
  OK   CJK 按 2 列计
  OK   补齐之后中英文行的显示宽度一致——%-24s 按字符补会让表歪掉

[8] 死人开关必须是一个**独立**的计划任务
  OK   plan() 出三个任务；status/remove 都遍历它，加在这里就等于处处加上
  OK   死人开关跑的是 run_pipeline.bat check-alive，不是增量入口
  OK   **它不能跟增量共用入口**：要抓的失效正是'增量任务不跑了'，挂进去就会跟着一起哑掉
  OK   不要求联网：这个检查一个字节都不联网，**'家里断网了'绝不能成为警报不响的理由**
  OK   FBScraperDelta 仍然要求联网（它真的要联网）
  OK   FBScraperDeltaCatchup 仍然要求联网（它真的要联网）
  OK   不加解锁触发器：一天最多一个新结论，锁屏解锁就弹是纯噪音——误报的代价是让整条告警通道失效
  OK   登录时 + 每天各查一次：前者抓'笔记本两周没开机'，后者抓'开着但任务被禁用'
  OK   绝不把机器叫醒
  OK   只读检查给 10 分钟就够，不沿用抓取那个 2 小时
  OK   XML 头与命名空间与另外两个一致
  OK   任务名保持纯 ASCII（schtasks 的任务名走命令行）

[9] run_pipeline.bat 的字节约定（cmd 对这三样零容忍）
  OK   没有裸 LF——LF 换行会让 cmd 误解析整行（CR-62 刚踩过一次）
  OK   没有 BOM
  OK   纯 ASCII：中文消息一律留在 pipeline.py 里
  OK   设了 PYTHONIOENCODING：check-alive 的输出会重定向进 pipeline.log，本机 cp936 编不出 ⚠，第一次打印就会把整个进程带走
  OK   check-alive 的输出是**追加**不是覆盖：无人值守跑的东西，上一次的输出往往是排查今天问题的唯一线索
  OK   用 goto 标签而不是括号块取 ERRORLEVEL——块里的 %ERRORLEVEL% 在解析时就展开了，拿到的是命令执行**前**的值

[9] preflight 把「现在到底差什么」算出来，而不是靠手维护一张表
  OK   预检覆盖发布证据闸与 14 个 UI 上限那一位
  OK   预检覆盖 GO_LIVE 上剩下的每一步，不用人再去对照文档
  OK   预检回答的是「激活之后每天会发生什么」，不只是「配置填没填」
  OK   预检末尾必须给出下一条命令，否则读完还是不知道按哪个键
  OK   预检是只读判断，用退出码表达通过与否，不抛异常
  OK   预检的只读承诺写在 docstring 里；它会在真实 archive 上跑，不许有副作用

全部通过
