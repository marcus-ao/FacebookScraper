# 需求与共识

**基线日期：2026-09-12；事实同步至 2026-09-14。** 本文件定义做到什么才算满足业务需求，以及**每个验收单元现在是什么状态**。功能形状和编号以 [FUNCTIONALITY.md](FUNCTIONALITY.md) 为准，证据能证明到哪以 [HANDOFF.md](HANDOFF.md) 为准，人工操作顺序在 [MANUAL_STEPS.md](MANUAL_STEPS.md)。

五阶段与八批实施范围已经接受。本文列需求，不把"入口存在"或部分测试通过写成业务完成；每个验收单元独立判断。

## 0. 状态词

全项目共用这五个词。**一行只能用一种状态**；宽功能按子项拆行，已知代码缺口不能只列外部依赖。

| 状态 | 判据 |
|---|---|
| 代码未完成 | 验收单元有已知实现、恢复、失败闭合或必需接口/界面缺口；已有部分代码或测试不能关闭整个单元 |
| 离线通过 | 该单元所需行为存在，且有对应离线证据；不表示真实账号/企业权限/远端结果成立 |
| 待真实联调 | 该行代码前置已具备，剩余是明确的真实数据、人工操作、权限或远端验收 |
| 真实通过 | 有日期、真实输入、结果和可复核证据，**只适用于证据覆盖的范围** |
| 明确延期 | 已接受的范围决定，本轮不做；与缺陷或联调依赖分开 |

## 1. 目标与边界

系统面向一名上海运营，把美国站公开图文内容转成可审校、可追溯、可在德国站单渠道排期的内容。人的日常动作应集中在审校、修改、选择时刻和确认发布；系统负责归档、付费处理、确定性检查、状态留痕和失败闭合。

目标账号固定为：

| 平台 | 当前目标 | 处理规则 |
|---|---|---|
| Facebook | `neakasaofficial` | 独立抓取、独立审校、只发 Facebook |
| Instagram | `neakasa.global` | 时间线合作帖全部抓取；独立审校、只发 Instagram |
| Instagram 历史 | `in_neakasa.tech` | 冻结，只读；不得继续抓取、加工或发布 |

Facebook 与 Instagram 不配对、不合并、不共用发布候选。相同文案或素材也不能改变来源渠道。

本轮只处理有正文且媒体全部为静态图片的帖子。纯视频、图文混合、无媒体和无正文都要分类计数并可见，但不加工、不发布。

## 2. 用户与人工边界

本轮只有一名审校人，界面为中文，在岗窗为上海 08:00–19:00。她可以：

- 修改德语正文、语义标签、单篇链接覆盖和 Instagram bio 引导语；全局映射由业务明确维护，在 Web 只读；
- 查看并选择标签建议，标签建议不可成为发布硬依赖；
- 对每张图片发起最多三次优化；
- 挂起任务，默认三个上海工作日后恢复；
- 填写理由后决定不发，或下载资源后转人工处理；
- 选择任意合法柏林时刻；同渠道前后 90 分钟有占用时，系统拒绝并给出建议；
- 在看到最终文案、图片、账号、渠道和时刻后确认一次真实提交。

Web 运营设置只开放默认柏林时刻（默认 10:00/17:00，预填而非固定槽）和挂起工作日数（默认 3）。保存带配置版本 CAS，拒绝未知/受控字段，保留注释和无关参数。其它受控配置和提示词只读，原解释随界面展示，不能用确认弹窗代替权限边界。

机器不得覆盖人工文案或人工图片。源帖更新时保留人工劳动并标记需要复核，不静默丢弃。

本轮不做登录与 RBAC。所有审校事件必须保留 `actor` 字段，值固定为 `null`。未来启用身份管理前，不得把 `null` 解释成某个具体人员。

## 3. 监测与抓取要求

抓取必须依附人工登录的专用 Chrome，不实现自动登录。浏览器角色分为三套独立 profile：

| 角色 | 默认端口 | 行为 |
|---|---:|---|
| 回填 | 9222 | 人工滚动历史；不自动滚到底 |
| 发布 | 9223 | Business Suite 提交和 Planner 回读 |
| 探测 | 9224 | 低深度增量探测与兜底对账 |

探测调度从运营作息推导：上海 08:00–19:00 默认每 60 分钟、离岗默认 180 分钟，07:00 附近带抖动兜底。发帖窗每月依据上个完整月归档重算，返回两平台样本数、覆盖率、窗口和计算时间；月份/样本不足保持原计划并说明原因。具体频率受 C7 预算约束，不自动提频、重试或增加深度。

处理截止预算覆盖两个平台扫描及整批全部翻译、风险、图片与余量，据此前移兜底并持久化同一 08:00 截止点；不能用单篇最坏时间保证整批完成。模型、标签周采样和远端镜像/消息 I/O 在各自执行器运行，不阻塞调度主循环；正在处理时新发现不丢，扫描事实与处理事实分别保存。常驻任务支持安装/查询/停用/恢复，检测旧每日任务并发；生成 XML 或 mocked schtasks 通过不能当成实际安装。

时效验收分别统计源帖发布时间至首次待审就绪、批次请求至内容就绪的 P50/P95，以及上海晨间截止前就绪率。第一项包含发现等待，按来源引用只计首次就绪；两项不能混用。只统计已记录的源时刻和就绪事实，历史缺数显示未知，不用激活时间、修改时间或零延迟补造样本。

`preflight` 必须呈现出口 ASN 类型、近期出口/ASN 变化、稳定性和证据新鲜度，区分网络断开与会话/平台阻塞；缺证据显示未知。状态只读已有记录，不新增 FB/IG 请求。当前 9224 `.global` HTTP 429 已持久停机，恢复须先人工核对会话和出口。

新增内容、跳过内容、错误、会话异常和错过的兜底窗口都必须可观测。“没有新帖”和“有帖子但全被分类跳过”必须能区分。

兜底运行中途过期时，保留该平台本轮普通探测；只有实际执行深扫后才能合并普通探测。过期跳过进入技术告警和晨间摘要，按日期/平台去重，不能只留 scheduler 内部错误。下一次时刻先落盘，避免恢复后集中补跑。

## 4. 内容与付费要求

归档必须记录来源平台、目标账号、真实作者、合作方、发布时间、永久链接、原文和每个媒体项。`account` 与 `owner` 不得混用；Instagram 是否在目标时间线上由 `parse.on_timeline_of()` 判断。

本地化编辑器把正文、语义标签和链接分开。品牌、型号、优惠码、@提及、合作方署名和需要原样保留的标签不得被机器改写；图片中的数值和单位不自动换算。不确定时保留原样交给人。

Facebook 可在正文光标处插入 `{{linkN}}`，由对应德国链接替换；未插入的有效链接在末尾追加，已插入的不重复追加。非法编号/缺目标/损坏占位符阻止通过，IG 不支持。即时 `/check` 使用完整当前本地化草稿，按替换后的最终正文、标签和链接/CTA 计数；返回前的估算必须注明，不能把占位符长度当成真实 URL 长度。

所有模型调用共用月度/每日预算和追加式付费账本。第三方来源先取得内容级许可。超预算、缺许可或远端计费状态不确定时失败闭合。重放不确定请求之前必须先核账。

模型任务关联拥有进程、job ID、paid request ID、source/prompt 版本和更新时间；中断批次以 operation_id 关联付费记录和费用。恢复区分正在执行、中断未请求、结果已落盘、远端可能已计费；持版本核对，恢复动作不隐含重试。旧候选在来源或提示词版本变化后不可应用，费用统计纳入风险扫描和不确定金额。相关恢复代码已有定向测试，真实供应商核账另验。

原文未变化与机器结果仍有效分开显示。`stale=false` 仅说明原文哈希未变，不保证提示词版本当前；界面/API 应说明机器版本和当前版本。当前旧 Facebook 机器译文为提示词版本 5、当前为 6，当前读取没有可接受的德语图，原 FB 目录未发现 media_de；这不能证明“曾有 FB 德语图但因版本失效”，也不能自动触发付费重做。冻结 `.tech` 的 3 张历史德语图不作新来源素材。

风险提示分两类：确定性检查来自本地规则；翻译前真实预扫描覆盖 `pun`、`ambiguous`、`us_only`，结果绑定源文与提示词哈希/版本、模型及时间。未扫、失败、成功零风险不能混淆；失败不称安全。`web/api/fixtures/risks.json` 仅演示，不参与真实决定或验收。

标签推荐的三类来源都在范围：Google Trends 公开 CSV 在同一英文标签的德语候选组内、`geo=DE`、相同时间范围/同批比较；IG 全球累计量级；德语同类账号近 14 天用词频次按周采集。三者分别保留原始值、来源、采样时间和 freshness/失败原因，不能混比不同组 Trends 分数或把 IG 全球累计称德国热度。空同类名单跳过，失败/过期降级为候选加人工选择；降级不免除三个采集器的实现责任。品牌/型号原样，默认标签数与原帖等量，不静默削减。

Trends 导出只接受与本次候选组、地域和起止日匹配的被动 CSV 按钮证据；登录按钮或普通链接不能替代导出控件。CSV 保留原始字节，兼容 BOM、CRLF、周/月日期；上下文必须绑定源文与原始数据摘要。429/challenge 持久停止，人工确认恢复后通过只读状态取得当前版本，再持锁 CAS 恢复，不能删状态或无版本重置。当前 Trends 与 `.global` 均真实遇到 HTTP 429 并已停止，尚无本轮三来源真实采样验收。

## 5. 存储与恢复要求

业务真相与派生物必须单向流动：

| 数据 | 角色 | 恢复规则 |
|---|---|---|
| `post.json`、原图 | 来源真相 | 原图 URL 可能过期，不假设能重下 |
| `translated.jsonl` | 机器译文真相 | 由付费结果产生 |
| `translated_human.jsonl` | 人工文案真相 | 不可由机器覆盖 |
| `review_items.jsonl` | 审校决定真相 | 损坏行必须显式失败，不能静默忽略 |
| `paid_requests.jsonl` | 付费真相 | 不确定项先核账 |
| `published.jsonl` | 发布真相 | 不可重建；丢失可能造成重复发布 |
| manifest / SQLite / HTML / Planner cache | 派生 | 可由本地真相重建，不参与反向决策 |
| 飞书云盘 | 单向镜像 | 不反向读；新版本不覆盖旧版本 |

接续另一工作区或机器时，先停写并备份核对整个 archive/state/运行配置，含 `config.toml`、存在时的 `config.local.toml`、本机 `.env` 受控副本与解释器位置/版本。凭据不进共享证据。所有入口绑定相同数据和锁，保留 `published.jsonl`、付费账本和激活边界；不能“重新开始”清空。

2026-09-12 本次接续已有核验备份 `runtime.zip`（5,246 文件、465,064,677 字节），原激活 `2026-09-03T09:00:58.277710Z` 保留。主配置/不可重建 state 账本的周期包、大型证据与媒体的独立 hash 版本已有离线验证；一次本地备份不等于企业云盘周期运行和恢复已验收。

源图变化必须按每张实际字节哈希、数量和顺序识别，同名同大小换内容、增删和换序都不能漏。保留人工稿/图并提示复核；已排期仅提醒，不改冻结内容。SQLite 与文件比较实际记录集合及字段，发现缺失/多余/不符后重建或只读回退，不能仅检查文件存在。

同名 `source_text_sha256` 全链路采用统一的来源文字规范并有 AST 守卫；原始风险扫描另存 scan_text_sha256 保护高亮偏移。最终提交文案/媒体的精确指纹不随来源文字规范改变，不能为了统一字段破坏冻结字节绑定。

云盘补送只能来自冻结内容快照。历史版本缺失时补送同一版本；当前内容已变化时不得用新内容冒充旧版本。

## 6. 飞书要求

飞书卡片是审校入口。德国运营组接待审/积压/排期成功/排期失败，开发者组接系统告警，**两个接收组独立**。未尝试消息发送前重新读取当前状态，按帖子聚合，发现和处理同帖不重复计数；终结或挂起项退出待审。卡片展示当前有效德语首图、人工优先德语摘要、来源/图片数、确定性检查和风险状态；原图回退、图片上传失败明示并保留链接/可行动文字。

一旦尝试发送，UUID、接收组、正文与图片引用固定。远端结果不确定先核对；原卡仍有效时，人工 resolve 后补送保持同 UUID/同内容，不能重组当前版本冒充原消息。聚合卡部分帖子失效时保留被取消的旧卡和尝试历史，仅为仍有效帖子向尚未收到的收件人生成新提醒，不重复已收者、不复活失效帖。缺凭据、上传失败、明确失败和不确定状态在运行页可见。

在岗窗即时推送，离岗到下一 08:00 合并；晨间摘要包含兜底补抓、分类跳过、处理延迟/失败、待审数量和最早等待。14:00/18:00 仅有积压提醒；无新增、积压、失败或可行动变化时不推空日报。机器向外部服务报心跳，缺席由外部观察者告警；不能依靠停机后也会停的本机告警替代。

outbox 默认保留 30 天终态热数据，完整关联事件/投递组件超期后按 SHA 归档，保留原卡片/UUID/回执与主状态去重索引。未决、待重试、待发送、离岗未发或仍有缺失收件回执的内容不归档；保留期不是按创建时间删除所有旧事件。

企业飞书应用、收件人、云盘目录和权限都需要真实企业环境联调；离线测试不等于这些权限已可用。

## 7. 审校、历史与排期要求

业务状态固定为七态：`pending_review`、`edited`、`snoozed`、`approved`、`scheduled`、`skipped`、`handed_off`。`skipped` 与 `handed_off` 含义不同，不能合并统计。

审校账本必须追加写、并发检查和版本检查。任一非空坏行、截断、非法状态/revision/时间都使读取失败并定位，不得跳过后计算“最新状态”。该严格读取已有定向证据，当前完整集成仍需回归。

日常任务聚焦当前待办；90 天是默认窗口，历史列表须服务端分页、总数、平台/月/tag/状态过滤并显式返回查询 range/page/total。详情按稳定 ID 查询，不受 90 天上限，冻结 `.tech` 可只读访问。查询不触发翻译或其它业务写入，也不为方便展示加载全部历史到浏览器。本轮原归档 1,067 条与 SQLite 实际一致，真实第 1/2 页各 20 条、页间无重复及冻结只读详情已有证据。

排期按柏林时刻让运营选择，按 Business Suite UI 时区验证可选范围。当前 UI 的同月边界是 2026-09-01 真机事实；系统必须在跨月时重新读取并适配当前可见范围，不能永久用固定“同一自然月”规则代替。

提交前和提交后都要按渠道检查占用。人工选的冲突时刻直接拒绝并给建议，不自动顺延。`scheduled` 只在远端回读确认后写入；它不表示到了发布时间后已公开。

月历必须读取完整可见月份的所有日期格/时刻条目，包含手工 Planner 任务和延迟加载。活跃时间建议不是帖子，月份标题或 35 格外壳不能证明完成；不完整/未知不能判为空档。选时用缓存提示，提交前持发布锁实时复核。公开状态另做只读观察并存时间/证据，无明确依据显示 unknown，不从排期到点推定公开。

每个发布入口都必须先冻结并展示同一份提交快照：最终正文、图片字节/哈希、单一目标渠道、目标账号、柏林时刻和来源版本。批准、CLI 或其他入口都不能绕过快照。

真实单渠道发布须有新证据证明渠道切换、目标账号、该渠道时间控件与提交后回读。2026-09-13T05:01:33Z 的生产月历读取已覆盖 35 格、4 条公开帖和 2 个已排除推荐时段，含 3 个独立 IG remote ID；这只验收本次月份读取。新提交回读仍需精确全文、完整日期格范围、唯一渠道/账号/remote ID 因果及最终图片证据；远端图片数量和顺序当前未知，不能由本地快照反推。资产 ID 或同一卡片的时间证据冲突时拒绝确认，旧双渠道 journal 不能作本轮 G8。所有入口共用 preflight/激活/提交能力判据；回执不确定只幂等补有证据的状态、快照、镜像和通知，不重新点击。

当前单渠道选择、完整正文/身份/时间回读代码已有离线和复审结论；**远端 scheduled 详情图片读回适配器仍未实现**，需先对受控且具体确认的排期取得真实控件证据再补适配。G8 必须同时有 full_caption_equal、remote_images_verified 及远端媒体数量/顺序/source SHA 与冻结清单一致的证据；只有编辑器图片通过或 scheduled/正文/ID 不能升级真实验收。既有 scheduled 记录仍用于防重，不因验收闸变严格而丢掉历史。

## 8. 验收证据

离线验收包括最终同一集成版本的完整 Python、前端 build 和版本化离线浏览器回归入口，文档与 API/配置一致。版本化入口须有固定命令、夹具/schema 版本和行为断言，不只截图。初版 `42c8bc9` 的 6 条浏览器后新增 FB 光标链接，共 7 场景；保存/历史/设置/链接计数使用临时真实后端，远端与运行恢复状态的显式 UI 夹具仅验界面。全部当前修改的最终整套结果另记，不把浏览器回归扩大为业务端到端通过。`GET /api/runtime` 与 `preflight --json` 共用五阶段只读状态契约，缺失/未知/过期/损坏清晰，无模型、社交或消息副作用。真实验收分别留日期、账号、输入和远端证据，不包含凭据。

以下每项要独立记录：

1. `neakasa.global` 与 Facebook 的真实增量探测和跳过分类；
2. 真实模型任务、预算和孤儿恢复；
3. 三类真实标签采样各自来源、时间与失败/过期/空名单降级；
4. 飞书两接收组、当前德语首图/有效摘要/检查、原图回退、按帖去重、晨报和冻结尝试重试；
5. 云盘新版本、冻结补送和单向约束；
6. 完整月历的手工任务/延迟加载/活跃建议排除，90 分钟冲突、跨月/DST、只读公开 unknown；
7. 长正文完整回读、多图字节/数量/顺序、FB 链接和 IG CTA 各自真机证据；
8. 坏账本显式失败、历史分页/总数/范围和 90 天外详情；
9. 原 archive/state/config 恢复保持激活/幂等，周期备份/大型证据版本与真实 DB 一致性；
10. 先准备两篇实际文案、图片、账号、单一渠道和柏林时间给用户确认，再各做一次 FB-only/IG-only 排期及 remote ID 回读；
11. 运营从真实飞书卡片进入审核、修改、选期确认并收到排期回执的完整流程。

测试夹具、模拟响应、静态风险文件、跑在空数据上的页面和人工改写 journal 都不能算真实验收。

## 9. 明确延期与固定边界

| 项目 | 状态 | 决定 |
|---|---|---|
| 生产机迁移 | 明确延期 | 本轮开发机，迁移另验 |
| 登录/RBAC/真实 actor | 明确延期 | 单人，`actor: null`，不补造身份 |
| 视频加工/发布 | 明确延期 | 本轮分类计数 |
| FB/IG 内容复用 | 明确延期 | 独立车道与人工决定 |
| `supervised`/无人审核发布 | 明确延期 | 真实提交先展示具体内容并取得确认 |
| 发布任务队列 | 明确延期 | 冲突拒绝、保持对账器；模型批次不改变此边界 |

自动登录、自动滚历史、突破 C7、数据库反向决定业务、云盘反向同步是**固定禁止边界**，不作为未来待实现项。延期不关闭任何已接受的代码缺口或真实联调。

## 10. 五阶段验收状态

**事实截至 2026-09-14，代码截至 `3718c0d`。** 这一节原先是独立的 `docs/OPTIMIAZATION.md`，合并进来是为了让"要做到什么"和"现在做到哪"挨在一起。状态词判据见 §0；下面的测试文件名是验收入口，不代表已经执行。

历史基线：规划基线 `c67c56a` 曾通过 47 个 Python 脚本与一次前端构建；核心集成 `565f17c` 全量执行 65 脚本，首轮 64/65（唯一失败是 `tests_feishu` 新增测试 `read_text` 未指定 encoding，在默认 GBK 下失败，生产 UTF-8 读写无此缺陷），修正后相关补跑通过，65 脚本均有通过记录。**不把它写成一次全绿。** React 审校台切换后的最新一轮是 Python 66/66、React 498/498、typecheck 干净。

### 10.1 阶段一：监测有没有新帖

| 编号与验收单元 | 状态 | 代码缺口或下一步 | 外部依赖 | 验收入口 |
|---|---|---|---|---|
| F1-1 目标与独立车道 | 离线通过 | `.global` 所有合作帖归档，`.tech` 冻结，FB/IG 独立 | 无 | 目标/合作方/冻结账号回归；不按 owner 丢合作帖 |
| F1-1 `.global` 初次回填 | 待真实联调 | 人工恢复会话后建新目录/游标，核对时间跨度与媒体完整性 | 当前 429 停机；人工登录和出口 | 实际数量、最早/最新时间、覆盖和合作方样本；旧 `.tech` 不算 |
| F1-2 在岗/离岗、月度发帖窗 | 离线通过 | 上个完整月按两平台计算样本数/覆盖率；不足保留计划，不提频 | 真实数据不足时明示 | `tests_monitoring`；月份/边界/样本/覆盖/保留原因可复算 |
| F1-2/F1-3 双平台整批截止预算 | 离线通过 | 累计扫描、风险/翻译、全部图片和余量；前移兜底并固定同一截止点 | 真实耗时后续校验 | 定向验证含 503/3003 分钟跨日期；需真实批次完成时刻 |
| F1-3 兜底与补抓 | 离线通过 | 有限深扫、时刻/深度抖动；错过窗口不追补全部轮次 | 探测 profile、C7 允许 | 差集、抖动、错过窗口测试；补抓接晨报另验 |
| F1-3 兜底中途过期 | 离线通过 | 只在实际深扫后合并普通探测；前序耗时超窗仍普通探测，跳过进技术/晨间消息，先落盘下次时刻 | 真实晨间批次另验 | `afb6784` + `tests_pipeline_service`/`tests_scheduler_recovery`：不中途吞探测、不重复通知 |
| F1-4 三浏览器隔离 | 待真实联调 | 9222 回填、9224 探测、9223 发布；启动不代表有效登录 | 三套人工会话与稳定出口 | 路径/端口隔离与各角色真实身份/会话结果 |
| F1-5 独立执行器与持久批次 | 离线通过 | 模型、标签周采样、远端镜像/消息 I/O 移出主循环；处理中新发现持久保存；不确定不自动重领 | 实际长批次另验 | `tests_pipeline_service`/`tests_runtime_recovery`：后继批次不丢、周采样无重复、外部 I/O 不堵维护 |
| F1-5 Windows 生命周期代码 | 离线通过 | scheduler-install/status/disable/enable；旧每日任务冲突拒绝，停用结束实例、恢复启动实例 | 无 | XML/任务状态/命令测试，未知状态失败闭合 |
| F1-5 Windows 真实安装/恢复 | 待真实联调 | 运行前置通过后检查 Action/账户/工作目录再安装 | 任务计划程序权限、会话和安全闸 | 安装/查询/停用/恢复、重启单实例、旧任务互斥记录 |
| F1-6 高频连带机制 | 离线通过 | 窗口最小间隔、capture 按天、失败预算、零新增降频、僵死锁 | 无 | scheduler/delta 回归；恢复不提高预算 |
| F1-7 扫描/处理事实与分类 | 离线通过 | 分别存开始结束/结果；视频/混合/无媒体/无正文/失败分项 | F4 晨报 | 零新帖、全跳过、补抓、中断输出不同；日报按帖去重另验 |
| F1-8 出口 ASN/稳定性 | 待真实联调 | preflight 只读已有证据，显示类型、时间、出口/ASN 变化、过期/未知 | 人工确认稳定住宅/普通宽带 | `tests_network_evidence` + 当前真实类型/稳定性记录，不新增平台请求 |
| F1 处理时效/晨间就绪率 | 离线通过 | `requested_at → content_ready` 的 P50/P95、样本数、晨间就绪率已输出；缺数据 unknown | 连续真实处理批次 | metrics 5 项定向通过；不冒称源发帖端到端延迟 |
| F1 源发帖至待审时效 | 离线通过 | `source_created_at`/`post_content_ready` 已接，按来源首次就绪统计并含发现等待 | `.global` 覆盖、真实新帖 | `tests_runtime_status` 6 项含源发帖 P50/P95；缺历史事实不补造 |

### 10.2 阶段二：抓取、存储、历史与镜像

先保住原始内容、人的决定与发布幂等，再重建查询。原图 URL 可能失效，媒体字节和历史回执不能假设能重取。

| 编号与验收单元 | 状态 | 代码缺口或下一步 | 外部依赖 | 验收入口 |
|---|---|---|---|---|
| F2-6 备份与路径绑定 | 真实通过 | 原 archive/state/config 已核验接续，保留旧激活/引用 | 本机受控备份 | 5,246 文件、465,064,677 字节的核验备份与哈希；不公开 `.env` |
| F2-6 所有入口同源同锁 | 离线通过 | runtime binding 有定向测试；最终核验批处理/CLI/Web/调度组合 | 原解释器、本机配置 | `tests_runtime_config`：同 archive/state/env/锁 |
| F2-6 坏审校账本失败闭合 | 离线通过 | 已有严格读取与定向恢复验证，保留现场在副本修复 | 无 | `tests_review`/`tests_content_recovery`：坏中间行、截断、非法字段/revision/时间报文件与行，不降级待审 |
| F2-1 历史分页/筛选/旧详情 | 真实通过 | 原数据 page1/page2 每页 20、页间无重复，冻结详情只读 | 当前真实 archive 只读 | 原 1,067 条历史读取；`tests_history`/浏览器回归的筛选、range/page/total、90 天外详情；不触发翻译 |
| F2-1 SQLite 实际一致性 | 真实通过 | 原归档 1,067 条与 SQLite 实际记录/字段一致，失配有离线恢复测试 | 后续持续变化另观察 | `tests_query_index` 缺失/多余/字段错误回归 |
| F2-2 月份布局/稳定文件名 | 离线通过 | 新 `.global` 按月+slug；slug 只创建时算，`.tech` 原布局保留 | 真实迁移另核验 | 布局/恢复测试；改正文/tag 不改 slug 或误移媒体 |
| F2-3 分类 tag | 离线通过 | 型号表预填、多 tag、未分类，人工修改有 revision | 业务分类 | 版本冲突、主 tag 落点、全部 tag 元信息保留 |
| F2-5 源图片变化 | 离线通过 | 指纹含每张实际字节、数量、顺序；人工劳动保留 | 可控来源样本 | `tests_content_recovery`：同名等长换字节、增删、换序；旧候选失效，已排期只提醒 |
| F2-5 来源文字摘要统一 | 离线通过 | `source_text_sha256` 全链路统一 + AST 守卫；风险另存 raw `scan_text_sha256`，最终提交字节摘要不改 | 无 | `tests_hygiene`/`tests_risk_scan`/`tests_publication_recovery`：字段算法与高亮偏移、来源空白兼容 |
| F2-4/F5 快照与补投影 | 离线通过 | Web/CLI/人工结转共享冻结，receipt 绑指纹，幂等补状态/镜像/通知 | 真实回执另验 | `tests_publication_recovery`：中断、重复恢复、当前文件变不改旧版 |
| F2-4 镜像代码与恢复组合 | 离线通过 | 冻结补送/统一入口投影与独立远端 I/O 执行器已接，原版不被当前内容替换 | 企业云盘另验 | `tests_mirror`：v1/v2、源改后补 v1、仅移 tag 路径、幂等恢复 |
| F2-4 云盘权限/版本实测 | 待真实联调 | 受控目录验证上传/移动/版本/恢复，无反向写本地 | 企业应用/目录权限，当前未启用 | file/folder ID，v1/v2 并存、冻结 v1 补送 |
| F2-4 周期备份/大型证据代码 | 离线通过 | 主配置/state 账本周期包，大型 probe/截图/冻结媒体独立 hash 版本；本机密钥另受控备份 | 真实周期和云盘权限另验 | `tests_mirror` 定向：配置/完整账本、小回执入包，大证据单独版本 |
| F2-4 周期备份实际恢复 | 待真实联调 | 企业目标配置后核两个周期/旧版本恢复，不能用一次 `runtime.zip` 代替 | 云盘根/权限、安全备份位置 | 时间/哈希/数量、恢复副本、远端 file ID；凭据不入共享证据 |

恢复顺序：停写 → archive/state/config/本地环境受控备份 → 验账 → 副本重建派生 → 核对原激活/发布幂等 → 恢复同一绑定。历史查询不意味着重译冻结账号。

### 10.3 阶段三：本地化、风险、标签与恢复

| 编号与验收单元 | 状态 | 代码缺口或下一步 | 外部依赖 | 验收入口 |
|---|---|---|---|---|
| F3 三区块编辑/人工优先 | 离线通过 | 正文/hashtags/链接分区，人工稿/图优先 | 当前来源 | `tests_localization`/`tests_web_review` 回归，刷新/重建不覆盖 |
| F3-1 FB 链接 | 待真实联调 | 未映射进人工队列；单篇覆盖不改全局；链接不进翻译输入 | 德国落地页确认 | 一篇 FB 最终链接/目标正确，长文回读保留链接 |
| F3-1 FB 正文链接/实时计数 | 离线通过 | 光标插入 `{{linkN}}`，按已选 URL 替换；未插入末尾追加，非法编号/目标/token 拒绝，IG 不支持；`/check` 按最终草稿计数 | 真实落地页另验 | `tests_localization`/`tests_web_review`/浏览器回归：插入位置、最终字符数、无重复、失效拒绝 |
| F3-2 IG CTA/稳定 bio | 待真实联调 | 链接移出正文、稳定聚合页只读；不自动改 bio | 业务确认聚合页 | 一篇 IG 最终 CTA 与当前 bio 对应 |
| F3-3/F3-4 优化/付费边界 | 离线通过 | 单篇指令追加、模板只读、每图最多受理 3 次、统一预算/许可 | 真实模型账单另验 | `tests_refinement`/`tests_paid_requests`：并发第 4 次拒绝、人工图不变 |
| F3-4 模型任务恢复/旧候选 | 离线通过 | 按进程/job/request/source/prompt 对账，恢复不发新付费请求 | 不确定项人工核账 | `tests_content_recovery`/`tests_refinement`：执行中不抢占、未请求收敛、不确定阻塞、源变候选不可应用 |
| F3-4 恢复 UI/费用呈现 | 离线通过 | job 与批次 `operation_id` 关联 paid request、费用、CAS 版本；人工核对恢复不重新付费 | 真实供应商核账另验 | runtime/content recovery 后端定向；浏览器用明确 UI 夹具验证中断/费用/取消恢复 |
| F3-8 风险扫描代码 | 离线通过 | 翻译前真实调用路径，pun/ambiguous/us_only，源文/提示词绑定 | 无 | `tests_risk_scan`：未扫/失败/成功零风险/stale，缺钥匙不造 paid started，费用入账 |
| F3-8 实际语义扫描 | 待真实联调 | 一篇受许可/预算约束的真实扫描并核 usage | 模型凭据/许可 | request ID、模型/提示词/结果/时间、供应商用量；夹具不算 |
| F3-5 排序/降级 | 离线通过 | 品牌型号不推荐，标签数默认与原帖等量，来源/时间可见 | 无 | `tests_hashtag_rank`/`tests_hashtag_sampling`：Trends 跨组不混排，缺/过期/失败可手选 |
| F3-5 Trends 证据门控导出代码 | 离线通过 | CSV 按钮专属被动 proof、DE 同英文标签候选组/同起止日、周/月解析；CLI 保留 BOM/CRLF 原字节，context 三摘要与源文必核；状态版本 + 共享锁 CAS 恢复 | 无 | `7f131a3`：22 相关 + 6 rank；后续 11 项导出测试通过、T1–T4 复审关闭；未真实下载 |
| F3-5 Google Trends 真实 CSV | 待真实联调 | 公开页实际 429 后已停止；人工核对恢复后被动录控件，再同组导出 | 访问恢复与真实控件 proof | 原 CSV/哈希/地域/候选组/时段/采样时间，失败/过期降级 |
| F3-5 IG 全球累计采集代码 | 离线通过 | `f02b2ed` 已修共享 C7 持久停机/失败预算和明确 hashtag 结构 count | 无 | hard-block attach 前拒绝，429 持久化，未知结构降级，不猜通用 count |
| F3-5 IG 全球累计实际采样 | 待真实联调 | 当前 9224 429 停机保留，不能继续实采 | 人工会话/出口恢复 | 真实同名 hashtag 容器与累计值，明确全球非德国热度 |
| F3-5 德语同类账号周更代码 | 离线通过 | 独立 worker 周更近 14 天用词，共享停机/失败预算，空名单跳过 | 无 | sampling/runtime recovery：空名单、失败/过期、周任务不堵调度 |
| F3-5 德语同类账号真实样本 | 待真实联调 | 名单与访问可用后采集，不用其它账号替代 | 业务名单、探测恢复 | 真实来源/时间/用词次数；空名单当前降级明示 |
| F3-6 来源许可/费用核账 | 待真实联调 | 第三方只抓不自动花钱，许可绑来源，源变重新确认 | 业务、供应商账单 | 第三方许可、预算拒绝、不确定对账，恢复零自动付费重放 |
| F3-7 两个计数器 | 离线通过 | IG 2,200 字符/30 标签、90% 黄提示；不静默削减压缩 | UI 实际上限另观察 | emoji 字符计数、12 标签保留、长文另验 |
| F3-3 两个设置保存 | 离线通过 | 仅 `default_times`/`snooze_default_days`，CAS 保留注释/其它键；默认柏林 10:00/17:00、3 工作日 | 无 | `tests_operating_settings`：未知字段/过期/格式冲突拒绝，运行读取新默认 |
| F3-3 设置保存后缓存失效 | 离线通过 | `operating_settings.save()` 在 `os.replace` 成功后显式让 `cfg()` 缓存失效——等长改写（`["10:00","17:00"]→["11:30","18:00"]`、`3→4`）`st_size` 不变且 68% 情况下 `st_mtime_ns` 也相同，长跑进程会继续用旧默认值 | 无 | `tests_operating_settings`：同时间戳改写、失败写入不得让缓存失效 |
| F3-3 受控配置说明 | 离线通过 | `controlled_fields`/`editable_help` 已带配置原注释，安全/映射/模板只读 | 无 | 设置 + 冻结恢复 12 项：注释归属、引号内 `#`、隐藏配置不泄露、CAS 保留 |

三类标签来源分别验收。空名单或失败降级让人工流程可继续，不能据此删掉采集器需求。

### 10.4 阶段四：飞书主入口与运行可见性

两个接收配置组分别是德国运营和开发者；本轮各组可只有一人，不要求一定是群聊。

| 编号与验收单元 | 状态 | 代码缺口或下一步 | 外部依赖 | 验收入口 |
|---|---|---|---|---|
| F4-1 应用通道/权限 | 待真实联调 | 适配器存在，校验缺钥匙/无权限/远端失败展示 | 管理员、当前缺凭据 | 授权/收件范围、真实 message ID，不记密钥 |
| F4-2 两组/五类消息代码 | 离线通过 | 业务待审/积压/排期成功/失败与开发者告警分组，远端 I/O 在独立 worker | 两组实际目标另验 | `tests_notify`/`tests_feishu`：不串组、失败下一步、unknown 独立可见 |
| F4-3 按帖聚合/离队剔除 | 离线通过 | 同帖多问题只计一篇，挂起/终结剔除；多收件人部分失效漏发修复已复审 | 当前真实审校场景另验 | `tests_review_notifications`：有效 B 仅补给漏收者、已收者不重发、失效 A 不复活 |
| F4-3 晨间摘要/静默代码 | 离线通过 | 08:00 合并、14:00/18:00 有积压提醒；补抓/分类/延迟/失败/最早等待，无事不发 | 实际飞书晨报另验 | notifications/service：按帖计数、扫描处理事实与晨间就绪率，空轮次静默 |
| F4-4 当前内容卡片代码 | 离线通过 | 德语首图/人工优先摘要/检查，原图回退明示，上传失败仍可行动 | 真实图片/URL/权限另验 | `tests_review_notifications`：排队后改文/换图、显式原图、已排期源变仅提醒 |
| F4-3 冻结尝试/人工 resolve | 离线通过 | 有效原卡补送保持 UUID/内容；部分失效旧卡保留 cancelled，后续仅为漏收者生成有效新提醒 | 实际远端核对另验 | runtime rereview 2/2：原卡冻结、版本 CAS、多收件人不漏/不重 |
| F4-3 终态保留与归档 | 离线通过 | `keep_delivered_days=30`，完整关联终态组件按 SHA 归档，原 card/UUID/回执可读且主状态保留去重；未决/待发/离岗未发不归档 | 长期真实运行另验 | `923091f` + `tests_review_notifications`：不丢关联/漏收者、重复入队拒绝、归档损坏失败闭合 |
| F4 企业操作流程 | 待真实联调 | 代码行关闭后运营从卡片进入当前帖、审校选期、收回执 | 两组、运营机器可达链接 | 桌面/手机开卡片，运营桌面审校；实际 message ID/任务/时间 |
| F4-5 外部缺席告警 | 待真实联调 | 本地发心跳、外部检测缺席；会话被动推断，不增主动轮询 | 外部服务，当前未启用 | 关机/断网/停任务后仍告警，无事静默 |

代码缺口不能由权限行代替；云盘通过也不代表消息和审校链接通过。

### 10.5 阶段五：单渠道排期与完整月历

| 编号与验收单元 | 状态 | 代码缺口或下一步 | 外部依赖 | 验收入口 |
|---|---|---|---|---|
| F5-1/F5-2 同渠道 90 分钟 | 离线通过 | 同帖去重与占用分开，人工冲突拒绝 + 3 条建议，不擅自顺延 | 远端完整月历另验 | 90 分钟边界、不同渠道不冲突、别人正文也占槽 |
| F5-1 跨日占用比较 | 离线通过 | 按真实墙钟距离跨 ±1 天比较，不按日历日——否则 23:30 与次日 00:30 在界面上显示"附近没有"，而后端 90 分钟闸会拒 | 无 | `features/approval/occupancy` 单测 |
| F5-3 柏林时刻/实际 UI 范围 | 离线通过 | 按录证的可见窗口验证合法任意时刻、月界/DST；范围改变需重录，不永久写死自然月 | 当前 UI 范围改变后重录 | 发布/批准时区与窗口回归；未证实新月份不自动放行 |
| F5-4 七态/挂起/接管 | 离线通过 | `actor: null`、3 上海工作日、源变唤醒、理由与终态分计 | 通知联动另验 | `tests_review`/`tests_web_review`：`handed_off` 不计 `skipped`，模糊回执不重开提交 |
| F5-5 本次完整月份读取 | 真实通过 | 2026-09-13T05:01:33Z ready，35 格 8/30–10/3，4 条公开帖 / 2 个推荐时段区分，3 个独立 IG remote ID | 当前 9223 | **仅限本次已观测月份形态** |
| F5-5 月历展示/公开观察代码 | 离线通过 | 截至时间/范围/完整性/stale/busy，公开以 remote ID 和明确观察为准，不按时钟推算 | 真实新 scheduled 形态另验 | runtime recovery/浏览器 UI：未知不报公开，建议不计帖子 |
| F5-6 提交前实时复核 | 离线通过 | 缓存仅提示，持发布锁重读；未读完不能判空档 | 完整月历真实输入另验 | 缓存后新增人工项造成冲突，busy/不完整即拒绝 |
| F5-7 人工/机器修改循环 | 离线通过 | 单篇文案/tag/link/优化，人工真相独立，机器候选另存 | 具体内容 | 人工文案/图、源变更、旧候选回归 |
| F5-8 ZIP/人工结转 | 离线通过 | 包完整后 `handed_off`；缺德语图标原图；同一冻结投影 | 公开链接可选 | 导出中断不转态、重下稳定、人工回填不伪造自动回读 |
| F5-9 发布锁/不确定恢复 | 离线通过 | Web/CLI/Planner 同锁，中断只补证据充分投影，不重提 | 真实 Planner 核对另验 | `tests_publication_recovery`：重复恢复、缺冻结版、ambiguous 阻塞 |
| S2-1 单渠道控件录证 | 真实通过 | 仅证明当前渠道/目标控件，不证明提交验收 | 当前 9223 | `channel_controls.json`、被动 probe、FB/IG 显示名 |
| S2-1 单渠道/全文/身份/时刻回读 | 离线通过 | 生产上层直接接 `month_inventory`/`month_readback`，精确全文/完整月份/唯一渠道 remote ID；P1/P2 复审关闭 | 新受控排期真实验收另列 | `tests_publish` 末 15 项定点通过；不从旧双渠道 journal 推新 G8 |
| **S2-1 远端 scheduled 图片读取** | **代码未完成** | **尚无真实排期详情图片控件证据，适配器未实现**；须受控排期录证后补数量/顺序/来源图片绑定 | 有效样本、具体提交确认、真实详情控件 | 编辑器两图核验不能关闭；完成适配/失败闭合测试后再验远端图片 |
| S2-1 G8 媒体验收闸 | 离线通过 | 同时要求全文相等、`remote_images_verified`、正整数媒体数及有序 source SHA 与冻结清单一致；严格结构拒绝伪通过 | 远端适配尚缺 | 只有 `scheduled`/ID/编辑器图不足，历史 `scheduled` 继续防重 |
| S2-1 编辑器媒体准备 | 真实通过 | 2026-09-13T06:01:17Z FB-only 技术草稿 2 张 1536×2048 原图，数量/顺序视觉核验通过；可能留下草稿 | 当前发布浏览器 | 两图 dHash/RGB 误差均 0、宽高比比值 1；**未提交，不替代远端排期图片证据** |
| S0-2 长文/多图/链接/CTA | 待真实联调 | 分别准备长正文、多图、FB 链接、IG CTA，核最终快照与远端内容 | 两篇实际内容与用户确认 | 长文完整、图数量/顺序/字节、FB 德国落地页、IG 话术独立记录 |
| F5 两篇实际单渠道排期 | 待真实联调 | 代码/证据前置关闭后各提交一次并回读 | 确认文案/每张图/账号/单渠道/柏林时间 | 分别 remote ID/渠道/目标/时刻/fingerprint；现无本轮新提交 |

### 10.6 接口、前端与最终验收

| 验收单元 | 状态 | 尚需完成 | 证据要求 |
|---|---|---|---|
| 历史/恢复/风险采样/设置 API | 离线通过 | range/page/total、`detail.meta` 来源指纹/`snapshot_id`、批次与消息恢复、风险/采样元信息和设置说明已接 | 现有接口/版本错误与只读回归 |
| Web/CLI 五阶段只读状态 | 离线通过 | 共用 snapshot，含批次 `operation_id`/费用/版本、处理时效、Trends blocked 和独立投递状态 | `tests_runtime_status` + 浏览器 UI 夹具；只读 GET 零外部操作 |
| React 审校台迁移 | 离线通过 | `web/ui-next/` 全量替换旧 Vue 界面；路由改为真实路径，深链接可刷新可分享 | React 498 项单测 + `tests/browser_regression.py` 12 组场景 |
| 前端静态部署契约 | 离线通过 | `SinglePageFiles` 提供 SPA 回落，且不吞 `/api` 404、带扩展名的缺失资源和非 HTML 客户端 | `tests/tests_spa_static.py` 14 项；退回裸 `StaticFiles` 会红 11 项 |
| 切换与回滚演练 | 离线通过 | 真实 FastAPI 伺服真实 dist，两个方向各演练一次 | `tests/cutover_rehearsal.py --dist web/ui-next/dist` 与 `--dist web/ui/dist` |
| 历史页缩略图成本 | 待真实联调 | 已知项，非回滚触发条件：首屏每行一次缩略图 GET，React 默认 50 行、旧 Vue 30 行。生产归档（1,067 篇）实测中位 1.57s、最大 2.0s，六连接下首屏约 12–13 秒 | 观察一个工作日；太慢先把每页改成 20 条 |
| 最终 Python + 前端构建 + 浏览器回归 | 离线通过 | 切换后最新一轮 Python 66/66、React 498/498、typecheck 干净；浏览器阶段 I 与 React 演练均通过 | `tools/test_offline.py` 给各脚本独立 archive/state/环境 |
| 运营飞书到审校排期闭环 | 待真实联调 | 运营从真实卡片开帖、改审文图标签链接、选期确认、收到回执 | 两渠道排期证据 + 实际经过；两个独立 CLI 调用不算闭环 |

### 10.7 接下来的收口顺序

1. 人工核对旧不确定提交，以及 IG `.global` / Google Trends 的会话与出口；
2. 恢复访问后建立 `.global` 回填与三来源真实样本，同时补齐飞书凭据、云盘目录与运营可达的审校 URL；
3. 完成并确认受控样本，录到真实 scheduled 详情图片控件后补回读适配；
4. 两渠道完整验收与运营完整流程。

**样本现状**：FB 有一份"Cat or CCTV"单图待制作包（提案、原图、指令齐备，缺最终德语图，拟定时刻未占位）；过期 8 月活动、美元促销和技术草稿都不是可直接发布的样本；IG 尚无 `.global` 新素材。FB 旧译文 prompt 5、现版 6，当前无可接受德语图，原 FB 目录未发现 `media_de`；冻结 `.tech` 的 3 张历史德语图不能移用。
