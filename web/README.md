# 审校台

审校台读取本地归档，并把人工文案、本地化选择、分类标签和审校决定写入真实追加式真相源。`web/api/fake_writer.py` 与旧 `_fake_state.json` 只是原型遗留，当前 FastAPI 路由不使用它们。

业务规则以 [../docs/FUNCTIONALITY.md](../docs/FUNCTIONALITY.md) 为准，接口和界面约束看 [DESIGN.md](DESIGN.md)。

## 当前能力与限制（2026-09-12）

规划阶段前端构建和相关离线测试通过，但当前 worktree 没有 archive/state 数据，未做真实企业或浏览器联调。

| 能力 | 状态 | 说明 |
|---|---|---|
| 当前任务列表与详情 | 离线通过 | 从 archive/账本读取；无数据时为空 |
| 人工文案、正文/标签/链接编辑 | 离线通过 | 写 `translated_human.jsonl` 与 `localization.jsonl`，带版本检查 |
| 七态审校 | 离线通过 | 挂起、不发、人工接管等写 `review_items.jsonl`，`actor=null` |
| ZIP 资源下载 | 离线通过 | 完整生成后才记录 `handed_off` |
| Planner 月历 | 离线通过 / 待真实联调 | 只读缓存和显式刷新；与提交共用发布锁 |
| 审校通过并排期 | 待真实联调 | 走真实批准路径；单渠道证据缺失时应失败闭合 |
| 最近 90 天历史 | 代码未完成 | 当前尚无完整历史入口；底层历史不得裁剪 |
| 损坏审校账本处理 | 代码未完成 | 当前读取会跳过坏行，应改为显式失败 |
| 风险预扫描 | 代码未完成 | `fixtures/risks.json` 是手写演示数据，不是实时/真实结果 |
| hashtag 热度 | 部分离线通过 | 排序/存储存在，真实 samplers 缺失 |
| 跨月排期范围 | 代码未完成 | 当前月逻辑存在，需按 Planner 可见范围动态适配 |

## 启动

开发机构建前端：

```powershell
cd web\ui
npm install
npm run build
cd ..\..
```

启动服务：

```powershell
.venv\Scripts\python.exe -m uvicorn web.api.app:app --host 127.0.0.1 --port 8765
```

打开 `http://127.0.0.1:8765/`。API 文档在 `/api/docs`。

生产机运行静态构建不需要 Node；生产迁移本轮明确延期。若拷贝 `web/ui/dist/`，必须与同一提交的 API 一起部署，避免前后端契约漂移。

## 使用规则

- 当前 FB/IG 任务独立，不在 UI 合并。来源渠道是唯一发布渠道。
- 保存采用来源哈希和 revision 乐观并发控制；409 表示页面过期，应刷新后重做。
- 人工文案和图片优先，后台重建不能覆盖。
- 挂起默认三个上海工作日；“不发”必须有理由；“人工接管”与“不发”是不同终态。
- `scheduled` 只表示 Business Suite 回读确认排期，不表示帖子已公开。
- 人工选择的柏林时刻若同渠道前后 90 分钟有占用，直接拒绝并给建议，不自动顺延。
- 每个发布动作都必须在最终确认对话框展示冻结后的文案、图片、账号、唯一渠道和时刻。

## 风险夹具

`web/api/fixtures/risks.json` 只用于稳定演示黄色风险标记。它不来自当前帖的模型扫描，不能参与批准、排序、飞书卡片或验收。正式风险结果必须绑定账号、post ID、来源文本哈希、模型/规则版本和生成时间；来源变化后旧结果应过期。

## 数据错误

当接口报告来源版本、审校 revision 或锁冲突时刷新页面。若报告账本损坏，停止写入并保留 `review_items.jsonl`；不要删除坏行或把文件置空。当前损坏行仍可能被静默跳过，因此真实数据接入前要先完成 [../docs/OPTIMIAZATION.md](../docs/OPTIMIAZATION.md) 的 F5-4。

## 发布边界

2026-09-01 的历史 Business Suite dump 没有单渠道勾选证据。当前发布代码必须在打开浏览器前拒绝缺证据的提交，不能退回默认双渠道选中。完成新的 FB-only / IG-only probe、实现对应选择与回读、并让用户审查具体内容后，才能做真机排期验收。
